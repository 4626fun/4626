# AGENTS.md

## Cursor Cloud specific instructions

### Architecture overview

4626 (4626.fun) is a monorepo with two primary dev loops:

| Component                     | Directory           | Toolchain                      | Dev command                  |
| ----------------------------- | ------------------- | ------------------------------ | ---------------------------- |
| **Frontend SPA + Vercel API** | `frontend/`         | Vite 7 + React 19 + TypeScript | `pnpm -C frontend dev`       |
| **Solidity contracts**        | `contracts/` (root) | Foundry (forge)                | `forge build` / `forge test` |

Optional components: XMTP Keepr Agent (`frontend/server/agents/eliza/`), KPR automation (`kpr/`), Docs site (`apps/docs-site/`), Solana program (`programs/creator-share-hook/`).

### Default working style

Use `.cursor/rules/product-builder-workflow.mdc` as the generic feature-shaping workflow: clarify the problem, reduce the MVP, design a simple system, choose the smallest proven stack, break work into steps, then implement and iterate. Prefer speed, clarity, and maintainability over enterprise-style overengineering.

### Rule precedence

`AGENTS.md` is the repo-level authority for architecture, operations, and cross-cutting product invariants.

Path-scoped or topic-scoped rules in `.cursor/rules/*.mdc` are authoritative inside their scope and override the generic builder workflow when they conflict. In particular:

- `.cursor/rules/product-builder-workflow.mdc` owns the generic feature-shaping workflow when no stricter domain rule applies.
- `.cursor/rules/4626 secur-agent guardrails for repo-native implementation.mdc` adds generic secure-automation process guidance without overriding product-specific invariants.
- `.cursor/rules/ERC-4337-Wallet-Invariants.mdc` owns canonical wallet/account selection (`profiles.csw_address`, `CANONICAL_CSW_ADDRESS` policy pin).
- `.cursor/rules/csw-agent-lifecycle.mdc` owns CSW delegation, XMTP identity, ERC-8004 identity, and deploy-session wallet mechanics.
- `.cursor/rules/waitlist-onboarding-simplicity.mdc` owns waitlist/signup simplification inside its scoped auth and waitlist files.
- `.cursor/rules/frontend-seo-core.mdc` and `.cursor/rules/frontend-seo-internal-linking.mdc` own frontend SEO policy inside `frontend/`.
- **`docs/ACCOUNT_MODEL.md` is the canonical reference for the 4626 account model** — user populations, identity invariants, the `command_issuer_execution_context` schema, and the existing-flows inventory (e.g. `setPayoutRecipient` is already part of the deploy phase-2 batch). Read it before writing any design doc that touches account, wallet, signer, sub-account, or paymaster behaviour.

Do not preserve legacy routes, aliases, or compatibility shims just for backward compatibility. When replacing a path or interface, migrate active callers and remove the old surface unless product explicitly requires a staged rollout.

### Security and trust-boundary rules

These are repo-level guardrails for internal automation, deploy orchestration, and Telegram identity flows.

Canonical maintainer references:

- `docs/security/mutable-surface-inventory.md` — authoritative write-surface map (auth model, guardrails, rollback posture).
- `docs/security/historical-risk-review.md` — quarterly historical-drift checklist for env/authority/schema/runbook drift.

- **Deploy status and preflight paths must be read-only.** They may gather config, build payloads, and report readiness, but they must not provision infrastructure, register tokens, or perform onchain mutation as a side effect.
- **Internal Solana mutation paths must require machine auth.** Do not fall back to ambient user sessions, cookies, wallet auth headers, or admin login state for route provisioning, token registration, or other mutating Solana setup.
- **Telegram Mini App link completion must require fresh Mini App session proof.** Shared secrets or server-side toggles must not bypass Telegram session verification for public Telegram-launched linking flows.
- **Telegram link-start tokens must be single-use, claim-bound, and consumed on success.** Do not leave link intents replayable across users or sessions until expiry.
- **Group-scoped Telegram message actions must be owner-scoped.** Deletion, refresh, pause, or other controls on shared bot messages and live cards must only be executable by the actor who created or owns that surface, unless product explicitly wants collaborative controls.

### Agent validation and editing discipline

These are repo-level process rules for any agent (cloud or local) editing this codebase. They exist to prevent the repeatable failure modes observed during model confidence tests.

#### Validation honesty

Agents must report validation results exactly.

- If a command fails, say it failed. Include the exact command, exit code, and error text.
- If the failure appears unrelated or pre-existing, identify it as "failed with likely pre-existing error" and include the exact file, line, and error message. Do not claim the gate passed.
- Do not summarize a failed gate as passed, even if the failure is in a file you did not touch. A targeted test passing does not imply `typecheck`, `lint`, or the full suite passed.
- Do not omit failed validation commands from the final report. Every validation command run must appear in the results section with its actual outcome.
- When a multi-stage command (e.g. `tsc ... && tsc ...`) short-circuits, fixing the first stage can surface previously-masked errors in the second stage. Report the newly-surfaced errors honestly as pre-existing if they predate your change, and do not claim the overall command passed until both stages are clean.

#### Pre-edit checkpoint for wallet / auth / XMTP / deploy / swap / canonical-CSW changes

Before editing files in wallet, auth, XMTP, deploy-session, swap execution, or canonical CSW code, an agent must summarize (in its response or plan, before any edit):

1. Exact invariant being changed or tested.
2. Files inspected (with paths).
3. File(s) proposed for modification (with paths).
4. Why the change is the smallest safe diff.
5. Whether the change is test-only or production code.
6. Targeted validation command to run first (e.g. `pnpm -C frontend exec vitest run <file>`).

Do not edit until this checkpoint is complete. This is especially important in 4626 because user-initiated frontend execution and server-side deploy-session execution are intentionally orthogonal (see `.cursor/rules/ERC-4337-Wallet-Invariants.mdc` § Execution address depends on the execution track). A change that blurs that boundary can route sponsored swaps through the wrong sender or gate legitimate frontend execution behind server-session checks.

#### Regression test quality

For invariant tests, prefer concrete behavior assertions over prose/string assertions.

Strong assertions include:

- return value shape (e.g. `{ name, avatar } | null`)
- routing mode and fallback mode
- sender identity and execution method
- called/not-called boundary mocks (e.g. `sendCoinbaseSmartWalletUserOperation` not called for sub-account sends)
- env precedence (override honored, invalid override ignored)
- consumer behavior (e.g. `getAgentIdentity` returns `null` for a Privy embedded EOA)

Weak assertions include:

- checking only that a debug `reason` string contains or omits words
- testing comments, labels, or incidental wording
- adding broad tests that do not fail for the intended regression

If a resolver is already tested, prefer testing an untested consumer of that resolver rather than duplicating resolver-level coverage. For routing boundaries, assert mode / sender / method / called-not-called behavior where the code under test supports it.

#### Local LLM / provider configuration isolation

Local development model configuration for Cursor, Aider, Hugging Face Router, OpenAI-compatible endpoints, or BYOK must not modify production runtime behavior.

Do not change:

- Hermit runtime model config (`HERMIT_AGENT_*`, `/api/hermit/draft` provider wiring)
- Eliza runtime model config
- XMTP production agent behavior
- deploy-session runtime model/provider settings
- production env names for canonical CSW, Privy, XMTP, or paymaster behavior

If a user asks to configure a local coding assistant, limit changes to local docs, ignored local env examples, or user-specific editor settings. This matches the existing distinction: the Hugging Face Inference Router is for local Cursor/Aider only (`OPENAI_API_BASE=https://router.huggingface.co/v1` in shell profile or Cursor Settings), not for production Hermit/Eliza lanes.

### Running services

- **Frontend**: `cd frontend && pnpm dev` starts Vite at `http://localhost:5173/`. Hot-reloads on file changes. The app is in waitlist mode by default — unauthenticated routes redirect to `/` or show the waitlist modal.
- **Contracts**: `forge build` to compile, `forge test` to run all 72+ Solidity unit tests. Foundry must be on PATH (`$HOME/.foundry/bin`).
- **XMTP Keepr agent**: production runs on Railway only, as a single primary XMTP consumer. Do not introduce a standby or second live deploy target unless product explicitly changes that operating model.
- **Telegram is not the live Eliza transport.** Telegram bot updates and Mini App flows remain separate from the Railway XMTP runtime, even when they reuse shared agent-core helpers.

### Lint / test / typecheck

Standard commands are documented in `frontend/package.json` scripts:

- `pnpm -C frontend lint` — ESLint (clean — 0 warnings, 0 errors)
- `pnpm -C frontend lint:a11y` — jsx-a11y rules at **error** (alias of `lint`; see `frontend/eslint.config.js`)
- `pnpm -C frontend smoke:a11y -- --serve` — Playwright + axe on `/faq`, `/faq/how-it-works`, `/waitlist`, `/swap` (fails on serious/critical). With `--serve`, `frontend/scripts/a11y-smoke.ts` restarts Vite per host shell (`marketing` for FAQ/waitlist, `app` for swap). CI: `.github/workflows/accessibility.yml` (non-blocking until repo var `A11Y_CI_BLOCKING=true`). Checklist: `frontend/docs/accessibility.md`
- **Marketing-host a11y + wagmi:** `4626.fun` has no `WagmiProvider` — do not use `TokenImage` / wagmi hooks on `MARKETING_ONLY_ROUTES` (e.g. `/faq/how-it-works` uses static story badges). Secondary copy on dark cards should use `text-zinc-400` (not `zinc-500`/`zinc-600`) for WCAG contrast; audit with `A11Y_BASE_URL=https://4626.fun pnpm -C frontend smoke:a11y`.
- `pnpm -C frontend typecheck` — TypeScript (clean — 0 errors)
- `pnpm -C frontend test` — Vitest (289 tests, all passing)
- `forge test` — Foundry unit tests (72 tests, all passing)
- `pnpm -C frontend guard:schema` — blocks raw `CREATE TABLE` / `ADD COLUMN IF NOT EXISTS` strings in `frontend/server/` production code (enforced in CI; see schema rule below and `frontend/scripts/guard-no-raw-schema-ddl.mjs`).
- `pnpm -C frontend guard:canonical-csw` — blocks retired `XMTP_AGENT_CSW_*` / `VITE_AGENT_XMTP_ADDRESS` env reads, stray pre-migration `0x4beabd…` CSW literals, direct `process.env.CANONICAL_CSW_*` outside `canonicalCswEnv.ts`, and retired keys in `.env.example` (enforced in CI; self-test: `pnpm -C frontend test:guard:canonical-csw`).
- `pnpm security:local` — optional sweep: `forge test`, frontend lint/typecheck/test, Semgrep on `frontend/api` + `frontend/server/_lib` (needs Docker). Script: `scripts/security-audit-local.sh`.
- **Security CI:** `.github/workflows/security-scanning.yml` — gitleaks, pnpm audit summaries, **blocking** Semgrep on that API surface, Slither (report-only). PRs: `.github/workflows/dependency-review.yml` (high+ vulns, runtime **and** development scopes; enable Dependency graph per `docs/audits/github-supply-chain-setup.md`). Index: `docs/audits/README.md`.

**June 2026 x-ray contract audit pass (full "for all" execution) + follow-ups completed:** 
- Complete P0/P1 review of `review-todo.md` (DeploymentBatcher, CreatorOVault + modules, ShareOFT/Oracle/Lottery, Solana NAV, invariants, cross-contract).
- P2 test gaps closed (new `test_partialPhase1Stuck_thenReset_allowsRetry` in `DeploymentBatcher.ThreeWaySplit.t.sol` + references to existing hostile withdraw/replay coverage).
- Follow-ups executed and verified: CLM size warn-guard hardened (24,450 threshold + PR "size budget review" policy), SC hygiene guard added to `security-audit-local.sh` (CLM headroom + contracts canonical terminology), lint fixes in `TacticalTokenMap.tsx`, docs updated.
- Re-run of `scripts/security-audit-local.sh` (post-fixes) exit 0; hygiene/lint/typecheck clean.
See `docs/audits/x-ray/contract-audit-pass-2026-06.md`, updated `review-todo.md`, and `docs/operations/contract-size-gate.md`.

### Non-obvious caveats

- **Git submodules are required** for Foundry compilation. Run `git submodule update --init --recursive` after cloning. The submodule tree is deep (Uniswap CCA/Liquidity Launcher pull in many transitive submodules) and takes ~2 minutes.
- **Two separate pnpm lockfiles**: root `pnpm-lock.yaml` (Solidity deps like OpenZeppelin, LayerZero) and `frontend/pnpm-lock.yaml` (frontend deps). Install both: `pnpm install` at root, then `pnpm -C frontend install`.
- **Foundry path**: After installing via `foundryup`, binaries are at `$HOME/.foundry/bin`. Add to PATH or invoke directly.
- **`.env` files**: Copy `.env.example` at root and `frontend/.env.example` for local dev. Most env vars are optional for basic frontend dev — the app runs without external service credentials but wallet/auth features require Privy, Supabase, etc.
- **API routing**: Vercel API routes go through `frontend/api/[...path].ts` dispatching to `frontend/api/_handlers/_routes.ts`. New endpoints must be registered in the static route map (no dynamic imports).
- **`pnpm.onlyBuiltDependencies`** is configured in `frontend/package.json` to avoid interactive `pnpm approve-builds` prompts.
- **Waitlist/marketing page on localhost**: By default, `localhost` is treated as the "app" domain and redirects unauthenticated users to `4626.fun`. To test the waitlist/marketing page locally, set `VITE_HOST_MODE_OVERRIDE=marketing` and `VITE_MARKETING_ORIGIN=http://localhost:5173` in `frontend/.env`. This is already configured in the Cloud Agent `.env`.
- **Railway-only XMTP primary**: the Eliza/XMTP runtime is intended to have exactly one live Railway primary with `AGENT_RUNTIME_ROLE=primary`, `AGENT_CONSUME_XMTP=true`, and `numReplicas = 1`. Local standby mode is for inspection only. If a Railway redeploy crashes, expect downtime until restart or rollback; there is no default standby failover.
- **Keep app-shell providers quiet by default**: route-scoped or user-intent-gated mounts are preferred over eager global mounts. Current examples: `/api/auth/admin` only resolves on `/admin`, `AccountContextProvider` is mounted in the layout subtree rather than the outer app root, and chat/XMTP only mounts after explicit chat intent or deep-link context. **`Layout` must gate `useSiweAuth()` and `useAccountTrayPortfolio()` behind the app interactive layout only** — marketing shells (`/faq`, `/waitlist`, and public `/add` before the account route) render outside `PrivyClientProvider`; calling Privy hooks in the default `Layout` crashes with "must be used within PrivyProvider". **Exception:** marketing `/waitlist` group chat must wrap `XmtpChatProvider` in `AccountContextProvider` inside `WaitlistGroupChatPanel` — that route is outside `AuthenticatedAppLayout`, and `XmtpChatProvider` calls `useAccountContext()` internally.
- **`/swap` should not background-refresh idle quotes**: quote on input changes, then rebuild stale quotes during review/submit if needed. Avoid reintroducing timer-driven idle re-quote loops.
- **Do not add new ad hoc session polling around `useSiweAuth()`**: session restoration already dedupes shared `/api/auth/me` work and keeps a short-lived shared in-memory session snapshot so SPA route/provider remounts do not briefly fall back to signed out. New auth consumers should reuse the existing hook/provider path instead of layering separate refresh effects.
- **Railway primary must fail fast if misconfigured**: standby mode or `AGENT_CONSUME_XMTP=false` on Railway is a startup error, not a healthy passive mode. When Postgres is configured, the DB-backed runtime lease lock is expected to stay enabled for the Railway primary.
- **Transaction routing has two `executionMode` values and four send modes.** User-initiated frontend code paths branch on `executionMode: 'canonical' | 'eoa'` (exported from `frontend/src/lib/uniswap/walletMode.ts`). `txRouter` (`frontend/src/lib/tx/txRouter.ts`) selects one of: `sendCalls` (EIP-5792 atomic batching on CSW sub-account connectors), `canonical4337` (ERC-4337 UserOp via CDP paymaster — strongest fallback for CSW), `canonicalDirect` (direct `executeBatch` on the CSW contract), or `eoaDirect` (standard `eth_sendTransaction`, one tx at a time). Only `eoaDirect` is non-atomic (approval and swap sequential). Canonical approval+swap and parent-CSW fallback paths must stay locked to `canonical4337`; do not fall back to direct gas sends when sponsorship is denied. Full routing table: Section 5 of `docs/4626-connection-methods.md`.
- **Known-good sponsored ETH→token canonical swap shape:** `canonical4337` + `eth_sendUserOperation`; canonical CSW is the sender/asset owner; Privy embedded EOA signs as CSW owner; calls are `WETH.deposit()` → `WETH.approve(...)` → Uniswap swap proxy `execute(address,address,uint256,bytes,bytes[],uint256)` at `0x02E5be68D46DAc0B524905bfF209cf47EE6dB2a9` (`0x2894adf9`). The router/proxy call must have zero native value; native ETH enters only via WETH deposit. Runbook: `docs/operations/sponsored-canonical-swap-pattern.md`.

### Token identity invariants

Creator Coins and Share tokens are separate assets and must never be treated as interchangeable.

- **Creator Coin address != Share token address.** Do not infer one from the other without explicit contract lookup.
- **Creator Coin UI must use creator-coin artwork.** Never apply Share-token (vault-framed) branding to Creator Coin token rows, selectors, or quote surfaces.
- **Share token UI may use vault-branded renders.** Share token imagery can include framed/derived branding from vault context.
- **Token-kind intent must be explicit in image pipelines.** When requesting token images, pass token kind context (`creator` vs `share`) so caches and fallbacks do not cross-contaminate.
- **Token search/dedup logic must preserve token type.** If symbol/name collides, keep token-kind metadata so rendering and labels remain correct.

### Account and auth invariants

These are product-level rules, not implementation suggestions. Future auth/onboarding work must preserve them unless product explicitly changes direction.

Canonical wallet/account selection is defined in `.cursor/rules/ERC-4337-Wallet-Invariants.mdc`, and delegated signer / agent lifecycle mechanics are defined in `.cursor/rules/csw-agent-lifecycle.mdc`. This section defines the product-facing account states layered on top of those rules.

Canonical architecture reference: `docs/4626-connection-methods.md` — describes the three connection methods (CSW, external EOA, Telegram), the CSW address model, and the `executionMode` / send-mode routing table.

Wallet-role model for user-facing docs and copy:

- **Canonical CSW (parent) = identity + custody source of truth** (`profiles.csw_address`).
- **Canonical CSW (parent) = primary execution sender for sponsored canonical swaps** (`canonical4337` + embedded EOA owner on that same address).
- **One wallet, many roles** — for the 4626 canonical account, `profiles.csw_address` === `CANONICAL_CSW_ADDRESS`: XMTP agent 4626 inbox, Railway Keepr, AKITA vault owner, and swap/owner-install are **labels on the same CSW**, not separate accounts. Do not reintroduce "agent CSW" as a second wallet.
- **Privy embedded EOA = primary signer for parent-CSW sponsored UserOps.**
- **Base sub-account = optional app-scoped execution lane; keep hidden unless a route is actively using it as sender** (flag-gated swap-only fallback, not deploy).
- **Connected external EOA = fallback/override signer lane.**
- **Privy server wallet = delegated server-side signer for automation/deploy-session tracks** (signer identity, not the asset-holding CSW).
- Keep this role split explicit in docs and UI copy; do not collapse signer role into canonical identity language.

- **Verified email is the canonical 4626 identity and recovery key.**
- **No 4626 account is considered fully created until email OTP verification completes.**
- **All entry points converge to the same account model**: website, Base app, and Telegram must resolve into one 4626 account model keyed by verified email.
- **Privy remains the auth/session backend** for email OTP and account sessions, so the embedded EOA is created through Privy during signup/auth.
- **Normal web auth should present three entry paths**:
  - email OTP first/default
  - Base Account as the wallet-native path
  - Zora cross-app as the Zora-native path
- **Telegram is a linked identity and acquisition channel, not the canonical recovery key.**
- **Telegram Mini App verification must stay enabled for Telegram-launched flows.** Its role is to prove Telegram user/chat context, not to replace verified email as the canonical account key.
- **Telegram onboarding must collect and verify email inside the Mini App.** After OTP success:
  - if the email is new, create the 4626 account through Privy
  - if the email already exists, attach Telegram to that existing account
- **Base and Zora are login/linking paths, not separate account systems.** They must still resolve into the same verified-email-based 4626 account model.
- **The Base referral flow is an acquisition path into the same canonical-wallet policy, not a second canonical-wallet model.** If a user is routed through Base app to finish CSW setup, they still return to the single canonical wallet/account rules defined in `.cursor/rules/ERC-4337-Wallet-Invariants.mdc`.
- **`linked` / `waitlist-joined` is not the same as wallet-ready.** Verified email plus successful channel binding or waitlist join can complete without immediate CSW owner confirmation.
- **`execution-ready` / wallet-ready is defined per execution track.** Product features that require wallet execution must check the right track for the user's connection method:
  - **User-initiated frontend execution-ready (CSW path, `executionMode === 'canonical'`)**: canonical parent CSW recorded in `profiles.csw_address`, Privy embedded EOA present in `profiles.primary_embedded_eoa`, and the embedded EOA confirmed as an owner/signing authority for the parent CSW. Sponsored swaps use `canonical4337` with the parent CSW as ERC-4337 sender and the embedded EOA as signer. An app-scoped sub-account may still exist in `profiles.base_sub_account`, but it is optional infrastructure and must not be shown as the primary execution account unless the active route actually sends from it.
  - **User-initiated frontend execution-ready (external EOA path, `executionMode === 'eoa'`)**: external wallet connected via wagmi, `profiles.primary_wallet` populated, no sub-account (EOAs are not smart contract wallets). The EOA address is the execution address.
  - **Server-side agent / deploy-session execution-ready**: follows the direct owner delegation path in `.cursor/rules/csw-agent-lifecycle.mdc`. **Deploy-session** uses the **creator's canonical CSW** (`profiles.csw_address`) as ERC-4337 sender with a temporary Privy server wallet via `addOwnerAddress`. **Railway XMTP / Keepr / ERC-8004** use the **4626 canonical CSW** (`CANONICAL_CSW_ADDRESS`, same as that account's `profiles.csw_address`) with the long-lived delegated Privy server wallet. Both tracks are orthogonal to user-initiated frontend execution above.
- **Features that require wallet execution must stay gated until the account is `execution-ready` on the correct track.**
- **If the user does not yet have a CSW, route them to Base app with the referral flow, then resume embedded-owner signing setup for the canonical parent CSW when they return. Do not make waitlist onboarding explicitly create a sub-account.**
- **Cross-account Telegram conflicts must not auto-merge silently.** If a Telegram identity is already attached elsewhere, force explicit recovery/merge UX.
- **Website sign-in should use email OTP by default.** Do not assume Telegram is the primary website login flow unless product explicitly changes this rule later.
- **Do not preserve legacy auth paths just for backward compatibility.** If an old path conflicts with these invariants, remove or migrate it.

### Canonical CSW identity (one wallet per account)

- **`profiles.csw_address`** is the canonical parent Coinbase Smart Wallet for each 4626 account — identity, custody, and (when execution-ready) the default `canonical4337` sender.
- **`CANONICAL_CSW_ADDRESS`** (`0xAb6d5C10b03300326cd7fab7267ae192842967b5`) is the **same wallet** for the 4626 canonical account: policy in `frontend/src/wallet/canonicalWalletPolicy.ts`, runtime env via `frontend/server/_lib/wallet/canonicalCswEnv.ts`, client inbox via `frontend/src/lib/xmtp/agentXmtpAddress.ts`. XMTP agent 4626 inbox, Railway Keepr ERC-4337 sender, AKITA vault owner, sponsored swaps, and owner-install all use this address — not a parallel "agent CSW." Hard cutover **2026-04-23** from `0x4beabd0afbcc2f0440cdef1c3c745d43fae704ef`; retired env `XMTP_AGENT_CSW_*`, `XMTP_AGENT_PRIVY_WALLET_ID`, `VITE_AGENT_XMTP_ADDRESS` → `CANONICAL_CSW_*` and optional `VITE_CANONICAL_CSW_ADDRESS`. **Regression guard:** `pnpm -C frontend guard:canonical-csw` (CI); Railway `validateStartupEnv` **errors** on retired keys; doctor + Eliza early logs flag drift.
- **Other accounts** (creators on 4626) have their **own** `profiles.csw_address` — same canonical-CSW role, different on-chain address. Deploy-session and creator flows use that row.
- **`4626.base.eth`** resolves to operator **display** context (`0xB05Cf01231cF2fF99499682E64D3780d57c80FdD`), not the canonical CSW — do not use Basename resolution as custody or swap truth.

### Canonical Lane Terminology

All docs, UI copy, commit messages, and code comments that reference 4626's value lanes must use the canonical terms defined in `docs/audits/creatorvault-business-logic-core-structure-audit.md`. These lanes have separate triggers, units, custody domains, and authorities — using generic terms creates product-truth ambiguity.

- **`tradeFeeCollector`** — destination domain for ShareOFT/hook **trade-fee** routing (native ShareOFT `SwapOnly -> non-SwapOnly` plane and the optional hook fee plane).
- **`creatorCoinPayoutRecipient`** — CreatorCoin **external earnings** routing (`payoutRecipient`). In router mode this feeds `PayoutRouter.convertAndQueue(...)` and ends in holder PPS accretion, not a direct creator treasury spend.
- **`creatorTreasury`** — destination for the **creator ongoing lane** from `CreatorGaugeController.creatorShareBps`. This lane is disabled by default (`creatorShareBps = 0`). If enabled, `creatorTreasury != 0x0` is enforced by `setFeeSplit(...)` / `setCreatorTreasury(...)`.
- **`jackpotCustodian`** — the gauge (`CreatorGaugeController.jackpotReserve`, vault-share units). Gauge custodies reserves but does not select winners.
- **`jackpotPayoutAuthority`** — `CreatorLotteryManager`. Manager selects winners and calls `payJackpot(...)` into the gauge. Custody vs authority must always be split in docs and code.
- **Voter/protocol branch** — `protocolShareBps` from gauge split. Preferred route: `VoterRewardsDistributor.notifyRewards(...)`. Fallbacks: protocol treasury, then jackpot fallback. Do not call this "protocol share = treasury only".

**Naming policy:**

- **Never** use bare `payoutRecipient` in docs/UI; always qualify as `tradeFeeCollector` or `creatorCoinPayoutRecipient`. Onchain identifiers named `payoutRecipient` are fine in contract code but must be contextualized in docs.
- **Never** use "externalRevenueRecipient"; say `creatorCoinPayoutRecipient`.
- **Never** say "creator earnings" without naming the lane (`creator ongoing treasury lane` or `external revenue accretion lane`).
- **Never** conflate jackpot custody and payout authority into a single "lottery wallet".
- "Buy+sell fee" claims are **conditional on active hook configuration**. Do not present ShareOFT native transfer fees as guaranteeing a sell fee; the sell-side fee plane requires the hook to be enabled and aligned.

**Canonical completion truth:** vault settlement completion lives in `/api/keeper/sweep` (`frontend/api/_handlers/keeper/_sweep.ts`, delegating to the legacy KPR handler during sunset). `kpr/actions/cca-finalization.action.ts` is non-canonical and must not write DB `settledAt`. The five mandatory invariants in the audit §5.1 gate `settledAt`:

1. `CCALaunchStrategy.feeRecipient == expected tradeFeeCollector` and `CreatorShareOFT.gaugeController == expected tradeFeeCollector`.
2. CreatorCoin `payoutRecipient` equals expected mode target; in router mode `PayoutRouter.burnStream` matches expected burn stream.
3. If `creatorShareBps > 0`, `creatorTreasury != 0x0`.
4. `sweepCurrency` + `migrate` both succeed; hook config either applied or explicit `awaiting_owner_hook_config` stage with no settled mark.
5. DB `settledAt` is only written when the completion stage is `completed`.

Keep `KEEPER_ENFORCE_COMPLETION_INVARIANTS` and `DEPLOY_ENFORCE_PHASE2_INVARIANTS` enabled in production. Any override must emit an operational alert and must not be accompanied by public "fully live" claims.

## Telegram Mini App Flow Rules

The Telegram Mini App account-link/onboarding flow must follow strict architectural rules to remain reliable inside Telegram WebView.

### State Management

- The Telegram flow must use a **single authoritative state machine**.
- Keep one reducer-backed source of truth plus one **state-scoped effect layer**.
- Multiple `useEffect`s are acceptable when they are keyed by explicit machine state and guarded against re-entry.
- Route/bootstrap helpers may admit the Telegram surface, but they must not mutate machine state mid-session.
- UI must render directly from explicit machine state.
- Do not use derived booleans like `isVerified`, `isReady`, etc. across multiple sources.

### Authentication & OTP

- OTP must be handled **inline inside the Mini App**.
- Do NOT use Privy modal or popup flows inside Telegram WebView.
- OTP verification success is **not equivalent** to account/session readiness or `execution-ready` wallet status.
- A distinct `wait_for_privy_sync` phase must exist.

### State Integrity

- Do not allow regression from post-verification states back to email collection unless explicitly triggered by failure/expiry.
- Do not reset email or OTP input due to unrelated auth/session updates.
- Do not remount or key major UI trees on unstable async values.

### Routing

- `/telegram/link` must not fall back into normal waitlist-gated app logic when valid Telegram context is present.
- Telegram flows must remain isolated from generic app routing decisions.

### Telegram Identity

- Telegram session proof must be verified before entering the flow.
- Telegram identity must only be bound **after canonical account resolution via verified email**.

### Current Preserved Link Path

The currently working Telegram -> Privy -> canonical-account path is preserved in
`docs/operations/telegram-canonical-link-preservation.md`.

Any simplification must keep this semantic order:

1. verify fresh Telegram Mini App proof
2. perform inline email OTP inside the Mini App
3. wait for verified-email account readiness + Privy sync explicitly
4. ensure the active Privy user is linked to Telegram
5. complete backend persistence and consume any single-use, claim-bound link-start token on success when that tokenized path is in play
6. keep canonical CSW / embedded-EOA rules intact without requiring immediate CSW owner confirmation for link success

`Link success` here means the account/channel link can complete and the user can continue the Telegram or waitlist flow.

It does **not** mean the account is `execution-ready`. Features that require wallet execution remain gated until the correct track completes per the Account and auth invariants and `.cursor/rules/ERC-4337-Wallet-Invariants.mdc` — embedded-owner signing for the user-initiated parent-CSW path, connected EOA for the user-initiated EOA path, or owner delegation for server-side agent/deploy-session automation per `.cursor/rules/csw-agent-lifecycle.mdc`.

Do not replace this with hidden provider magic, modal auth, webhook-only
binding, or a shortcut that binds Telegram before verified-email account
resolution.

### Anti-Patterns (STRICTLY FORBIDDEN)

- Privy popup/modal usage inside Telegram WebView
- Multiple competing sources of truth for verification state
- Hidden retries masking state transitions
- Rendering “verified” UI before canonical account state is confirmed
- Route guards mutating flow state mid-session

Authoritative implementation notes live in:

- `docs/operations/telegram-canonical-link-preservation.md`
- `frontend/docs/account-auth-invariants.md`
- `frontend/docs/waitlist-accounts-architecture.md`

### Solana program deployment

The `creator-share-hook` Anchor program lives at `programs/creator-share-hook/`. It is deployed to Solana **mainnet**.

| Detail              | Value                                                                             |
| ------------------- | --------------------------------------------------------------------------------- |
| Program ID          | `EjpziSWGRcEiDHLXft5etbUtcJiZxEttkwz1tqiuzzWU`                                    |
| Upgrade authority   | `7Qi3WW7q4kmqXcMBca76b3WjNMdRmjjjrpG5FTc8htxY` (from `SOLANA_PRIVATE_KEY` secret) |
| ProgramData         | `DojrYy5obEk2w9ZMpX1bLFHU4rrZqYQsZJZaXFxFGKFU`                                    |
| Binary              | `programs/creator-share-hook/target/deploy/creator_share_hook.so`                 |
| Data capacity       | 372,488 bytes (extended with 80KB headroom beyond current 345KB binary)           |
| Anchor.toml cluster | `mainnet` (see `[provider]` section)                                              |

**Upgrade procedure:**

1. Decode `SOLANA_PRIVATE_KEY` (base58) to a Solana CLI JSON keypair file (64-byte secret key array).
2. `solana config set --url https://api.mainnet-beta.solana.com --keypair <deployer-keypair.json>`
3. If the new `.so` is larger than the current program data, run `solana program extend EjpziSWGRcEiDHLXft5etbUtcJiZxEttkwz1tqiuzzWU <extra-bytes>` first.
4. `solana program deploy programs/creator-share-hook/target/deploy/creator_share_hook.so --program-id EjpziSWGRcEiDHLXft5etbUtcJiZxEttkwz1tqiuzzWU`
5. The deployer wallet needs enough SOL for the temporary buffer (~2.4 SOL for a ~345KB binary, refunded after upgrade) plus any extension rent.

**Non-obvious caveats:**

- `SOLANA_PRIVATE_KEY` is a **base58-encoded** secret key, not a JSON array. Convert before use with Solana CLI (the CLI expects `[u8; 64]` JSON array format).
- The program keypair at `programs/creator-share-hook/target/deploy/creator_share_hook-keypair.json` does **not** match the deployed program ID — do not pass it as `--program-id`. The deployed program ID is hardcoded in `declare_id!()` in `src/lib.rs`.
- Solana CLI 3.x is installed at `~/.local/share/solana/install/active_release/bin/solana`; Anchor CLI 0.31.1 is at `/usr/local/cargo/bin/anchor`.
- The Anchor IDL is at `programs/creator-share-hook/target/idl/creator_share_hook.json`. Regenerate with `cd programs/creator-share-hook && anchor idl build > target/idl/creator_share_hook.json`.

### Solana bridge on-chain config (Base mainnet)

The deployment batcher is configured for Solana bridging:

| Contract                              | Config                | Value                                         |
| ------------------------------------- | --------------------- | --------------------------------------------- |
| Batcher (`0x16aEA8…F1D8`)            | `solanaBridgeAdapter` | `0x700b4BBAf965c013123bAd02a6562FBa487aC0f1`  |
|                                       | `solanaDestination`   | `0x5f38e34e...d4d1`                           |
| SolanaBridgeAdapter (`0x700b4B…C0f1`) | `owner`               | `0xB05Cf0...FdD` (= `PRIVATE_KEY` secret)     |
| Protocol treasury (Safe 1-of-2)       | address               | `0x7d429e...f2d3`                             |
|                                       | owners                | `0xB05Cf0...` (`PRIVATE_KEY`), `0x2C1Af6B...` |

**Key access:** `PRIVATE_KEY` secret is owner of both the adapter and the treasury Safe. To call `setSolanaConfig` on the batcher, execute via the Safe (threshold=1, so single-owner signature suffices). See git history for the `cast send` pattern used.

**Deploy behavior (current):** Solana transfer setup is handled out-of-band. `finalizePhase2` does not require `meteoraAlphaVault`/`solanaIxs`, and deployment flow should pass `bytes32(0)` + empty ixs for those fields.

**Out-of-band Solana path:** Route provisioning, token registration, and Meteora ix payload generation run via the provisioner and `/api/deploy/registerSolanaBridgeToken`, separate from phase-2 finalize.

**Planned model:** treat Solana allocation as strategy-stage orchestration (alongside Charm/Ajna) rather than phase-2 finalize logic.

### Provisioner operations

Operational access details for the Solana route provisioner (hostnames, IPs, SSH users, service names, and filesystem paths) are intentionally excluded from this repository.

- **Access management:** Use team-managed secrets and approved access channels only.
- **Operational runbook:** Use the internal runbook for restart/status procedures and environment locations.
- **Repo hygiene:** Do not commit live infrastructure endpoints, SSH commands, or root-level host details.

### Solana integration: per-creator setup

The Solana route provisioner (`frontend/server/solana-provisioner/`) handles the full Solana-side setup via HTTP endpoints:

| Endpoint              | Purpose                                                                                                    |
| --------------------- | ---------------------------------------------------------------------------------------------------------- |
| `POST /provision`     | Creates bridge route via `wrap-token` CLI (legacy creator SPL adapter lane only) |
| `POST /setup-creator` | Creates Token-2022 mint with Transfer Hook + TransferFeeConfig, initializes PDAs                           |
| `POST /create-pool`   | Creates Meteora DLMM pool for the creator's share token                                                    |
| `POST /meteora-ixs`   | Builds Meteora Alpha Vault deposit instructions                                                            |

**Single-token architecture:** Meteora DLMM rejects Token-2022 mints with TransferHook extension (`UnsupportedMintExtension`). The deploy uses only the bridge-wrapped standard SPL token (created by `wrap-token`) for DLMM pools, Alpha Vault deposits, and trading. Transfer Hook functionality (lottery entries, fees) requires a separate Token-2022 mint if needed.

**Legacy adapter registration (optional):** `POST /provision` registers bridge-wrapped creator SPL on `SolanaBridgeAdapter`. **Do not use `SOLANA_AUTO_POOL`** — it is retired. Share-mesh Meteora pools use the LZ mint runbook in `docs/operations/solana-share-mesh-budget-paths.md`.

1. (Optional) `POST /setup-creator` — creates Token-2022 mint with Transfer Hook, inits CreatorConfig/PendingEntries/WinnerRecord PDAs
2. Register Meteora vault config in DB or `METEORA_CREATOR_ALPHA_VAULT_MAP_JSON` env (legacy creator-SPL Alpha Vault lane only)

**Meteora vault config** is resolved via `frontend/server/_lib/meteoraAlphaVaultConfig.ts`:

- Priority 1: DB table `creator_meteora_alpha_vaults` (auto-created on first query)
- Priority 2: `METEORA_CREATOR_ALPHA_VAULT_MAP_JSON` env var (JSON map keyed by creator token address)

### KPR keeper bots

Keeper bots in `kpr/` relay data between Solana and Base. Install: `cd kpr && npm ci`.

**Solana-specific workflows:**

- `keepr-solana-relay-entries` — relays lottery entries from Solana → Base (every 30s)
- `keepr-solana-settle-fees` — settles Solana fees → Base gauge (every 5min)
- `keepr-solana-winner-relay` — relays Base lottery wins → records on Solana
- `keepr-solana-price-monitor` — monitors Solana vs Base price deviation

**Start:** `cd kpr && tsx runner.ts` (runs all workflows). Dry-run: `DRY_RUN=true tsx runner.ts`.

**Required secrets** (see `kpr/secrets.example.env`):

- `KPR_PRIVATE_KEY` — Base signer (EOA or ERC-4337 owner)
- `SOLANA_KEEPER_KEYPAIR` — Solana payer/authority (base58)
- `SOLANA_RPC_URL`, `BASE_RPC_URL` — RPC endpoints
- `SOLANA_BRIDGE_ADAPTER` — Base bridge adapter address
- `SOLANA_CREATOR_MINTS` — comma-separated Solana mints to monitor
- `SOLANA_SHARE_OFT_MAPPING` — JSON: Solana mint → Base ShareOFT
- `SOLANA_CREATOR_COIN_TO_MINT_MAPPING` — JSON: Base creator coin → Solana mint
- `SOLANA_TWIN_TO_PUBKEY_MAPPING` — JSON: Base Twin contract → Solana pubkey

**KPR TypeScript baseline is currently clean** (`pnpm -C kpr typecheck` passes). Keep this as a no-regression launch gate.

## Learned User Preferences

- For waitlist and account-setup surfaces, stay on the product brand palette (blue primaries with dark charcoal or near-black backgrounds) rather than purple or ad hoc accent colors unless design explicitly calls for something else. Build product UI on shadcn/Radix `@/components/ui/*` plus existing Tailwind brand tokens — use `<Button>` and siblings, not raw `btn-*` classes, CDS, or parallel ad hoc styling lanes.
- Prefer progressive disclosure in multi-step onboarding: emphasize the active step, collapse or quiet completed steps, and avoid duplicate titles or redundant headers on loading screens.
- Nest optional substeps (for example extra channel binding) under the primary step with a compact expandable affordance instead of introducing separate numbered steps when the flow is still one phase.
- Keep secondary actions such as Previous Step and Retry visually quieter than primary blue actions.
- When Zora linking is the active context, prefer showing the Zora handle as the human-readable identity label over a truncated wallet address that does not match what the user expects for their canonical smart wallet.
- For waitlist/onboarding UI, prefer cleaner card treatments with minimal framing; remove heavy outer borders when they add visual noise. Avoid nesting every setup step in boxed/bordered card shells — use typography hierarchy, whitespace, and timeline rails instead of stacked nested panels.
- For exported skill/context documentation packs and Notion uploads, prefer comprehensive self-contained writeups that assume the reader does not have direct repository access; preserve source-document detail, section structure, tables, caveats, and action lists unless the user explicitly asks for a condensed edition.
- Loading and progress labels must match the actual action context — reserve action verbs (e.g., "Deploying...") for when the action is executing, not for page navigation or initial load, keep unrelated controls (for example `Reset`) from switching labels during other in-flight actions, and prefer `PixelWaveLoader`-style motion for waitlist/account-setup loading over text-scramble or four-square placeholders that feel laggy or off-brand.
- App bootstrap should use one unified loading shell with one message — avoid flicker between session sync and route/page load overlays; prefer subtle route fade-in/out transitions over hard cuts between screens. Use `AppLoadingOverlay` debounced hide (~280ms), `app-loading-scroll-lock` + `scrollbar-gutter: stable`, `AppLoadingBootstrapGate` to keep route content out of the DOM during bootstrap (with ~220ms content fade-in when loading clears), and gate `PrivyClientProvider` children during Privy init — do not add per-route custom Privy/init panels (DeployVault removed its separate "Initializing authentication" block). Route changes use `PageTransitionOutlet`; nested tab/sub-route switches (Explore, Admin) share `PageTransitionNestedOutlet` instead of duplicating transition wiring.
- Explore dashboard and hero metrics must present only verified indexed totals — no live Zora read-path blending, synthetic inflation, or implied platform-wide totals during partial backfill; label indexed scope honestly when sync is incomplete.
- For metric/analytics charts on content-detail and vault surfaces, prefer a single multi-series chart with toggleable series (price/liquidity/volume/fees) over separate per-metric charts — render volume as bars, fees as cumulative bars, and liquidity as bars split by the two pool tokens. Charts must fill the full component area without feeling cramped, and axis value labels must sit on the correct axis (Y-axis values along the vertical axis, not rendered as a horizontal row below).
- For waitlist/account surfaces, consolidate closely related panels (for example "Linked Identities" and the link-to-earn points panel) into one compact section and use real platform icons instead of text labels for channel rows.
- For nav bars, prefer edge-to-edge layout over centered max-width, text-only links without hover backgrounds, and no visual separators between nav and content.
- Prefer aggressive simplification passes in large folders: identify and remove or archive unnecessary files (including `.ts`/`.tsx`), not just documentation, to reduce complexity.
- During database/storage optimization sweeps, preserve real/raw data — remove only derived or regenerable artifacts (duplicate indexes, matviews, snapshot retention within approved windows, bloat). Data-loss levers such as dropping audit columns or pruning source rows require explicit user approval; default answer is no.
- During broad cleanup/refactor sweeps, prefer sustained autonomous execution with minimal pause-and-confirm loops, and parallelize with subagents to expedite when the sweep spans many files or folders.
- When a task comes with an attached implementation plan, pre-created todos, or a high-stakes deployment runbook, execute against that source of truth without editing it, preserve branch/version/checkpoint state exactly across pauses or resumes, and use the existing todo set (marking items in-progress/completed) instead of recreating tasks.
- Keep waitlist point awards as even integers so the 50% referral passthrough stays whole, and prefer framing the recurring daily earn action as a social share on X / Farcaster / Telegram rather than a "lottery check-in"; per-submission AMOE entry points should stay removed.
- Waitlist/marketing shells on `4626.fun` and `app.4626.fun` must expose an explicit **Sign out** control (for example `WaitlistFlow` / setup tray) so users can break Privy/session auto-restore loops; redirecting to `/waitlist` after sign-out is acceptable. On mobile, render sign-out as red text without a heavy border and keep header actions on one line.
- Waitlist leaderboard identity rows should show Basename when available, otherwise the canonical CSW address — never the Privy embedded EOA; use the Coinbase logo for CSW affordances (not a generic CSW badge) and avoid generic labels like "creator" for the primary identity string.
- Rename user-facing labels that describe CSW owner-install flows to match the parent-CSW embedded-owner architecture (e.g. "Enable 4626 signing" rather than "Connect Coinbase Smart Wallet"). Default waitlist Step 2 targets **parent CSW** embedded-owner install (`legacy-owner-install`). When `WAITLIST_SUBACCOUNT_FLOW_ENABLED=1` / `VITE_WAITLIST_SUBACCOUNT_FLOW_ENABLED=1`, Base App users without an EOA owner path see **Connect Base App** instead — sub-account is **swap-only**, not deploy.
- Waitlist signup stays **email-only Privy** by default; the path is **Continue → setup workspace** with no blocking `connect-base-app` step or dual Base-vs-email auth on `/waitlist`. Base App wallet connect belongs in the setup workspace as an optional post-verify nudge and must not block joining or auto-bootstrap on page load. **Enter App** stays admin-approval gated — optional Zora/Base/signing must not block waitlist join. Email-collision recovery (`RECOVERY_REQUIRED_EMAIL_BOUND`) must sign out the colliding Privy + server session before existing-account handoff — reusing a stale Privy token recreates the waitlist redirect loop. Never display a stored pre-auth email hint (`cv:waitlist:verified-email-hint`) as a live Privy session — use `resolveWaitlistPrivyDisplayEmail` for copy; hints are for OTP continuity only.
- Do not rewrite or redesign the premium token icon renderer — keep the existing deterministic Sharp-based pipeline, composition, premium bezel, soft glow, and recessed inner chamber, and make targeted refinements only. Primary visual goal: **subject breakout over the bezel** — extend the inner-chamber silhouette/micro-pattern **outside** the frame, not a second rear card or background frame behind the icon. Preserve top headroom so breakout elements (hats, ears) are not clipped. Never let a rectangular crop block, white background patch, or broad banner escape the frame; keep the ticker/signature bottom-right in black and always inside the frame; optically center the subject inside the chamber without flattening its depth. In `premium-v2` experiments, use fujilab/rembg subject masks for segmentation and a darker in-chamber background; keep breakout fully opaque (no translucent halo); suppress decorative corner glow (especially bottom-right); prefer brand-blue frame aura over white when tuning outer glow.
- Do not redesign `/swap` into a new layout — keep the Uniswap-like information architecture and only improve hierarchy, spacing, microcopy, states, accessibility, and responsiveness within it. The token selector modal may use Uniswap-style search, quick-pick chips, per-row balances, and clear loading/empty states without changing swap-page IA; pin AKITA under a **Trending** section when present; use collapsible **Creator coins** vs **Content coins** groupings plus an optional **Hide balances under $0.01** dust filter; use the creator/display name as the primary row label with on-chain symbol + shortened address as secondary (not Zora handle text that disagrees with ERC-20 metadata), and surface Zora coins the execution wallet already holds in a dedicated holdings section. Prefer token/protocol logos (Coinbase, Base, Uniswap) over text labels for wallet/chain affordances — Base chain selector should use the square Base logo shape without redundant "Base" text; format sub-cent prices with Uniswap-style significant-digit rules rather than long decimal tails. Surface a "Powered by Uniswap" mark, keep wallet/address rows to one line, and use a draggable percent slider (DeFiLlama-style) rather than 25/50/75/100 buttons. After a successful swap, show a persistent completion affordance (bottom-left `SwapCompletionNotice` with Basescan link) — do not leave users without on-chain confirmation when the tx lands. Do not surface internal bundler slippage-retry ladder copy — user-selected slippage should stay authoritative. Skip unverified-token warnings for Zora-indexed creator/content coins. Do not show Permit2 wallet-confirm copy when the Privy embedded EOA auto-signs. Swap Details should match Uniswap fidelity: honest output USD (never mirror input), multi-hop route preview with pool fee tiers, and accurate gas USD / price impact.
- For Telegram bot multi-step or progress responses, prefer a single editable message (e.g. one deploy progress list with per-contract green-check updates and basescan-hyperlinked addresses) over spamming new messages; menu selections should replace the prior message and provide a Back affordance. Keep command naming consistent across features (`/help`, not `/fullhelp`) and avoid gating Telegram-native flows on the Mini App when not required.
- For Hermit/AlfaChat creative commands, prefer clean replacement over legacy aliases — `/meme` replaces `/hermitimg`, and old Hermit command surfaces should be removed rather than kept as compatibility shims unless product explicitly asks for a staged transition. AlfaClub Hermit replies with GIFs/images should post the media in-thread (WebSocket-first; bot-token paths must not drop attachments) and use threaded `reply_id`; when cross-posting to X, follow with a Twitter hyperlink so previews render — avoid generic fallback GIF spam on failed routes.
- For agent-facing / share / OG endpoints, prefer structured JSON with short, deterministic, high-signal strings and an explicit missing-context fallback envelope (`ok: false`, `error`, `missing: [...]`) instead of guessing fields.
- Use "waitlist" for app-access queue copy and "whitelist" for vault-deployment eligibility copy; surface the one-deploy-per-wallet-per-deployment-version cap explicitly in deploy UI.
- Agent-install / delegate-signing surfaces (Zora content coin, landing page, in-app prompts) must avoid alarming verbs like "install", "connect", or "drain"; prefer softer framing such as "delegate signing authority to the 4626 agent", "add co-signer", "authorize", or a neutral queue/process term instead of "drain" — keep ticker/title choices in that register too (e.g. `$agent` / "agent 4626" rather than "AGENT INSTALL").
- In the top-right account chrome, the primary identity shown should be the connected external EOA (Rabby / MetaMask / Base App wallet) rendered with ENS or Basename and the matching profile picture, with the canonical CSW surfaced separately (via its own basename where one exists). Do not display the Privy embedded EOA as the primary identity — it is a delegated signer, not the user-facing account. Keep XMTP chat entry points on the left rail, avoid duplicate top-right XMTP buttons, and keep desktop chat windows left-side.
- After shipping requested changes, the user frequently expects immediate `commit` + `push` execution (and often asks for merge/PR follow-through) rather than stopping at a local diff summary.
- After rotating Hermit creative-brain env (`HERMIT_AGENT_CHAT_ENDPOINT` / `HERMIT_AGENT_BEARER_TOKEN` and provider/model keys), expect a Vercel production redeploy (and local `frontend/.env` sync) before treating creative Hermit as live in prod.
- When inspecting production envs or service variables (Railway, Vercel, Supabase, AlfaClub, etc.), avoid unredacted full dumps such as `railway variables --json`; query scoped keys or redact output, and rotate any secret material that was printed.
- For Pinata OpenClaw Hermit stuck on `starting`, try the Pinata UI recovery path first (`openai/gpt-4.1-mini` → Save → Restart Gateway → retry the target model); delete/recreate only if that fails, because a new agent id forces re-wiring `HERMIT_PINATA_*` on Vercel and in local env.
- For continual-learning runs, the user expects the full `agents-memory-updater` flow with strict incremental transcript processing and index refresh (new/updated mtimes only) plus a concise run report.
- When stopping dev servers/processes in Cursor, say **"kill background jobs"** (or name specific processes) rather than **"kill all terminals"** — bulk-killing terminal PIDs can detach Cursor's PTY host and break the integrated terminal until dead tabs are closed and the window is reloaded.
- When asked to restart local dev servers or complete local ops (kill old Vite/Anvil, run `pnpm -C frontend run dev:deploy-dry-run`, verify WASM/ports), execute those steps directly rather than only pasting commands.
- For KPR deprecation work, prefer hard-cut sunset execution (remove references/docs/workflows) over compatibility shims unless explicitly requested.
- For ad-hoc 3D model generation tasks (for example hologram GLBs), prefer the user's Hugging Face API/Space path over alternate providers unless they explicitly request a different provider.
- For Hugging Face Inference Router as a local Cursor/Aider coding model, configure `OPENAI_API_BASE=https://router.huggingface.co/v1` and the HF token in shell profile or Cursor Settings only — do not add setup blocks to root `.env.example`, and never replace `frontend/.env` `OPENAI_API_KEY` (OpenAI image/Eliza lanes).
- For CSW owner-setup and deploy flows, prefer click-first Base App deep-link/prolink or parent-CSW Relay owner-install (`/waitlist?setup=owner-install`, `AddOwnerSigningPanel`) — not app sub-account owner targets. Avoid external-EOA fallback paths, and if canonical signing is unavailable, fail with explicit guidance rather than forcing long manual EOA signature sequences.
- On Deploy UI surfaces, prefer a canonical always-visible stage timeline with plain-language phase labels/status and explicit disabled states, rather than hiding or ambiguously marking stages as "optional." The Deploy Vault page uses the premium dark redesign (`DeployHero` with pills — top-right Base `NetworkBadge` removed, restyled deploy card, structured `role="alert"` "Deployment failed" card instead of raw red error dumps, presentation-only toasts watching existing state transitions) — styling passes must stay presentation-only and never touch deploy logic. Address rows render the **full untruncated address** (mono, wrapping) with copy/BaseScan icons — no click-to-expand. Separate **"Your contracts"** from protocol shared infrastructure structurally as **side-by-side two-column layouts** (protocol factories/modules/helpers in one column, the user's contracts in the other, with row baselines aligned across columns — pad missing per-column descriptions so rows line up); the emergency-safety wiring section uses the same two-column split. Attach **protocol/brand logos wherever possible** on contract rows (Base, Solana, Chainlink, Safe for treasury rows, Uniswap near the CCA auction phase) via `frontend/public/protocols/` + `PROTOCOL_LOGOS`/`manifest.json`. During dry-run/deploy, contract rows should update dynamically (green check per contract as it lands) and completed phases show a green **complete** pill. Keep the page borderless — separation via background tint, spacing, and typography; borders only on functional affordances (inputs, timeline nodes).
- For bonding-curve visualizations, prefer green candlestick bodies for the bonding-curve spread with Sudoswap overlays, and keep direct chart interactions (mouse-wheel zoom around cursor plus click-drag horizontal panning).
- When requesting infrastructure migrations/cutovers, the user may explicitly prefer a hard cutover over staged coexistence and accepts downtime if it simplifies execution.
- Ethos score UI should follow the official Ethos/Twitter-style treatment: use official label text and color bands, keep the pill slightly overlapping the avatar/logo, wrap the entire logo/avatar in the score-colored border when score data is meaningful, treat score `0` as neutral/unknown with a dash rather than negative styling, use standard green for online indicators, avoid overlapping availability dots, and use static, interactive hover cards that persist while hovered so profile actions are clickable.
- In user-facing communication for this workspace, avoid using the words "scrape" or "crawl" unless the user explicitly asks for those terms.
- When debugging wallet, Base App, Relay, or Coinbase Smart Wallet behavior, be explicit about what is known, observed, and still uncertain; do not present inferred wallet/API behavior as fact without runtime evidence.
- Run nginx, certbot, and `systemctl` for the Solana keeper orchestrator on the remote Solana ops host over SSH with sudo, not from the local dev checkout.
- Prefer `hello@4626.fun` as the Let's Encrypt contact email when issuing new `*.4626.fun` nginx certificates on the Solana ops host.
- **Do not enable or pursue Vercel PR preview deployments** — product ships via **`main` production only**; canceled Preview rows from `vercel-ignore.sh` are intentional skips, not build failures to fix.
- On the `4626.fun` marketing home (`frontend/public/immersive/index.html`), center the primary **Join the waitlist** CTA and keep hero/CTA copy concise — avoid redundant captions when the lockup already states product context. Reuse the dark, modern `/faq/how-it-works` visual language for downstream landing sections; merge or drop redundant Anatomy vs How-it-works (prefer scroll-driven How-it-works that reveals step 1 first and adds steps 2–4 on scroll), remove "The short version", and link to the FAQ page for deeper detail. Avoid hard dividers between landing panels — use gradients or soft transitions for seamless section flow.
- Marketing vault showcase/example carousels must not fabricate APY, TVL, or holder metrics; use real Zora creator names/tickers (e.g. `$jesse`, `$gabrielhaines`) and label slides as demo/example/illustrative only.
- Immersive multi-panel scroll sections should use consistent vertical spacing between panels — match the hero-to-"One vault, two tokens" gap rather than large uneven gaps.
- Marketing footer/legal pages should avoid vertical line-streak backgrounds; use subtle gradients or patterns instead, and keep a consistent top-right nav set (Home, Docs, Security, About, Terms, Privacy, FAQ) across all marketing shells.
- Waitlist modal/panel chrome should feel seamless — avoid obvious heavy top/bottom borders or boxed framing on the waitlist component.
- Approved waitlist workspace layout: on desktop, pin the leaderboard as a left rail and group chat as a right rail; consolidate referral/points/identity into coherent sections and keep the layout mobile-friendly.
- **Explore creator detail pages:** keep page 1 hero identity-only; put stats in a scroll-scrubbed GSAP beat in the first `CreatorScrollBridge` (`CreatorImmersiveStatsBeat` + shared `creatorStatsModel.ts`) with **one metric revealed at a time** during the pinned scroll section (not a six-metric dashboard grid), then a compact static stats rail from the 3D scene onward; use a reduced-motion vertical stack fallback when `prefers-reduced-motion`.
- On `/explore/creators`, merge Ethos into the **Token** column — show the score on the avatar badge (`CreatorEthosAvatar`), make the Token header sortable for `?sort=ethosScore` (high → low), and drop the standalone Ethos column/subnav entry to save horizontal space.
- On Explore creator/content tables, fold fee-version copy (1% V4 vs 3% legacy) into the **Fees (24h)** column via a compact ℹ tooltip on the **column header only** (not per row) instead of a standalone Fee % column.
- On Explore list tables (Creators, Content, Trends), omit visible rank/index columns (# 1, 2, 3…) — let sort order convey ranking without ordinal labels.
- For Ethereum vault hero 3D, iterate offline in `tools/vault-images/` (Blender) with art direction **obsidian crystal ETH** (monolithic dark crystal) — **not** gold kintsugi/marble, and **no purple**: accents are Base-blue. Texture target is **obsdn.trade**-style: fine-grained, matte, very detailed — not glossy and not "cheap leather"; keep the bipyramid ETH silhouette large and upright but visually distinct from the bare Ethereum logo. Accent **veins flash only on lightning beats** (not always-on), and **remove the top halo / force-shield ring** (`ForceShieldMesh`). Map the crystal's six points to integrations via **brand colors, not logos** (top = Solana, center square = Base/blue, bottom = Ethereum; four mid-square points = Ajna, Uniswap, Charm, Zora), optionally with faint internal integration logos; label the object both **ERC-20 and ERC-4626** (token standard badge top-left). Production `4626.fun/` hero: Blender `ethereum_vault.glb` + marketing R3F island (`frontend/src/marketing/`, build `pnpm -C frontend build:marketing-vault` → `public/immersive/vault-hero/vault-hero.js`); fallback `public/immersive/vault.js` or `?hero=legacy`. Do not reintroduce Explore/creator-page R3F vault experiments. Runbook: `docs/operations/obsidian-vault-pipeline.md`.
- For the `/positions` social-signal chart (Hyperliquid-style room trade overlay), keep the timeline-events list and event-inspection panel on the **side** of the chart (not stacked above/below), make the chart fill the page width, and anchor hover tooltips to the cursor (not a fixed/random offset). Keep filter controls minimal and non-overlapping (and the **Host only** filter must actually show host messages); render room messages on the chart with the **sender's profile picture** as the marker (not a generic `W`/`RM` label); label position opens as **Open** with the entry size, use ☠️ for liquidated and 💬 for messages; final marker colors are **green for entry/add, red for close**; on hover show current price, the position at that timestamp, and P/L, differentiate **closed vs liquidated**, and keep host vs agent positions as separate entries (never collapse them into one market row).
- Identity presentation should use one shared resolution order everywhere (chat rail, chat windows, waitlist leaderboard, and account chips): **Zora profile -> Basename -> ENS -> 0x fallback** with consistent avatar sourcing and no slow-loading regressions that replace known labels with raw addresses.
- When explaining multi-bot/agent topology (Hermit, Keepr, InverseAKITA, Virtuals, Eliza), prefer a concise ownership table mapping runtime host, transport (AlfaClub/Telegram/XMTP), model lane, and purpose before proposing architectural changes.
- **`/arena/backtest` UI:** keep controls minimal — drop saved-run lists and sweep filter rows (health floor, chunk, cooldown); use direct USD inputs for long/short capital, one horizon picker (not select + duplicate pills), sliders for leverage/tuning where helpful, plain-language **bar size** copy (not "Resolution"), and premium full-horizon replay charts; 90-day runs should target finest available bars with honest coverage labels when minute cache is incomplete.

## Learned Workspace Facts

- Canonical project framing: 4626 is not only vault + swap infrastructure; it includes a core lottery/rewards system and a **ve(3,3)-inspired** incentive model that favors **share-token burn/value-capture mechanics** over inflationary token emissions.
- Isolated feature work often uses `git worktree add` under `/.worktrees/` at the repo root; that directory is gitignored via `.gitignore`.
- Continual-learning transcript indexing in this workspace is parent-transcript scoped: process only top-level `agent-transcripts/<uuid>/<uuid>.jsonl` files, exclude nested `subagents/*.jsonl`, and refresh mtimes incrementally from `.cursor/hooks/state/continual-learning-index.json`.
- `frontend/src/config/wagmi.ts` sets `multiInjectedProviderDiscovery: false` on `createConfig` to avoid eager EIP-6963 multi-provider discovery that can trigger extension `requestProvider` races against non-writable `window.ethereum` getters when several wallets are installed. Keep targeted Rabby/MetaMask discovery explicit via manual EIP-6963 provider lookup, and keep `WagmiProvider` reconnection enabled so app/deploy route-boundary remounts restore an already-approved external EOA connection instead of appearing to disconnect it. A startup `detectEthereumProviderCollision()` check additionally skips EIP-6963 `requestProvider` dispatch entirely when `window.ethereum` is locked/getter-only or multiple injected providers collide; `DeployVault` gates its heavy mount behind `useDeferUntilAfterCommit()` to avoid setState-during-`Hydrate` warnings.
- Perplexity-ready skill packages live under `docs/perplexity/perplexity-skills/` as one-folder-per-skill bundles with a `SKILL.md` entrypoint, and are commonly shared as `.zip` archives.
- Feature flags are centralized in `frontend/src/lib/featureFlags.ts` using a typed `FeatureFlag<T>` registry (11 flags across 4 categories), with `FlagToolbarBridge` in Layout for Vercel Toolbar integration and `/api/flags/evaluate` + `/api/flags/discover` endpoints for Vercel Flags dashboard resolution.
- `/api/uniswap/quote` and `/api/uniswap/swap` proxy endpoints require an authenticated 4626 session principal; `useSwapExecution` gates quote requests behind session hydration to avoid 401 errors. `/swap` quote display is read-only and should require session + execution address, not full `executionReady`; submit/build remains gated on `executionReady`.
- **Paymaster swap simulation recognizes Zora Universal Router as `swapRouterKind: 'zora-universal'`.** Keep paymaster router-kind detection aligned with client swap build paths when adding new Zora or Universal Router execution lanes.
- `GET /api/waitlist/stats` serves live waitlist count data for urgency display; waitlist capacity uses configurable 100-slot tiers with dynamic "Only X spots remaining!" messaging.
- Canonical CSW owner-install sponsorship accepts `addOwnerAddress` self-calls for the authenticated Privy embedded EOA without requiring deploy-session signer state; keep deploy-session checks scoped to deploy-session automation paths.
- `frontend/src/lib/aa/` and `frontend/src/lib/uniswap/` are cross-cutting client utilities with multiple feature consumers; keep them under `src/lib/` rather than relocating into individual `src/features/*` folders.
- `pnpm -C frontend guard:frontend-boundaries` enforces that `frontend/src/components/ui` has no imports from `src/features/`, complementing the existing `guard:server-core-boundary` check (`scripts/check-frontend-boundaries.mjs` / `scripts/check-server-core-boundary.mjs`).
- Mega-file decomposition pattern: extract pure, side-effect-free logic into sibling `*Helpers.ts` / `*Utils.ts` modules next to the parent file (e.g. `deployVaultHelpers.ts`, `telegramLinkHelpers.ts`, `adminOpsHelpers.ts`, `coinbaseErc4337Telemetry.ts`, `xmtpHelpers.ts`, `telegramTradingHelpers.ts`, `elizaSwarmRoles.ts`) rather than moving across feature boundaries; keep runtime-bound state (React hooks, DB clients, XMTP/Eliza lifecycle) in the parent.
- `/swap` wallet gating uses a pure state machine `deriveSwapConnectGate` in `frontend/src/lib/swap/connectGate.ts` plus a `SwapConnectGate` component; the connect CTA uses `<Button variant="primary">` from `@/components/ui/Button`, and the wallet-required path uses wagmi `connectAsync` with the shared preferred-connector helper instead of Privy `connectWallet`/`login`. When provider-collision mode hides injected wallets, filter Rabby/MetaMask/injected connectors before choosing the preferred connector so Coinbase/Base App remains the safe path. `<SwapCard>` only mounts when the gate resolves to `ready`. Balance labels in canonical mode read from the canonical CSW (`profiles.csw_address`), which is also the default `canonical4337` execution sender.
- Sponsored canonical/paymaster swaps cannot sell native ETH directly because the paymaster path cannot sponsor `tx.value`; `/swap` blocks that embedded canonical path and offers `Switch Sell to WETH` so the sponsored ERC-20 approval/swap path can continue. Native ETH sells still require a funded external signer or explicit wrapping first.
- **Embedded canonical swap fallback is dev-only.** `frontend/src/lib/tx/txRouter.ts` keeps production strict (`canonical4337` required) but now permits a local/manual fallback to `canonicalDirect` only when `VITE_ALLOW_EMBEDDED_CANONICAL_DIRECT_FALLBACK=1` or host is `localhost`/`127.0.0.1`/`::1`; this unblocks local paymaster outages without weakening production sponsorship policy.
- **`TRANSFER_FROM_FAILED` during sponsored canonical swap gas simulation usually means the CSW has 0 WETH while the simulator runs only the Uniswap swap-proxy leg (`0x2894adf9`) without batched `WETH.deposit()`.** Skip `simulateTransaction` when a sponsored native-ETH sell batches wrap+approve+swap (`shouldSimulateSwapTransaction` in `useSwapExecution.ts`); map the error to insufficient-funds copy in `error.ts`; run `assertSwapSpendBalancePreflight` on the execution wallet before `eth_sendUserOperation`.
- **canonical4337 AA25 (invalid account nonce) is an in-flight UserOp collision, not a signer bug.** CSW UserOps use `ownerIndex` as the EntryPoint nonce sequence key — sparse high slots (e.g. owner 18) are normal. On bundler AA25, `coinbaseErc4337.ts` must wait for the sequence to advance (`waitForEntryPointNonceAdvance`) instead of re-submitting the same nonce; owner-index scans in `coinbaseErc4337Owners.ts` wrap reads with `readContractWithRpcRetry` for RPC 429 resilience.
- **Canonical CSW unification (see § Canonical CSW identity):** `profiles.csw_address` and `CANONICAL_CSW_ADDRESS` are the **same wallet** for the 4626 canonical account — legacy "XMTP agent CSW" env is retired. Runtime: `CANONICAL_CSW_*` only (`canonicalCswEnv.ts`); client inbox `resolveClientAgentXmtpAddress()` / chat `CANONICAL_CSW_INBOX_ADDRESS`; server `resolveServerAgentInboxAddress()`. Execution gate: `CANONICAL_CSW_EXECUTION_OWNER_ADDRESSES` / `isAllowedCanonicalCswExecutionSigner` when `shouldApplyCanonicalEnforcement` (active wallet === policy constant). After `addOwnerAddress` on the canonical CSW, update both allowlists per `.cursor/rules/csw-agent-lifecycle.mdc` § Canonical CSW owner allowlist.
- **`/swap` from the 4626 canonical CSW:** requires sell-token balance on that CSW; gate automation to active allowlisted owner signers only. Zora `canonical4337` sells use a single router `execute` call; revert `0x2c4029e9` is CSW `ExecutionFailed` wrapping inner Zora Universal Router failure (Permit2, slippage, balance) — not AA25. Sparse owner slots are normal (`ownerCount` ≪ `nextOwnerIndex`, e.g. signer at slot 18).
- **canonical4337 must fail fast when preflight simulation says the inner call would revert.** When `coinbaseErc4337.ts` preflight reports `ExecutionFailed` or another deterministic router revert, abort before `estimateUserOperationGas` and transient retries — do not loop gas estimation on a UserOp that preflight already proved would revert.
- **`canonical4337` swap-router gas floors and bundler estimate policy:** apply `callGasLimit` floors to Zora execute, Uniswap UR `0x3593564c`, swap-proxy execute, and multi-call wrap/approve/swap batches — not Zora-only. Bundler `estimateUserOperationGas` advisory skip applies to stub-signature `InvalidContractSignature` (`0xb0669cbc`) on echoed **`execute`** and batched **`executeBatch`** (`0x34fcd5be`) calldata when direct preflight passed; **`ExecutionFailed` (`0x2c4029e9`) at estimate is a real router revert** (slippage, Permit2, thin pools) even when production `eth_call` preflight passed — do not advisory-skip and submit. **`paymaster proxy internal error` is an `/api/paymaster` handler crash** (`paymaster_proxy_internal_error`), not sponsorship denial — do not classify it as policy rejection or retry unsponsored canonical swaps.
- **canonical4337 `executeBatch` execution reverts get per-leg replay, ladder abort, and a prefund-gated unsponsored fallback.** `replayExecuteBatchPerLeg` in `coinbaseErc4337.ts` replays wrap/approve/swap prefixes via production `eth_call` to pinpoint the failing leg and map it to user copy (Permit2 `InvalidNonce`, slippage, insufficient funds); deterministic execution reverts abort the verificationGasLimit retry ladder (`shouldAbortUserOpGasLadder`) instead of resubmitting ~6 identical UserOps; the unsponsored fallback is skipped for swap-heavy execution reverts and otherwise gated by `assessUnsponsoredPrefundFeasibility` (`walletBalance >= callValue + maxGasCost`). `simulateSmartWalletCalls` must not report first-leg-only success for multi-call batches.
- **Zora V4 hooked creator-coin pools quote at 10% slippage by default** (Zora's own `getQuoteStrict` sends `slippage: 0.1`). The canonical auto-slippage floor in `swapAutoSlippage.ts` and the CSW escalation ladder in `zoraTradeApi.ts` start at 10% (10 → 15 → 20) — do not lower the floor back to 5%, which burns retries in the "slippage too tight" loop. Also, the CDP paymaster denies bare approve-only UserOps (`request denied - approve_only_not_allowed`) — sponsored approvals must be batched with the swap leg.
- `/api/onboarding/bootstrap` and `/api/accounts/me` both expose `executionTrack`, `baseSubAccount`, and `privyEmbeddedEoaIsOwnerOfCanonicalCsw` as the source of truth for execution-track decisions; clients must read these fields rather than recompute canonical-vs-sub-account routing from wallet state. Server profile resolution for both endpoints must use `listProfileIdsForPrivyUser` (alias-first), not a direct `profiles.privy_user_id = ?` lookup — merged/alias Privy users otherwise miss canonical CSW rows and swap gates falsely show execution-setup-required. Account-setup surfaces (`useAccountSetupController`, `/add`) must merge both via `mergeAccountMeWithBootstrap` — reading `/api/accounts/me` alone can miss canonical CSW already on the profile.
- **Waitlist bootstrap auto-adopts wallet-bound Privy collisions** when the active session's wallets match the canonical profile (`runWithWaitlistWalletCollisionAdoption` in `emailCollisionAdoption.ts`): if `WALLET_BOUND_TO_CANONICAL_EMAIL_PROFILE` fires but connected CSW/embedded EOA owns the target row, seed `privy_user_aliases` and retry instead of looping "Use existing account" — distinct from email-collision sign-out recovery.
- `/accounts` redirects to `/waitlist`; the advanced account-setup controls live in `WaitlistAdvancedSection` mounted inside the waitlist page (no separate `/accounts` UI).
- `referral_passthrough` is a first-class `waitlist_points` source written by `recordReferralPassthrough` in `frontend/server/_lib/onboarding/waitlistPoints.ts`; it credits referrers with 50% of every point the referee earns, and every scoring query (`waitlistLeaderboard.ts`, `waitlist/_position.ts`, `waitlist/_referrer.ts`, `accountsIdentity.ts`) must include this source. Invariants, all enforced in code:
  - **One hop only.** The exempt set is derived from the exhaustive `REFERRAL_FAMILY_EXEMPT: Record<Extract<WaitlistPointSource, 'referral_${string}'>, true>` map — adding a new `referral_*` source to the union without listing it there is a TypeScript compile error, not a runtime regression.
  - **50% floor.** `Math.floor(amount * REFERRAL_PASSTHROUGH_FRACTION)` — keep caller amounts even integers (the AGENTS.md user preference) so no point is lost to the floor.
  - **Bounded amount.** Both `awardWaitlistPoints` and `recordReferralPassthrough` reject anything above `MAX_AWARD_AMOUNT = 10_000`; callers passing larger values are treated as bugs.
  - **Strict integer IDs.** `signupId` / `referrerId` / `refereeSignupId` must pass `Number.isInteger(...) > 0`; fractional or string-coerced values are rejected before touching the DB.
  - **Collision-safe `source_id`.** Use `buildPassthroughSourceKey(refereeSignupId, source, sourceId)`; it returns the natural composite when it fits in 256 chars and falls back to `<prefix>#<sha256>` otherwise. Never `.slice(0, 256)` a composite key — distinct awards could dedupe under `ON CONFLICT DO NOTHING`.
  - **Observable failures.** Passthrough errors do not block the referee's award, but they emit a structured `console.warn('waitlist_points.passthrough_failed', { refereeSignupId, source, message })` instead of being silently swallowed — a persistent failure should be alertable.
  - **Reciprocal referrals are allowed.** A↔B pairs each earn passthrough on the other's organic awards, never on passthrough rows (compile-time exempt), so there is no compounding. Block at referral-code claim time if product ever wants to forbid this.
- AMOE ↔ waitlist points bridge: `awardAmoeCheckinPoints` in `frontend/server/_lib/lottery/amoeWaitlistPoints.ts` is called from `claimDailyTwitterCheckin` in `frontend/server/_lib/lottery/lotteryAmoe.ts`; AMOE entry submissions no longer award waitlist points.
- AMOE ZK submit/publish/retry API paths fail closed when `LOTTERY_AMOE_ROUTER` is empty or undefined; runtime readers return a 503-style disabled/misconfigured response rather than falling back to a default router address, so production deploys that enable AMOE must configure this env var explicitly.
- AMOE protocol-entry target resolution is explicit request, then `LOTTERY_AMOE_PROTOCOL_CREATOR_COIN`, then legacy `LOTTERY_AMOE_DEFAULT_CREATOR_COIN`, then the current AKITA creator coin fallback until the new 4626 stack is deployed; submit requests must reuse the nonce-selected `creatorCoin` so env changes between nonce and submit cannot trigger `creator_mismatch` for a valid signed message.
- **AMOE off-chain credit constants are not interchangeable.** `AMOE_CREDITS_PER_ENTRY` (100 in `lotteryAmoe.ts`) is credits burned per lottery entry submission; `AMOE_DAILY_TWITTER_CREDIT` / `AMOE_DAILY_XMTP_CREDIT` are daily earn amounts (currently **1 credit/day** each after reverting a temporary 100/day promo); frontend `AMOE_MIN_POINTS` (100 in `AmoeEntryCard.tsx`) is the minimum variable burn for ZK submit — distinct from daily check-in credits. Daily check-in waitlist bridge stays **6 points** via `amoe_checkin`, not tied to AMOE credit amounts.
- **AMOE ZK prover assets for Vercel.** Bundle `amoe_eligibility.wasm` + `amoe_plonk_final.zkey` under `frontend/server/_lib/lottery/amoe-zk-assets/`; `defaultAmoeZkAssetPaths()` in `amoeSubmitZk.ts` resolves module-relative bundled paths (override with `AMOE_ZK_WASM_PATH` / `AMOE_ZK_ZKEY_PATH`); `frontend/vercel.json` `includeFiles` ships `server/_lib/lottery/amoe-zk-assets/**` on the catch-all API function.
- **`AMOE_SIGNUP_SALT` must be set on production Vercel** for ZK signup linkage — see `docs/operations/deployment/amoe-signup-salt-provisioning.md`.
- **Twitter credentials split (`frontend/server/twitter/twitterEnv.ts`).** Protocol lane uses `X_*` env vars with fallback to `TWITTER_*` / `HERMIT_TWITTER_*`; `X_BEARER_TOKEN` for server tweet verification (AMOE daily X check-in); OAuth 1.0a keys for protocol posting (`X_*`) or bot/Hermit posting (`HERMIT_TWITTER_*` when `HERMIT_TWITTER_STRICT`); Privy dashboard OAuth 2.0 is for user account **linking** and is separate from AMOE verify + bot lanes.
- **AMOE `points` ledger identity (`amoeProfileResolve.ts`).** Twitter daily awards use `verified_privy_only`; waitlist `amoe_checkin` bridge uses `privy_linked`; anonymous lottery ledger may create synthetic `amoe-*@wallet.4626.fun` under `lottery_ledger` policy.
- **Public points are one weighted total everywhere.** `readWaitlistPointsBreakdown` in `waitlistScoring.ts` is the SSoT for tray/waitlist tiers (`score.points`), `/api/waitlist/position` `points.total`, `/api/v1/lottery/amoe/credits`, and lottery spend gates — do not reintroduce a second `lotteryCredits` / `amoeCredits` / `points_amoe_eligible_balance` balance for user-facing totals. AMOE ledger sources (`amoe_entry_spend`, `amoe_twitter_daily`, `amoe_xmtp_daily`, `amoe_entry_refund`) contribute **0** to that weighted sum; only `amoe_checkin` counts among AMOE-family sources on public surfaces.
- **Account tray points are Privy-only and fail closed.** `GET /api/accounts/me/points` must resolve via `buildAccountTrayPointsForPrivyUser` in `frontend/server/_lib/onboarding/accountTrayPoints.ts` (alias-aware `resolvePrimaryProfileIdForPrivyUser` → `profiles.id`); do not reintroduce wallet-based tray rank/history. Requires `X-Privy-Token`; client uses `fetchAccountTrayPoints` (throws `AccountTrayPointsAuthError` without token). Rank/`totalCount` are withheld unless `leaderboardEligible` (verified non-synthetic email, non-tombstone). `/api/waitlist/points-activity` delegates to the same builder for history-only callers. Integrity guards: `account_tray_points_profile_mismatch`, `account_tray_points_breakdown_mismatch`, `account_tray_points_tier_mismatch`. Tray UI uses one unified points total everywhere (not separate waitlist/leaderboard/lottery balances in copy), tab order **Overview → History → Leaderboard**, edge-to-edge activity/history panels without nested bordered cards, and Zora username on leaderboard rows when available (else Basename/CSW).
- Uniswap pool-history fetch falls back to Zora `getCoinSwaps` via `/api/zora/coinHistory` (handler `frontend/api/_handlers/zora/_coinHistory.ts`) to derive OHLCV candles when the Uniswap subgraph has no data points; the fallback is built into `fetchPoolHistory` in `frontend/server/_lib/uniswap/hooks.ts`, and the endpoint must stay registered in both `_routes.zora.ts` and the Vite dev proxy in `frontend/vite.config.ts`.
- **Product UI design system (`frontend/docs/ui-design-system.md`):** shadcn/Radix primitives at `@/components/ui/*` mapped to 4626 CSS vars (`btn-accent`, `--vault-*`); product surfaces use `<Button>` / siblings instead of raw `btn-*` classes; explore charts use Recharts via `MetricChartPlot`; runtime `@coinbase/cds-*` is removed from app code — do not reintroduce CDS providers or packages.
- Run targeted Vitest files with `npx vitest run <file>` — `pnpm -C frontend test -- --run <file>` still executes the full suite in this workspace.
- The waitlist wallet-readiness gate follows parent-CSW + Privy embedded-owner only. Gate on `useEnsurePrivyEmbeddedWallet().embeddedEoaAddress` (or `ensureEmbeddedWallet()` during provisioning), not `privy.user.wallet.address`. Signing is ready when `executionTrack` is `legacy-owner-install` (embedded EOA is an on-chain owner of the parent CSW). `/swap` setup CTAs deep-link to `/waitlist?setup=owner-install`, not `connect-base-app`. Do not revive `ownerInstallMapping.ts` wholesale.
- **Sub-account execution track is flag-gated for swaps only.** When both `WAITLIST_SUBACCOUNT_FLOW_ENABLED=1` (server) and `VITE_WAITLIST_SUBACCOUNT_FLOW_ENABLED=1` (client) are set, `resolveExecutionTrack` (`frontend/server/_lib/wallet/executionTrack.ts`) may return `sub-account` for a distinct registered `profiles.base_sub_account`. Waitlist Step 2 shows `WaitlistConnectBaseApp` for Base App passkey CSWs; `/swap` may route through sub-account `wallet_sendCalls`. Deploy remains parent-CSW + `legacy-owner-install` only — do not promote sub-account as deploy sender. Zora users with EOA owners still use `AddOwnerSigningPanel` / `canonical4337`. When parent-CSW embedded-owner install is already complete, waitlist must skip sub-account UI (`parentCswSigningReady` on `WaitlistConnectBaseApp`) — sub-account is swap-only fallback, not a substitute for parent owner-install.
- **`resolveWaitlistStep` must not regress verified users to email auth.** When email is verified but signing is incomplete, route to the `done` setup workspace (not `auth`). **`shouldAutoBootstrapWaitlistSession` stays false** — bootstrap runs only after explicit Continue, not on Privy auto-restore. When Privy marks authenticated but `login()` never resolves after email OTP, resume bootstrap from an auth-pending flag instead of leaving the full-screen "Loading…" overlay stuck.
- **HttpOnly `cv_auth_session` ≠ Privy session.** Users can have a 4626 session cookie while `privyAuthenticated` is false; canonical `/swap` then shows **Privy sign-in required**. Restore Privy with email OTP (`signIn({ method: 'privy' })` / waitlist email), not Base-only wallet connect. In `useSiweAuth`, `preferBaseAccountWallet: true` must still call `setActiveWallet` after `connectWallet` and fall through to `login()` when no Privy access token is present — connecting Base alone does not load the embedded signer.
- **Waitlist/chat/Zora session repair shares `frontend/src/lib/auth/sessionRepair.ts`.** Use `attemptSessionRepair` + `createStaleSessionProbe` (double-probe; true-stale only after `>= 2` misses with no live `cv_auth_session`) — a single null `getAccessToken()` after OTP is transient, not hard sign-out. XMTP mounts repair via `useXmtpSessionRepair`/`ChatProviderWithSessionRepair`: one bounded repair + single `connect('user')` retry before embedded-signer expired copy. `ChatBar` Connect uses `connectBusy` with ~15s safety timer. Zora cross-app timeout/auth/collision failures degrade to read-only `resolveZoraReadOnlySignals` (`/api/zora/resolve`); `isWalletProviderCollisionError` must not count as stale session.
- **Hugging Face Inference Router is local-only assistant config.** Repo code does not read `OPENAI_API_BASE`; Cursor/Aider use `https://router.huggingface.co/v1` via user shell env or Cursor Settings. Primary coding-model candidate: `Qwen/Qwen2.5-Coder-32B-Instruct` (fallbacks: `deepseek-ai/DeepSeek-Coder-V2-Lite-Instruct`, `bigcode/starcoder2-15b`). Unrelated repo HF usage stays on `HF_TOKEN` (e.g. `generate_akita_3d_hf.py`).
- **`useEnsurePrivyEmbeddedWallet` lives in `@/lib/privy/embeddedWallet`**, not under `@/hooks/`.
- **Privy uses the custom auth domain `privy.4626.fun` with per-environment dashboard clients.** That domain must be present in the app CSP `frame-ancestors`/connect rules and in the Privy app's allowed domains/origins alongside `4626.fun`, `app.4626.fun`, localhost dev ports, and `web.telegram.org`. **Production CSP `connect-src` in `frontend/vercel.json` must include `https://privy.4626.fun` and `https://privy.app.4626.fun`** (not just `frame-src`/`child-src`) — the SDK's session-refresh fetches hit `https://privy.4626.fun/api/v1/sessions`, and blocking them leaves Privy init permanently stuck (loading overlay `held by: privy-init`); regression test in `alfaclubVercelWiring.test.ts` pins both domains in `connect-src`. Privy clients are environment-scoped (Local Dev / Production Web / Production Mobile) — a stale or mismatched `VITE_PRIVY_CLIENT_ID` in a running dev server surfaces as `Invalid app client ID` / "Something went wrong"; restart the dev server after `.env` Privy changes before debugging deeper. Cookies are enabled on the `4626.fun` base domain so sessions survive the marketing ↔ app host split.
- **Privy on localhost runs through `loopbackSessionMarkerShim` with known limits.** The custom auth domain puts the SDK in server-cookie mode; on loopback those cookies are third-party, so the shim re-asserts the `privy-session` marker every 2s (loopback origins only). Localhost sessions do **not** survive a hard page reload, the embedded-signer iframe session lives ~1 hour, and Zora cross-app OAuth is structurally flaky on localhost — these are documented limitations, not bugs. **"Missing auth token" from inside the Privy embedded-wallet iframe** (raw digest signing, XMTP signers) means a stale/expired iframe session: page-side token refresh cannot fix it — only a real Privy logout + fresh email-OTP login re-seeds the iframe. `/swap` surfaces a "Sign in again to fix signing" recovery CTA for this, and refresh checks must decode the JWT `exp` (a truthy-but-expired token is a failed refresh, not success).
- **Smart-wallet Permit2 (canonical CSW sells) needs raw digest signing from the embedded EOA.** `signRawEcdsaDigest` tries `secp256k1_sign` plus `eth_sign` param shapes and reports per-method failures; disconnected-session errors (`secp256k1_sign: Disconnected` / `eth_sign: Disconnected`) are treated as refreshable and route through the same session refresh/retry path as missing-auth-token failures instead of hard-failing. `useSwapEmbeddedEoa` must not treat `signMessage`-only wallets (no RPC provider) as sign-ready for canonical Permit2 — gate readiness so failures surface as the correct setup prompt instead of an opaque Permit2 error.
- **Waitlist Airtable sync is an ops mirror, not account truth.** `/api/waitlist/airtable-sync` is cron-secret protected and writes server-side Airtable rows from Supabase; Airtable may carry review/outreach fields, but it must not rewrite points, referral ownership, wallet readiness, CSW addresses, Privy IDs, or Telegram linkage. Keep merge keys stable: referrals include `id` from `referral_conversion_id`, onboarding upserts by `signup_id` with a legacy `canonical_wallet` / `profile:` backfill before the `signup_id` upsert. `Personalized Onboarding Email` in the applicants table is an Airtable-owned `aiText` field, not API-writable; use an Airtable AI prompt there, or add a separate writable long-text field if server-generated drafts are needed.
- The production Vercel project for this app is `akita-llc/4626` (https://vercel.com/akita-llc/4626/deployments); the docs site is a separate project. **Production deploys are `main`-only** — `frontend/vercel.json` (`deploymentEnabled`) and `frontend/scripts/vercel-ignore.sh` skip non-`main` refs and empty/no-`frontend/` commits; use `[force-vercel]` in a `main` commit message to force a rebuild. Verify the linked project before triggering deploys — cloud agents have repeatedly deployed to the wrong project.
- **Vercel cron and keeper routes must use `getDbForCron()`** (connect deadline + pool-generation rebind), not ad-hoc shared-pool `getDb()` — avoids `:00` cron pile-ups hanging 60s or poisoning the pool after reset. Runbook: `docs/operations/vercel-cron-production-fixes.md`; keep `POSTGRES_POOL_MAX=1` and Supabase transaction pooler (6543) on serverless.
- Vercel CLI deploys for this monorepo should use archive upload (`vercel deploy --archive=tgz`, plus `--prod` for production) because raw file upload can exceed Vercel's file-count/size limits before build starts.
- **Vercel production builds should keep `VERCEL_FORCE_NO_BUILD_CACHE` unset** so deploy cache can reuse artifacts; forcing no-cache was a major driver of ~10m builds. After function-fanout trimming + cache restore, steady-state production builds land around **7m** with stage timings roughly `build:app` ~60s, `build:telegram-link-standalone` ~20s, `build:marketing-vault` ~7s. Next safe cuts: conditional skip for telegram standalone when telegram paths are unchanged; fix conservative `commit range unavailable` fallback that prevents marketing/telegram skip.
- `/api/deploy/config` must remain available to any authenticated deploy user, not admin-only; the Deploy page uses this public runtime config to replace stale build-time contract addresses such as legacy `DeploymentBatcher` values after env/config cutovers. The response now includes `creatorVaultBatcherConfigError: string | null` — when non-null, `DeployVault` surfaces a dismissible runtime warning banner before dry-run; consumers of this endpoint should surface this field rather than failing silently.
- Vault-deploy product invariants: a creator must deposit between **50,000,000 and 100,000,000** of their Creator Coin as principal before deploying a vault (50M stays the floor per the 5%-of-supply invariant; the exact-50M pin was relaxed June 2026 by hot-swapping `DeploymentBatcherPhase2Module` to `0xdDbD468271BffF84De09AAc3C43B582e2dBEDC5E` via protocol-treasury-Safe `setPhase2Module` — the live batcher shell `0xa99058…0651` stayed in place). Frontend honors `?minFirstDepositTokens=…` / `VITE_MIN_FIRST_DEPOSIT_TOKENS`, clamped to the 50M–100M range; default remains 50M. Future module rotations: `frontend/scripts/ops/execute-set-phase2-module-safe.ts` plus the fork rehearsal pattern in `test/fork/DeploymentBatcherPhase2ModuleHotSwap.fork.t.sol` (validate live shell + new module on a mainnet fork before the Safe swap). Initial CCA seeding is 99% creatorCoin / 1% USDC (not a balanced pair); each wallet is capped to one deploy per `deployment version` (current copy: "v1.2.3x"), enforced on the Deploy page.
- Accounts page is expected to list all owners of the canonical Zora CSW (`profiles.csw_address`) with an optional revoke control; any canonical-CSW automation must reuse the existing deploy-session Privy embedded-EOA signer pattern rather than silently calling `addOwnerAddress` to add new owners — the "why is the deploy flow trying to add a new owner to my CSW?" complaint is a recurring regression signal.
- XMTP new-conversation search resolves a Basename suffix (e.g. typing `akita` matches `akita.base.eth`) and should render the Base App display name and profile picture for the canonical Coinbase Smart Wallet address, not a shortened hex address. Canonical CSW remains preferred for DMs, but if production XMTP says the canonical CSW has no inbox and the original Basename-resolved address does, fall back to that original reachable address and keep it as the chat peer.
- ETHOS score display in the chat rail is currently availability-cache scoped: scores render for `/api/v1/chat/availability` rows after `POST /api/v1/chat/presence/heartbeat` or chat search caches `chat_directory_profiles.ethos_score`. Recent XMTP conversations and curated agent rows need separate score hydration before showing `ScorePill`.
- Curated external XMTP/Base agents are not 4626 account peers. `.base.eth` agent entries must resolve directly to the Basename owner address and direct-address agents should open as-is; do not run them through canonical CSW remapping meant for human 4626/Zora profiles.
- The canonical 4626 XMTP agent inbox is **`profiles.csw_address` === `CANONICAL_CSW_ADDRESS`** (`0xab6d5c10b03300326cd7fab7267ae192842967b5`); chat rail rows should open chat to that XMTP recipient while displaying `agent 4626` with main account identity/Ethos context from `0xB05Cf01231cF2fF99499682E64D3780d57c80FdD`, and creator-agent rows that share an XMTP recipient should dedupe to one chat target. **`4626.base.eth` Basename resolves to a different operator display wallet, not the canonical CSW** — do not conflate Basename resolution with `profiles.csw_address` in `/add`, swap balance, keeper, or chat routing debug. **Enabling XMTP chat commands requires two consents:** Privy delegation to the 4626 agent quorum (backend co-sign) plus an on-chain spend-permission signature on the CSW — spend-permission-only signing surfaces "Privy delegation missing".
- `InboxValidationFailed` and XMTP sync-success exceptions indicate a broken local XMTP installation/inbox state, not a normal send failure. Close the client, mark messaging as requiring `Reset local XMTP state`, and clear this browser's XMTP OPFS cache/signing hints instead of repeatedly retrying with the same bad client. OPFS cleanup/reset can fail while another tab **or same-tab XMTP worker** holds the database lock — close other `4626.fun` / `app.4626.fun` chat tabs first; `frontend/src/lib/xmtp/provider.tsx` broadcasts cross-tab release, aborts in-flight connects via connect epoch, retries OPFS delete with backoff, blocks reconnect during reset, and auto-reloads once when the lock persists after worker shutdown (`cv:xmtp:pendingLocalReset` in sessionStorage finishes OPFS delete after that reload). Stream `onError`/`onFail` handlers should mark invalid local state via `isLocalXmtpStateInvalidError` and attempt soft reconnect through `Client.build` before creating a new install; **Reset local XMTP state** should auto-revoke the abandoned browser install (`Client.revokeInstallations`) before recreate so users are not forced to manually delete installs at the 10/10 cap. Page load should honor saved auto-connect via `readAutoConnectEnabled` for restore. After reset/reconnect, stale DM windows can separately hit `conversation_not_found`; recover those by reopening the DM from the peer address/inbox id, rekeying the window, and retrying once. Prefer reloading/reusing existing smart-wallet XMTP inbox/install state over delete-and-recreate cycles that consume the limited install budget (~256).
- **`Client.build` inbox registration must not use `unsafe_applySignatureRequest`.** When OPFS restore succeeds but `isRegistered()` is false, register via `registerBuiltClientInboxViaCreate` (`frontend/src/lib/xmtp/xmtpBuiltClientRegistration.ts`): close the built client, then `Client.create` on the **same stored `dbPath`** (reuses the installation). `unsafe_applySignatureRequest` for inbox creation returns **Unknown signer** and can look like a new install every visit — `xmtpConnectOrchestrator` retries setup only when already registered.
- Base App CSW owner-action probes and prolink owner-add flows should use the prepared-calls lane (`wallet_prepareCalls` → sign the replay-safe/prepared payload → `wallet_sendPreparedCalls`). Signing the raw `userOpHash` directly is not sufficient for Coinbase Smart Wallet owner validation. Canonical CSW self-auth sessions where `walletClient.account` is the CSW must instead route through the direct replayable lane (`eth_sendTransaction` from CSW to CSW with `executeWithoutChainIdValidation`) so popup self-call guards are not selected.
- Direct browser WebAuthn from `4626.fun` cannot assert Coinbase Smart Wallet passkeys issued under Coinbase RP IDs such as `keys.coinbase.com` / `sign.coinbase.com`; keep passkey-direct `navigator.credentials.get` UserOp lanes disabled or diagnostic-only, and route passkey-owned Coinbase CSW owner actions through Coinbase/Base App prepared-call context.
- CSW add-owner Relay paths must stay strict about the selected owner slot: replayable self-built UserOps need a WebAuthn/passkey owner signature for the CSW owner slot they validate against, and relaxed session-key/ECDSA wrappers can revert because `ownerIndex` selects the exact `ownerAtIndex(...)` validation path.
- `/api/relay/quote` is a focused Relay deposit-discovery helper for Base mainnet `EntryPoint.handleOps(...)` calls; it requests Relay `/quote/v2` with `explicitDeposit: true` after signed `handleOps` calldata exists, so CSW owner-add probes can inspect required Relay deposit/order context before execute.
- **Relay Vaults (`relayprotocol/relay-vaults`) is not Relay Settlement / relay-kit.** The relay-vaults repo is an ERC-4626 LP/bridge product; CSW owner mutations use Relay Settlement quote preview + manual Part 1 submit (`preview-add-owner`, `executeOwnerMutationViaRelay`), not relay-kit `execute()` for CSW self-auth. Guide: `docs/operations/relay-owner-mutation-kit-guide.md`; evaluation: `docs/research/relay-vaults-evaluation.md`.
- **CSW Relay Part 1 uses Depository `depositNative`, not router multicall or relay-kit `execute()`.** Parent-CSW self-auth submits preview `userCall` → `RelayDepository.depositNative` at `0x4cD00E38…` (selector `0x49290c1c`). Do not use Relay Settlement **RelayRouter** (`0xb92fe925dc43a0ecde6c8b1a2709c170ec4fff4f`) or its `multicall` (`0xcd6e13f7`) for CSW Part 1 or embedded `addOwnerAddress` — the CSW sees the router as `msg.sender`, not `address(this)`, so owner checks fail and Relay never indexes. Echoed EXACT_OUTPUT wei (~2.88e12) underfunds Part 1 deposits. **`addOwnerAddress` must be a CSW self-call** (EntryPoint UserOp or `wallet_sendCalls` to the CSW); guard with `assertAddOwnerSelfCallShape` in `frontend/src/lib/wallet/addOwnerCallShape.ts` (used on the `/add` Base App owner install surface). **In Base App, skip `wallet_sendCalls`** for Relay Part 1 (it auto-injects USDC paymaster); use prepare → strip paymaster → bundler only, and **fail closed** if a landed Part 1 UserOp has any paymaster — Relay Part 2 requires paymaster = 0. Part 2 is the Relay solver fill; poll status with `orderId ?? requestId`.
- **Relay owner-mutation quote deposit resolution.** Relay `/quote/v2` with `amount: "0"` may omit or zero `protocol.v2.paymentDetails`. When no deposit resolves, `buildOwnerMutationRelayFlow` re-quotes using `deriveRelayOwnerMutationDepositQuoteSeedWei`: seed = clamped zero-quote `fees.gas.amount` to `[MIN_OWNER_MUTATION_RELAY_DEPOSIT_WEI, MAX_OWNER_MUTATION_RELAY_DEPOSIT_SEED_WEI]` (8e12–0.0001 ETH), or MIN when `fees.gas` is absent/zero — no legacy 127× multiplier or gasPrice fallback. Resolve deposit via `resolveQuotedNativeDepositWei` (`paymentDetails`, `details.currencyIn.amount`, step value — ignore zero). Fail closed when no positive deposit; do not synthesize from echoed router value. `RELAY_API_KEY` is optional server-side (`x-api-key` on quote + notify only; status polling is unauthenticated).
- **Wake Relay solver after Part 1 confirms.** `ownerMutationExecution` calls `POST /api/relay/notify-deposit` (`/transactions/index` + `/transactions/single`) after Part 1 deposit confirms; notify failure is non-fatal — Part 2 may still complete via polling/`verifyMutation`. Relay `/transactions/index` expects the **bundle transaction hash**, not the AA UserOp hash — Base App `wallet_getCallsStatus` can return only the UserOp hash, so resolve the bundle tx via `eth_getUserOperationReceipt` in `resolveRelayPart1DepositTxHash.ts` before notify/index and on retries.
- **Relay has no official Discord.** Any server claiming to be Relay Discord is unofficial; integrator support paths are [radar.relay.link](https://radar.relay.link), support@relayprotocol.com, and [relayprotocol/relay-kit](https://github.com/relayprotocol/relay-kit) — do not reference a Relay Discord in runbooks or vendor asks.
- **Owner-install methods (Base App CSW direction)**: The validated, preferred path for Base App users is the **EntryPoint self-call** (`wallet_sendCalls` → single zero-value CSW self-call to `addOwnerAddress` → EntryPoint `handleOps`). This is the only shape that reliably satisfies `MultiOwnable.onlyOwner` because the wallet executes the call on itself. Real transactions confirmed this in 2026; attempts that put `addOwner` inside RelayRouter multicall fail the owner check exactly as expected. **`/add` tries this direct path first**; on Base App policy/funding/build failures (including misleading "not enough funds" when prefund passed), auto-fallback to **Relay Method A** via `shouldAttemptRelayMethodAFallback` + `executeAddOwnerRelayMethodA` — never on user rejections.

 Implementation:
 - Surface + hook: `AddOwnerUserOpExperiment.tsx` (exports `AddOwnerBaseApp`) + `useAddUserOpOwnerInstall` (the validated Base App owner install lane)
 - Core: `baseAppOwnerCalls.ts` (`addOwnerViaBaseAppSendCalls`)
 - Guards (already enforce the correct shape and block router multicall): `assertAddOwnerSelfCallShape` + `assertSendCallsEntryPointAddOwnerBundle` in `addOwnerCallShape.ts`

 This is the direction we are moving toward for Base App users instead of sub-account registration. Sub-accounts remain limited to swap-only scenarios under explicit feature flags. The older Relay Part 1/2 paths for owner mutation remain available for other contexts but are not the primary recommendation for new Base App CSW owner installs.
- **In Base App, Privy email OTP establishes the 4626 session but does not connect the Base Account signing provider.** Wallet execution surfaces (`/add` Method E, owner-install submit, sponsored swaps) need an explicit **Sign in with Base** / canonical CSW link — treat "logged into Privy but not Base" as a session misconfiguration, not an on-chain or Relay failure. `/add` EntryPoint owner-install gates on `ensureCanonicalBaseAccountWallet`: Base Account provider/`eth_requestAccounts` (or Privy Base Account wallet list) must match `profiles.csw_address` before submit. `/add` should surface definitive separate **4626 Privy session** vs **Base Account signing** status with working per-lane sign-out/disconnect — users repeatedly confuse the two.
- **Base App CSW UserOp owner-install requires CSW gas prefund before wallet review.** Base App often fails UserOp generation with "not enough funds" when the parent CSW has **0 native ETH and 0 EntryPoint deposit** (`EntryPoint.balanceOf(csw)` via `frontend/src/lib/wallet/cswEntryPointFunding.ts`). Preflight with `assessCswUserOpFunding` (~0.00005 ETH floor / ~0.0005 ETH recommended) on `/add` and block submit until funded — this is separate from Relay deposit semantics. **When prefund already passes, the same "Error generating transaction / not enough funds" copy is often Base App third-party UserOp policy block (misleading), not missing gas** — map via `mapAddOwnerFundingErrorMessage({ fundingPreflightOk: true })` and fall through to Relay Method A per the owner-install bullet above.
- **Passkey-first Base App Relay Part 1 preflight (Method B reference).** `cswSelfAuthOwnerDiscovery.ts` reads on-chain owner slots before Part 1: WebAuthn passkey at `owner[0]`, Base App session-key signer at `owner[2]`; ignore empty zero slots. Seed `submitSelfAuthRelayPart1SelfFunded` for session-key hash modes and `inner_secp256k1`. **Session-key Part 1 signing:** CSW `validateUserOp` checks the EntryPoint `userOpHash` — try that domain before replaySafe EIP-712 fallback; sign replaySafe via EIP-712 typed data (`CoinbaseSmartWalletMessage`), not raw `personal_sign` on the digest (Base App often surfaces misleading "not enough funds"). Base App may sign with a **delegated EOA** — `wallet_sendPreparedCalls` must use that recovered address in `signature.data`, not only `ownerAtIndex(2)`; prepared-calls signer candidates should try `session_key_delegated_eoa` before `owner_at_index`. When session-key ECDSA is structurally valid, **`validateUserOp` ecrecover preflight mismatch against `ownerAtIndex(2)` is advisory** — still submit via `session_key_prepared_calls_primary` or bundler; do not `continue` early or prompt a second sign. The `[preflightOwnerKeyMismatch]` console line is informational, not the failure. Fail fast only on **ephemeral substituted signers not in the CSW owner array** (`base app substituted signer not in csw owner array` / `baseAppOwnerInstallGuard.ts`). Prefer `eth_sendUserOperation` before prepared-calls; reject passkey (`owner[0]`) signatures for Part 1. Recipe: `docs/operations/base-app-session-key-relay-part1-recipe.md`. Historical Part 2 golden: `0x801b9d4b…91503` (passkey validates Part 2; inner add was session-key slot, not embedded EOA).
- **Relay Part 1 vs Part 2 use different CSW owner lanes (Method B).** Part 1 deposit UserOp = session key at owner slot 2; Part 2 solver fill = passkey `owner[0]` via `getUserOpHashWithoutChainId` (`org.toshi`). Part 2 landing alone does not prove embedded-EOA owner-install — gate on `isOwnerAddress(privyEmbeddedEoa)` and preview `addOwnerAddress(embeddedEoa)`.
- **Relay Part 1 may show only one internal tx.** When the CSW already holds EntryPoint prefund, Part 1 Basescan traces often show only `depositNative` (no separate prefund transfer) — that is normal, not a failed add-owner.
- **Relay owner-mutation status polling uses the deposit-bound id.** Poll `/intents/status/v3` with `orderId ?? requestId` from preview (`resolveRelayStatusRequestId`); treat Relay `status: unknown` as a stale quote; short-circuit and succeed on on-chain `verifyMutation` when Relay status lags. **Browser polling must not hit `api.relay.link` directly** — CSP `connect-src` blocks it; rewrite via `resolveRelayStatusFetchUrl()` to same-origin `GET /api/relay/intent-status` (Node scripts may still call Relay directly).
- **Never call bare `isOwnerAddress` on counterfactual smart wallets.** Guard with `readIsOwnerAddressIfDeployed` (`frontend/src/lib/wallet/cswOwnerRead.ts`) / `getBytecode` first; undeployed wallets return `unknown`, not a viem `"returned no data"` error.
- The deterministic premium token icon renderer is a production TypeScript + Sharp module that exports `renderPremiumTokenIcon({ size, sourceImage?, symbol? }): Promise<Buffer>`; composition (black rounded card, blue aura glow, white-to-blue bezel, recessed inner chamber, subtle top-breakout for suitable subjects, deterministic output) is considered stable — edits must be targeted refinement passes, not rewrites.
- **Token icon renderer lanes** live under `frontend/api/_handlers/token/renderers/`: **`premium-classic`** is the production default (`TOKEN_ICON_RENDERER` unset or `premium-classic`); **`premium-v2`** and **`fuji-lut-experimental`** are offline-compare lanes only until sign-off — A/B via `pnpm -C frontend exec tsx scripts/compare-token-icon-creators.ts` into `frontend/tmp/token-icon-compare/`; do not set non-classic `TOKEN_ICON_RENDERER` in production prematurely. **`premium-v2` silhouette read is padding-only** (`fieldPattern` + `paddingSilhouette` outside the bezel via `paddingSpillMask.ts`) — no in-chamber offset ghost stack; Fuji LUT applies to subject layers only, not full-frame chrome.
- ShareOFT is no longer bridged to Solana; the Solana side uses a standard SPL token created by the bridge `wrap-token` path, routed via `SolanaBridgeAdapter`, and consumers/docs should not reintroduce a ShareOFT→Solana bridge path.
- **Profile-merge infrastructure** is installed (`supabase/migrations/20260419200000_profile_merge_infra.sql`): `privy_user_aliases(privy_user_id PK → profile_id)` + `profiles.merged_into_profile_id` tombstone column. Every profile lookup by privy_user_id or wallet MUST chase `merged_into_profile_id` via the pattern:
  ```sql
  WITH matched AS (SELECT id, email, merged_into_profile_id FROM profiles WHERE <predicate> LIMIT 1)
  SELECT p.id, p.email FROM matched m
  JOIN profiles p ON p.id = COALESCE(m.merged_into_profile_id, m.id)
  WHERE p.merged_into_profile_id IS NULL LIMIT 1;
  ```
  Without this, wallet-match queries can resurrect tombstones (fragmented identity comes back from the dead when wallet sync writes `privy_user_id` onto a tombstoned row). Current tombstone-aware lookups: `accountsIdentity.listProfileIdsForPrivyUser`, `accountsIdentity.readUnifiedScore`, `walletSync.findExistingProfile`, `walletSync.findProfileByProfileColumns`, `profileSync.upsertProfileByWallet`, `amoeProfileResolve.resolveAmoePointsProfile`. Any new profile-lookup code must follow this pattern.
- **`INSERT INTO profiles` must be preceded by a canonical-collision check** whenever the call site has a Privy user id in scope. Use `assertNoWalletPrivyCollision({ db, privyUserId, privyUser })` (or `{ ..., evmAddresses }` when you already have a classified address list). Current guard sites: `api/_handlers/waitlist/_bootstrap.ts` (runs before upsert), `wallet/walletSync.ts:insertOrUpdateProfile` (runs before new-row INSERT only). Skipping this guard re-opens the split-identity bug where a wallet-login Privy user creates a fragment while a canonical email-verified profile already owns the same EOA.
- **`assertNoWalletPrivyCollision` must filter synthetic shells.** Emails matching `%@wallet.4626.fun` or `%@noemail.4626.fun` are pseudo-accounts (AMOE wallet-first claims, legacy wallet-login synthesis). They do NOT count as canonical — a real-email signup that shares a wallet with a shell must be allowed to proceed. The guard has this filter inline; do not remove it.
- **Wallet-collision queries that `ORDER BY p.id ASC` can pick the wrong profile when the same EVM address is attached to multiple live `profile_wallets` rows** — often stale merge-salvage rows on a lower `profile_id` stealing bootstrap/collision resolution from the true owner. Prefer verified-email/canonical-profile resolution over lowest-id wins; audit cross-profile duplicate addresses after merges; salvage sweeps must not leave one wallet on two real-email profiles (consider a live-profile uniqueness guard on `lower(address)`).
- **Email/profile collision ops:** run `pnpm -C frontend exec tsx scripts/diagnose-email-identity.ts --email=<addr>` to inspect profiles, aliases, wallet collisions, and orphan Privy rows before merge/repair — use this snapshot instead of ad hoc SQL alone.
- **Admin profile-merge lives at `POST /api/admin/profiles/merge`** with dual auth: admin session + `CRON_SECRET`. Dry-run by default; `{ mode: 'execute', confirm: 'MERGE-PROFILES' }` required to write. CLI operator: `pnpm -C frontend exec tsx scripts/merge-profiles.ts --from=<id> --to=<id>`. Merge primitive in `frontend/server/_lib/identity/profileMerge.ts` is idempotent per step; re-running is safe.
- **New public tables require RLS.** Supabase's linter (`rls_disabled_in_public`) blocks on any public table without RLS. Pattern: `ALTER TABLE public.<name> ENABLE ROW LEVEL SECURITY;` in the migration, plus a restrictive deny-all policy `CREATE POLICY "deny_public_rest" ... AS RESTRICTIVE FOR ALL TO public USING (false) WITH CHECK (false);` to document "server-only" intent and satisfy the `rls_enabled_no_policy` info-level lint. Never use `FORCE ROW LEVEL SECURITY` — the table owner is `postgres` which is the same role our server connects as, and forcing breaks server writes. Server `DATABASE_URL` connections come in as `postgres` via the Supabase pooler and bypass RLS; `anon`/`authenticated` (PostgREST) default-deny when no permissive policies exist. The schema guard (`pnpm -C frontend guard:schema`) also catches raw DDL attempts that would bypass this rule.

- **Schema definitions are condensed to one source of truth.** There is only one Supabase project (`qajpnuvqlcfseghnldkl`). All DDL must originate in `supabase/migrations/`. The `frontend/db/migrations-legacy/` folder is an archived historical mirror (read-only; do not add files). Runtime `ensure*Schema()` functions must delegate via `frontend/server/_lib/db/schemaBootstrap.ts` using `ensureMigrationApplied(...)` (or named helpers such as `ensureAlfaclubSchema`, `ensureFinalAdditiveColumns`) instead of any raw `CREATE TABLE IF NOT EXISTS` / `ADD COLUMN IF NOT EXISTS` strings. The guard `pnpm -C frontend guard:schema` (implemented in `frontend/scripts/guard-no-raw-schema-ddl.mjs`) is enforced in CI and must pass locally before committing. See `docs/operations/supabase-schema-condensation.md` for the full current state, migration process, and archive details. This permanently eliminates the previous three-copy tax. **Live migration history lags repo file versions:** migrations applied through the Supabase MCP (`apply_migration`) are recorded under MCP auto-timestamp versions, so the live `schema_migrations` names don't match repo filenames — repo files under `supabase/migrations/` remain the canonical DDL documentation and must stay idempotent.
- **June 2026 Supabase table cleanup (DB ~15 GB → ~2.85 GB).** Dropped from the live DB: `creator_ethos_15min_snapshots` (6.7 GB) and `creator_ethos_hourly_snapshots` (5.4 GB) plus their snapshot/prune functions and cron jobs (Ethos health views rebuilt against `creator_ethos_projection` + daily snapshots + `mv_ethos_*`), the `creator_ethos_score_distribution` / `creator_ethos_by_*_bucket` tables, the orphan `query_temp_io_snapshots` telemetry table, and 8 dead scaffold tables (`thread_*` ×4, `payment_rail_attempts`, `base_address_activity_30d`, `farcaster_rollout_events`, legacy `public.alfaclub_chat_ingest`). Do not reintroduce 15min/hourly Ethos snapshot tables — daily snapshots + projection are the trend SSoT. `cleanup_log_retention()` was rebuilt: legacy 5-arg overload dropped (zero-arg cron call resolves unambiguously), wrong `created_at` column refs fixed (`alfaclub_metrics_snapshot` prunes by `snapshot_ts`), duplicate delete blocks merged via `LEAST()`, and `memory_snapshots` / `episodic_summaries` pruning is an **intentional no-op** (Eliza agent memory must not be silently deleted). **Follow-up index/snapshot optimization pass (`supabase/migrations/20260713030000_ethos_index_and_snapshot_optimization.sql`, DB 2.85 GB → ~2.49 GB):** dropped the duplicate `creator_coins` rank index and the single-value `chain_id` index, removed the duplicate projection score index, reindexed `telegram_link_telemetry_events`, and rewrote `snapshot_creator_ethos_daily()` to snapshot **only scored creators** (~15K rows/day instead of all ~84K projection rows; `creator_ethos_daily_snapshots` pruned to scored rows under 90-day retention) — do not revert it to snapshotting the full projection. **Third pass (DB → ~2.0 GB, behavior/maintenance only):** the deprecated Ethos chart matview layer + endpoints were retired (`20260713110000_retire_ethos_chart_matview_layer.sql`); the full `recomputeCreatorCounts` creators reconcile is gated by `maybeRecomputeCreatorCounts` with a `creators_reconciled_at` watermark in `creator_metrics_state` (runs at most every 6h, tunable via `CREATOR_METRICS_RECONCILE_INTERVAL_MINUTES`; the explicit repair script still calls the ungated version) — do not put the ungated full pass back on every sync tick; and churn-heavy tables (`creator_coins`, `zora_csw_owners`, `creators`, `ethos_userkey_scores`, `creator_ethos_projection`, `creator_ethos_daily_snapshots`) carry per-table autovacuum tuning (`20260713130000`) so index bloat does not regrow. The remaining ~2 GB is legitimate data — dropping audit columns (e.g. `zora_csw_owners.creation_tx_hash`) or pruning unmatched CSW rows are explicitly rejected data-loss options.
- **Profile merge moves arch-b tables too.** `executeProfileMerge` in `frontend/server/_lib/identity/profileMerge.ts` moves `points`, `referral_conversions`, `profile_wallets` (Step 3b, counted as `walletRowsSwept` in `ProfileMergeResult`), `command_issuer_execution_context` (re-key or drop based on whether `to` already has one), and `command_issuer_daily_spend` (sum per-ymd) in addition to the referee repointing. Any NEW table that has `profile_id` as FK and represents per-profile state must be added to the merge primitive, otherwise merges leave orphaned rows on tombstones.
- **`profile_wallets` role flags must be forced FALSE on any sweep/salvage INSERT.** Partial unique indexes (`profile_wallets_one_canonical`, `_one_embedded_eoa`, `_one_primary`, `_one_canonical_solana`, `_one_operational_solana`) enforce at most one canonical row per profile. When moving rows from a tombstone (or any non-authoritative profile) onto the canonical target, set `is_primary`, `is_canonical_smart_wallet`, `is_embedded_eoa`, `is_canonical_solana_wallet`, and `is_operational_solana_wallet` to FALSE on the migrated rows, tag with `metadata.salvagedFromTombstoneId` for audit, and use `ON CONFLICT (profile_id, address) DO NOTHING`. The target's own canonical row stays authoritative; never displace it from a secondary attachment.
- **`walletSync.findExistingProfile` is alias-aware.** It consults `privy_user_aliases` before falling back to `profiles.privy_user_id`, mirroring `accountsIdentity.listProfileIdsForPrivyUser`. Without this, Rabby/EOA-first sign-in whose fresh Privy user id is aliased onto a canonical email-verified profile will create a new fragment instead of resolving back to the canonical row. Any future profile-lookup-by-privy-user-id code path must go through the alias cascade, not a direct `profiles.privy_user_id = ?` query.
- **Solana bridge-wrapped mint naming is lowercase-coerced, strict-parity.** `normalizeWrapTokenName` / `normalizeWrapTokenSymbol` in `frontend/server/_lib/onchain/solanaBridgeTokenMetadata.ts` lowercase the Base ERC-20 `name()` / `symbol()` before passing them to `wrap-token`, so every creator's Solana display ends up uniformly lowercase (e.g. `"akita"` / `"akita"`) regardless of Base casing. The mint pubkey is a PDA of those seeds, so the lowercase form IS the on-chain Solana identity — changing casing later would require a different mint address and full re-provisioning (new adapter, new DLMM pool, new Alpha Vault). The provisioner request body does NOT accept `tokenName` / `tokenSymbol` overrides; `ProvisionBody` in `frontend/server/solana-provisioner/index.ts` intentionally omits them. Byte-length checks run both BEFORE and AFTER lowercasing to guard against Unicode case folding that changes UTF-8 byte count (e.g. Turkish dotted I). Old name aliases `normalizeExactWrapTokenName` / `normalizeExactWrapTokenSymbol` remain as deprecated re-exports for import stability.
- **`SolanaBridgeAdapter` is plain `Ownable` (not upgradeable); Solana-side rebrands deploy a fresh adapter and swap the default.** `SolanaBridgeAdapter.registerToken` hard-reverts if `tokenToSolanaMint[baseToken] != 0`, so re-registering a creator is impossible on an existing adapter. Canonical address is the `solanaBridgeAdapter` in `BASE_DEFAULTS` (`frontend/src/config/contracts.defaults.ts`) / `VITE_SOLANA_BRIDGE_ADAPTER`. One historical predecessor at `0x90F578A4e23c1cB8DDFE63fd496ED7F4474f2b00` carries a legacy AKITA→`HuY4…9ouR` mapping from the pre-strict-parity era and is left on-chain for auditability; `docs/operations/solana-bridge-naming-invariant.md` has the full migration record. No live code references it.
- **Solana naming invariant is canonical-documented and parity-verifiable.** `docs/operations/solana-bridge-naming-invariant.md` is the single source of truth for the lowercase-coerced Solana mint naming policy, PDA derivation formula, AKITA v1→v2 migration history, per-drift runbook, and Meteora integration runbook. Shared PDA derivation lives in `frontend/server/_lib/onchain/solanaWrappedMintPda.ts` (`deriveWrappedMintPda`) with golden fixtures pinned to live mainnet state (legacy `HuY4…9ouR` and strict-parity `9JWh…LJdp` for AKITA). To check any creator's parity end-to-end (Base ERC-20 → adapter mapping → Solana on-chain metadata), run `pnpm -C frontend exec tsx scripts/verify-solana-mint-parity.ts --creator 0x<creator>` — exit 0 = parity, exit 2 = drift with a named reason. The programmatic entry point `verifyCreatorSolanaMintParity` in the same `_lib/onchain` folder returns a structured `{matched, drift[]}` result and is safe to call from scripts, CI, or status endpoints.
- **Greenfield vault deploy requires paid `vault_full_deploy` ($499 USDC, all-or-nothing).** `docs/operations/creator-strategy-features.md` is the canonical spec. Public catalog sells one bundle that expands to Charm (`charm_active_lp`) + Ajna (`ajna_sleeve`) at **45%/45%** Phase 3 weights, `solana_ovault_mesh` (Phase 2b Pipe A finalize bridge, not Phase 3 TVL), and `solana_meteora_alpha_vault` (Meteora entitlement, operator-provisioned post-deploy). **À-la-carte purchases** of those four keys return **HTTP 410** — activate `vault_full_deploy` at `/creator/strategy/features`. Legacy individual comp rows still resolve (AKITA). **`solana_bridge_strategy` is retired** — activation APIs return HTTP 410. **Three payment paths**, all landing in the same `creator_strategy_features` row with a `payment_source` discriminator: (1) `usdc_base` — creator sends a plain USDC `transfer` + POSTs tx hash to `/api/creator/strategy/activate`; verifier matches `Transfer(from=session,to=treasury,value>=effective)` log regardless of tx shape. (2) `x402_base` — creator POSTs `/api/creator/strategy/x402-activate` without a tx hash; server replies HTTP 402 with payment requirements; client signs EIP-3009 `TransferWithAuthorization`, base64s it into `X-PAYMENT` header, resubmits; server relayer (`X402_RELAYER_PRIVATE_KEY` or fallback `PRIVATE_KEY`) broadcasts `usdc.transferWithAuthorization(...)` and pays Base gas — single round trip, gasless for creator; `UNIQUE (payment_from, x402_authorization_nonce)` prevents replay. (3) `stripe` — creator POSTs `/api/creator/strategy/stripe/checkout`, server creates a Stripe Checkout Session (USDC price → USD cents via `floor(priceUsdc / 10_000)`), client redirects, Stripe webhook `checkout.session.completed` hits `/api/creator/strategy/stripe/webhook` which finalizes the row; Stripe fees apply (~$14.77 on a $499 bundle sale). **Discounts are supported operator-side** via `creator_strategy_price_overrides`: insert a row scoped to `creator_token` (per-vault) or `wallet_address` (per-buyer) with a `price_usdc_override` (and optional `expires_at`). `findActivePriceOverride` prefers creator-scoped match first then wallet-scoped; `applyPriceOverride` clamps effective price to `min(override, catalog)` so a malformed override can never raise prices. All three payment paths honour overrides: USDC clamps `minAmount`, x402 clamps `max_amount_required` in the 402 response, Stripe converts the discounted USDC into cents. **`vault_full_deploy` (or legacy Charm+Ajna entitlements) is required** — `DeploymentBatcher.deployPhase3Strategies` reverts with `InvalidWeight` when `charm+ajna == 0` (Phase 3 `solanaWeightBps` is always 0; Solana share seeding is Pipe A at finalizePhase2), `computeStrategyWeights` returns `{ok:false, reason:'no_paid_strategies'}`, and `/api/creator/strategy/list` returns `deployPlan.deployable = false` with `blockedReason = 'no_paid_strategies'`. **Weight scaling:** with the bundle (or both legacy keys), **4_500 bps Charm + 4_500 bps Ajna + 1_000 idle**; legacy single-strategy comps still get 9_000 bps on the paid leg only. Payment: client sends USDC `transfer` on Base to `protocolTreasury` (overridable via `CREATOR_STRATEGY_FEATURE_USDC_TREASURY`), POSTs `{creatorToken, featureKey, paymentTxHash}` to `/api/creator/strategy/activate`. `verifyUsdcPayment` is authoritative on the decoded `Transfer(from=session, to=treasury, value>=price)` log — tx shape (multicall, UserOp, router) doesn't matter, only the log. Deploy-side: `resolveCreatorStrategyPlan(db, creatorToken)` in `frontend/server/_lib/creatorStrategy/resolveWeights.ts` returns a tagged `{ok:true,plan}` or `{ok:false,reason:'no_paid_strategies',...}`. The patched `DeploymentBatcher.deployPhase3Strategies` skips the factory call + strategy deploy + `addStrategy` for any strategy with `weightBps == 0`; `StrategyCodeIds` for skipped strategies may be `bytes32(0)`. Phase 3 never deploys `SolanaBridgeStrategy` on greenfield batchers. Uniswap V3 CREATOR/USDC pool creation stays unconditional so future Charm activation has a pool ready, so single-strategy deploys must still pass `initialSqrtPriceX96 > 0` when the pool doesn't pre-exist. DB is `creator_strategy_features` with `UNIQUE (creator_token, feature_key) WHERE status IN ('pending','active')` and `UNIQUE (payment_tx_hash)`. `pending` == "paid but provisioner hasn't run yet"; the resolver counts both `pending` and `active` as paid so creators can deploy immediately after their USDC clears. **Post-deploy strategy addition (operator / legacy partial vaults only):** public API cannot buy individual strategies; operator runs `pnpm -C frontend exec tsx scripts/activate-strategy-post-deploy.ts --creator 0x… --feature <key>` for Safe calldata when a legacy vault deployed with only one strategy. All three payment handlers (`_activate.ts`, `_x402-activate.ts`, `stripe/_webhook.ts`) call `dispatchProvisioning` in `server/_lib/creatorStrategy/provisioner.ts` after row insertion — for v1 that's an enqueue-only stub that logs intent + returns an operator-targeted note; any failure is non-fatal (the `pending` row is the source of truth, operator polls). **Creator-facing UI is live at `/creator/strategy/features`** ([`frontend/src/pages/CreatorStrategyFeatures.tsx`](frontend/src/pages/CreatorStrategyFeatures.tsx)) — reads `/api/creator/strategy/list`; deploy strategies stay full cards with USDC/x402/Stripe payment, while vanity tiers are grouped into two collapsed optional sections via `frontend/src/lib/creatorStrategy/featuresPageLayout.ts` (vault prefix + share suffix tables). Unpaid deploys show an amber **activate `vault_full_deploy`** blocker. Operator comps: `pnpm -C frontend exec tsx scripts/grant-creator-strategy-comp.ts --creator=0x… --all-deploy-gating --execute --confirm=GRANT-STRATEGY-COMP` inserts `$0` `creator_strategy_price_overrides` and optional pending activation rows. **Mainnet greenfield deploy infra (v1.11.2-pipe-a shell, v1.12.1 bytecode epoch):** active `DeploymentBatcher` is `0xa99058f424FB3ACC639F59355C65C40149030651` with wired helpers (`phase2Module` `0x67FD…`, `phase3Helper` `0x674a2D5EE33e184e2120B373a9AcB3fef640885c`, `uniV4Helper` `0xF71a…`, `utilsHelper` `0xD71C…`), store-aligned Phase1Module `0x19Bd8d3b69Ee8b4D127adb0DE35372e2825FFC87`, paired CREATE2 deployer `0x4760216AFd59B843671E0FdFCe6498Ec8CFf38a7` + bytecode store `0x8B51E6784A0C6681F5de25bAC4f9B2fDCEDE72b4` (deprecated pair: deployer `0xF653…` → store `0x9C3e2A7…`). Deprecated shells (`0x16aEA859…`) must not be used for new deploys. **`v1.12.1`** adds payout-router queuer bridge + live `.v2` OVault module rotation; **`v1.12.0`** refreshed creation bytecode + Phase3 helper — after helper rotation authorize `phase3Helper` on the create2 deployer. Re-seed store from matching manifest, regenerate `frontend/src/deploy/bytecode.generated.ts`, verify with `scripts/ops/verify-bytecode-store-seeded.ts`, `verify-phase1-module-store-alignment.ts`, and `verify-phase3-helper-create2-auth.ts`. Runbooks: `docs/operations/deployment/releases/v1.12.0-bytecode-epoch.md` and v1.12.1 module rotation notes in `docs/reference/addresses.md`. Canonical addresses: `docs/reference/addresses.md` and `frontend/src/config/contracts.defaults.ts`. Paymaster-side `gateRequestedStrategyWeights` IS now wired into `api/_handlers/paymaster/_paymaster.ts`: Phase 3 UserOps are rejected with `paywall_weight_gate:<reason>` or `paywall_db_not_configured` before sponsorship if requested weights don't match the resolved paid plan; skipped strategies' codeId/keeper args may be zero. AKITA stays pinned to the v1.8.3 batcher at `0xcDbEeB76…C4F` (grandfathered, unaffected). **Still pending:** (a) Stripe end-to-end test against a real Stripe signature (signature verification path is confirmed; real payload handling depends on body-parser behavior), (b) x402 in-dapp wallet-signing helper (currently the UI surfaces raw 402 requirements for manual EIP-3009 signing), (c) automated Safe broadcast from the activation script (today the script prints calldata; operator submits via Safe UI). Adding a new feature = catalog entry + union member + runbook subsection in the doc; no migration.
- **Vault deploy pricing is flat-rate and USDC-denominated for now.** Per-strategy purchase pricing is removed; an AKITA-denominated deploy payment (Virtuals-style, paying in `$AKITA` `0x5b674196812451b7cec024fe9d22d2c0b172fa75`) is an explicitly deferred future phase — product decided to keep USDC pricing at launch and revisit AKITA value-capture via post-settlement routing later, partly because AKITA's USD price is too volatile/low to anchor pricing today. **The paid gate is surfaced on `deploy/vault` itself, before the flow:** `DeployVault.tsx` queries `/api/creator/strategy/list?creator=…`, blocks `canDeploy` unless `vault_full_deploy` is active, shows a "USDC deploy feature active" readiness row, and renders a blocked card linking to `/creator/strategy/features?creator=…` — do not rely on the strategy-features page alone to communicate the paywall.
- **`strategyMaxAssets` is a governance trust ceiling, not an allocation target.** Treat it as "maximum valuation the vault is willing to trust until governance/operator review updates the cap" and avoid static policy formulas like "30% of initial TVL + buffer" as durable guidance.
- **Charm and Ajna coordinate only through the parent vault idle CREATOR buffer, not direct strategy-to-strategy transfers.** `deployToStrategies()` / `tend()` split idle above `minimumTotalIdle` into weighted `deposit()` calls; user withdrawals pull strategies back via `_withdrawFromStrategies()`. `updateStrategyWeight()` changes target bps only — it does not auto-move TVL, and there is no vault `deallocate()`. Post-deploy weight shifts (e.g. Charm-only → 45/45) converge via KPR `vault-strategy-reallocator` calling `rebalanceStrategies()` (up to `VAULT_STRATEGY_REALLOC_MAX_PASSES` per tick, default 4) plus keeper_jobs `rebalance` workflow when enabled. **`CreatorCharmStrategy` may optionally borrow CREATOR from Ajna on withdraw shortfalls and repay on deposit** — that backstop is separate from the **`ajna_sleeve`** lending lane.
- **`solana_ovault_mesh` is not a Phase 3 vault strategy allocation.** Treat it as a Phase 2b routing entitlement; there is no `setStrategyMaxAssets` or `addStrategy` calldata to apply for this non-strategy entitlement.
- **Pipe A (greenfield ShareOFT auto-bridge at finalize) replaces Phase-3 `solana_bridge_strategy` TVL for new deploys.** Policy: `docs/operations/solana-share-mesh-lottery-policy.md`. Phase 2 split is **30/30/30/10** (auction / vesting / Solana LZ bridge / LP reserve). `finalizePhase2` / `finalizePhase2WithPermit2` are **payable**; session create + paymaster validate LZ `msg.value` and OFT peer wiring via `frontend/src/lib/deploy/finalizeShareBridgeFee.ts` and `shareBridgeOftWiring.ts`. Batcher must expose `solanaShareOftPeer()` + `setSolanaShareOftPeer(bytes32)`; registry peer OR batcher default peer must resolve before finalize (module seeds registry from batcher default when unset). **Greenfield Phase 2 finalize also requires `CreatorRegistry` factory auth:** split batcher must have `authorizedFactories(batcher)=true` on live registry `0x3f64087dc361Ad52300409E5873b26941D6418B6` — otherwise finalize reverts `NotAuthorized()` (`0xea8e4eb5`); wire via `CreatorRegistry.setAuthorizedFactory(batcher,true)` from registry owner (`script/SeedCreatorRegistry.s.sol`). **Live greenfield batcher is `SPLIT_PHASE1_DEPLOYMENT_BATCHER` (`0xa99058f424FB3ACC639F59355C65C40149030651`, v1.11.2-pipe-a)** — Pipe A wiring is live; readiness gate: `pnpm -C frontend exec tsx scripts/ops/verify-batcher-pipe-a-readiness.ts` (exit 0 = ready, includes `creator_registry_batcher_authorized`). Pipe A `previewDeposit` **"returned no data"** on the predicted wrapper means **no bytecode at that CREATE2 address yet** (Phase 1 core not landed) — not a missing ABI; debug with `frontend/scripts/ops/trace-wrapper-address.ts`. Deprecated pre-cutover shell `0x16aEA859…` must not be configured for new deploys (`docs/operations/deployment/batcher-pipe-a-cutover.md`). Safe ops: `frontend/scripts/ops/propose-batcher-solana-config-safe.ts` (`--only-share-oft-peer` / `--include-share-oft-peer`); execute via `execute-pending-safe-txs.ts`. Deploy UI: `ShareBridgeFinalizeWiringPanel` on `/deploy/vault`. Tests: `finalizeShareBridgeFee.test.ts` + `shareBridgeOftWiring.test.ts` (~1000+ stress iterations each) + `test/DeploymentBatcher.ShareOftPeerWiring.t.sol`. **Pipe-A batcher cutover is also blocked by EIP-3860:** v1.11.2-pipe-a `DeploymentBatcher` shell initcode (~53KB) exceeds the 49,152-byte CREATE2 limit — shrink initcode (more helper extraction or minimal-proxy deploy) before the shell can land; helper modules may deploy first via `wireDeploymentHelpers`.
- **Active chunked bytecode store seeding must include frontend deploy bytecode IDs as well as Forge artifacts.** The deploy page/paymaster use `frontend/src/deploy/bytecode.generated.ts` `DEPLOY_BYTECODE` code IDs; if the store is seeded only from current Forge artifacts, preflight can report missing bytecodes for contracts such as `OFTBootstrapRegistry`, `CreatorShareOFT`, `PayoutRouter`, `CreatorOVaultWrapper`, `CreatorGaugeController`, `CCALaunchStrategy`, `CreatorOracle`, `VaultShareBurnStream`, `AjnaERC4626Vault`, `ERC4626StrategyAdapter`, and `SolanaStrategy` even when similar artifact bytecode is present. `OFTBootstrapRegistry` must implement `getEidForChainId` because `CreatorShareOFT` resolves its LayerZero EID in the constructor.
- **UniversalCreate2DeployerFromStore is ACL-gated, and creator CSWs should not be individually authorized.** Direct deploy calls are allowed only from its owner or `authorizedDeployers`; creator-facing Base App/canonical-CSW deploy flows must route stored-bytecode CREATE2 through an authorized `DeploymentBatcher`, while direct deployer authorization stays limited to protocol/operator deploy surfaces. If deploy preflight reports creator-CSW CREATE2 authorization blockers, rotate to or configure a deploy-capable batcher rather than repeatedly authorizing each creator wallet. **On-chain CREATE2 deployer for live greenfield Phase1Module is `0x4760216AFd59B843671E0FdFCe6498Ec8CFf38a7`** (must pair with bytecode store `0x8B51E6784A0C6681F5de25bAC4f9B2fDCEDE72b4`; deprecated `0xF653…` points at store `0x9C3e2A7…`) — keep `contracts.defaults.ts`, Vercel `UNIVERSAL_CREATE2_FROM_STORE` / `VITE_UNIVERSAL_CREATE2_DEPLOYER`, and `.env.deploy-dry-run.local` on the paired epoch; `DeployVault` prefers `create2Deployer()` from the wired Phase1Module before falling back to defaults. **Phase1Module `create2Deployer.store()` must equal wired `bytecodeStore`** — CREATE2 reads the deployer immutable store, not the batcher getter; mismatch causes missing-bytecode preflight (`resolveAlignedPhase1DeployDeps`).
- **Phase 3 CREATE2 runs from `phase3Helper` (external call), not batcher delegatecall.** Phase 1/2 delegatecall keeps `msg.sender` as the batcher shell; Phase 3 external-calls the helper so CREATE2 `msg.sender` is the helper address — it must be on `authorizedDeployers` or deploy reverts `NotAuthorizedDeployer()` (`0x8164ae93`). After v1.12.0 helper rotation run `authorize-phase3-helper-create2.ts`; dry-run forks auto-prep via `ensurePhase3HelperCreate2AuthorizationOnFork`; live check: `verify-phase3-helper-create2-auth.ts`.
- **Deploy preflight bytecode/config reads may fall back to Base public RPC, but execution must not.** `DeployVault.tsx` uses a read-only `https://mainnet.base.org` fallback when the browser wallet RPC returns empty bytecode for live Base contracts; actual deploy transactions still route through the normal wallet/UserOp path.
- **AlfaClub Creator Coin/FriendKey LP is a secondary-market ERC20/ERC1155 AMM, not an AlfaClub primary-curve writer.** `contracts/alfaclub/AlfaCreatorKeyLPFactory.sol` and `AlfaCreatorKeyPool.sol` pair Creator Coins with AlfaClub FriendKey tokenIds using xy=k LP shares; they should transfer existing keys/coins only and must not call `FriendKey.buyShares`, `sellShares`, stake/unstake, or `FriendPool` write methods. The exploratory curve/LP convergence chart lives at `docs/alfaclub-lp-vs-bonding-curve-chart.html`. **Pool fee bps are room-type-scoped:** Social rooms = 3 bps, Trading rooms = 690 bps (6.9%); apply the correct fee constant when computing LP cost quotes or pool invariant checks.
- **4626 has two vault-share burn lanes — do not conflate them.** ShareOFT trade-fee revenue hits `CreatorGaugeController` and burns ~21.39% of vault shares immediately on distribute (no keeper harvest). Creator Coin external revenue in `payout_router` mode flows `PayoutRouter.convertAndQueue` → vault deposit to `VaultShareBurnStream` → permissionless weekly `checkpoint()` drip. Keepr harvest automates only the PayoutRouter lane; `drip()`/`checkpoint()` are public.
- **PayoutRouter ZORA payout conversion prefers a direct Uniswap V3 ZORA→creatorCoin path** when the pool exists (`resolvePayoutRouterSwapPaths` in `frontend/server/_lib/onchain/payoutRouterSwapPaths.ts`); multi-hop ZORA→WETH/USDC→creator is fallback only — do not default to WETH-hopping when direct liquidity is available. **Harvest V3 swaps must never run with zero min-out:** both the KPR action and the Vercel handler derive `minOut` by quoting the stored on-chain path through Uniswap QuoterV2 and applying the configured slippage bps; if the quote fails and no explicit floor is configured, the swap leg is skipped rather than executed unguarded. **Keeper signer resolution accepts bare 64-hex keys:** `KPR_PRIVATE_KEY` is stored without the `0x` prefix in this workspace — `resolvePayoutRouterKeeperPrivateKey` and `readHexPrivateKey` (`protocolTreasurySafe.ts`) normalize both forms. Leave `PAYOUT_ROUTER_KEEPER` unset in production so the keeper auto-derives from `KPR_PRIVATE_KEY`; never set it to the canonical CSW (harvest signs EOA-only and `convertAndQueue` is `onlyOwnerOrKeeper`).
- **Bridge ↔ Meteora integration is two decoupled hops.** `SolanaStrategy.rebalanceToSolana` only moves CREATOR tokens from the strategy contract to the `SolanaBridgeAdapter` on Base — it does NOT cross the bridge. The actual bridge + Solana-side destination routing is the `keepr-solana-rebalance` keeper workflow (`kpr/actions/keepr-solana-rebalance.action.ts` + `kpr/workflows/keepr-solana-rebalance.workflow.ts`, currently a stub gated behind `KPR_SOLANA_REBALANCE_EXECUTE=1`). Routing policy: if a creator has an `enabled=true` row in `creator_meteora_alpha_vaults`, dispatch `adapter.bridgeToSolanaWithIxs(creator, amount, alphaVault, ixs[])` for atomic bridge + Alpha Vault deposit; otherwise dispatch plain `adapter.bridgeToSolana(creator, amount, destination)` to a custody wallet. Per-creator Meteora infra (DLMM pool + Alpha Vault) costs ~3.5 SOL and is optional — creators without Meteora still bridge successfully; their SolanaStrategy accounting uses keeper NAV reports rather than on-chain DLMM valuation. Runbook in the canonical doc.
- **Keeper job coordination is the KPR-independent fallback lane, but not yet a full KPR replacement.** `public.keeper_jobs` is created by `supabase/migrations/20260506221000_keeper_job_coordination_queue.sql` with deny-all RLS; `/api/keeper/jobs/*` handles enqueue/claim/complete/status/health/run behind machine auth or cron auth; `pnpm -C frontend keeper:jobs:worker` claims `internal_api`/`noop` jobs using `KEEPER_COORDINATION_BASE_URL` and `KPR_API_KEY`. Legacy **`/api/cre/*` is removed** — `internal_api` payloads must use `/api/keeper/*` or `/api/keepr/actions/*` only (`docs/operations/keeper-http-api.md`). Current confirmed coverage is sweep/tend/report/**rebalance**/payout multi-vault fan-out plus sweep -> mark-settled follow-up. **Payout-router harvest** (`POST /api/keeper/payout-router-harvest` + KPR `payout-router-harvest`) is code-complete for `payout_router` vaults, but Vercel cron enqueue alone does not run harvest — `/api/keeper/jobs/run` cron (every 5 min) or an always-on **`keeper:jobs:worker`** or Railway KPR must claim jobs. **Pick one primary executor:** Vercel `keeper_jobs` payout fan-out (`KEEPER_ACTIVE_VAULT_ENQUEUE_ENABLED=1`) with Railway `KPR_PAYOUT_ROUTER_HARVEST_ENABLED=0`, or direct KPR harvest with payout omitted from Vercel workflows — do not run both against the same router. Greenfield phase 2 now wires `setBurnStreamAuthorizedQueuer(router)`; per-vault treasury `setKeeper` + swap paths self-heal via `maybeAutoSetupPayoutRouterTreasury` (Safe-batched `setKeeper` + `setSwapPath`) when `PAYOUT_ROUTER_TREASURY_AUTO_SETUP=1` — until setup runs, ZORA accumulates safely in the router and harvest skips. **Production payout harvest is enabled:** `KEEPER_ACTIVE_VAULT_WORKFLOWS` must include `payout` (it was silently missing) plus `PAYOUT_ROUTER_TREASURY_AUTO_SETUP=1` and `KEEPER_ACTIVE_VAULT_PAYOUT_RECIPIENT_MODE=payout_router`; gauge-mode vaults (e.g. AKITA) correctly produce no payout jobs. **`enqueue-active-vaults` honors the per-vault kill switch:** the enqueue query excludes vaults whose `keepr_vault_automation` row is `automation_enabled=false` or revoked (vaults without a row stay enabled); pausing a vault is a one-row update plus cancelling already-queued jobs — AKITA's scheduled tend/report/rebalance/payout automation is currently paused by operator this way. `process-keepr-actions` queue processing, bridge-integrity, Ajna/Charm, strategy-signal listener, and Solana workflows still need explicit replacement lanes. Enable cross-strategy rebalance in the worker with `KEEPER_ACTIVE_VAULT_WORKFLOWS=...,rebalance,...`. Keep privileged signing behind existing machine-auth endpoints, keep worker paths allowlisted to `/api/keeper/` and `/api/keepr/actions/`, and monitor non-zero retry/failed/expired-claim counts once carrying production work.
- **AKITA grandfathered vault keeper stack (Phase 1 Base).** AKITA is not in `CreatorRegistry`, so `/api/vaults/active` and local `vault-keeper` need the shared `grandfathered_onchain` fallback in `validateKeeperVaultListing` (`frontend/server/_lib/onchain/creatorRegistryVerification.ts`) and `kpr/utils/registry.ts` — strict registry-only checks drop the backfilled `keepr_vaults` row. On-chain AKITA `keeper`/`management` still point at deprecated batcher `0x32403a647e73e04ae42b02bdd1ade9c88698fd0c`; **owner** is the **canonical CSW** (`CANONICAL_CSW_ADDRESS` / `profiles.csw_address`, `0xAb6d5c10b03300326cd7fab7267ae192842967b5`). Default Vercel HTTP bridge tend/report (`KPR_USE_KEEPER_HTTP_BRIDGE` unset + `KPR_API_BASE_URL`/`KPR_API_KEY`) signs with `KPR_PRIVATE_KEY` as an EOA and fails unless that key is funded **and** matches on-chain `keeper` — AKITA needs `setKeeper` rotation or the local ERC-4337 lane below. Pre-deploy smoke from repo root: `./scripts/ops/sync-kpr-env-from-vercel.sh` then `./scripts/ops/test-akita-keeper-stack.sh`. Prod Supabase needs `keepr_vaults` + `keepr_vault_automation` rows and migration `supabase/migrations/20260512010000_ajna_vault_registry.sql` for `ajna_vaults`.
- **Local AKITA tend/report uses ERC-4337 on the canonical CSW, not the Vercel EOA bridge.** Set `KPR_USE_KEEPER_HTTP_BRIDGE=0` with `KPR_ERC4337_ENABLED=true` and `KPR_ERC4337_*` (not legacy `CRE_ERC4337_*` — KPR no longer reads `CRE_*`) so `vault-keeper` signs via the canonical CSW owner lane (CDP bundler/paymaster). `./scripts/ops/sync-kpr-env-from-vercel.sh` syncs `KPR_API_KEY`, RPC, and orchestrator URLs only — **not** `KPR_PRIVATE_KEY`; keep keeper signing keys local or set explicitly on Vercel.
- **AKITA Solana keeper Phase 2 stays blocked until ShareOFT maps on the canonical adapter.** Preflight fails when AKITA ShareOFT is not registered on canonical `SolanaBridgeAdapter` `0x700b4BBAf965c013123bAd02a6562FBa487aC0f1` — Phase 1 Base tend/report can be green while Solana reconcile remains `manual_review`.
- **Charm Alpha Vault deploy sets `rebalanceDelegate` to the creator CSW (`params.owner`), not protocol treasury.** `DeploymentBatcher` Phase 3 keeps `manager: protocolTreasury` for fee/parameter control; only the rebalance trigger address moves to the vault owner CSW.
- **Automated Charm rebalance uses the protocol treasury Safe manager lane, not a bare KPR EOA or creator CSW delegate.** When Charm `manager()` is the protocol treasury Safe (`0x7d429e…`), keepers route `charmVault.rebalance()` through `executeViaProtocolTreasurySafe` (`frontend/server/_lib/wallet/protocolTreasurySafe.ts`, `kpr/utils/protocolTreasurySafe.ts`) so `msg.sender` is the Safe manager; signer resolution is `PROTOCOL_TREASURY_SAFE_OWNER_PK` → `KPR_PRIVATE_KEY` → `PRIVATE_KEY`, and the key must be an **EOA owner** on that Safe. Creator CSW `rebalanceDelegate` is for manual Base App rebalance; CSW UserOp automation applies only when the queue row CSW matches the delegate.
- During KPR sunset, `frontend/api/_handlers/vaults/_active.ts` and `frontend/api/_handlers/kpr/vaults/_active.ts` are not behavior-equivalent; keep the live route pinned to the KPR handler until parity migration is explicitly completed.
- **`resolveCommandIssuerContext*` (canonical implementation in `@4626/server-core` via `commandIssuerContext.ts`; transitional thin re-export still at `frontend/server/_lib/wallet/commandIssuerContext.ts`) is tombstone-aware.** Both `ByAddress` and `ByProfileId` chase `merged_into_profile_id` and filter tombstones. Future arch-b sub-account rollout (`docs/arch-b-sub-account-design-addendum.md`) will add `sub_account_address` / `parent_csw_address` / `spend_permission_*` columns to this table — the resolver's tombstone filter must be preserved through that migration. The addendum is design-only today; when implementing, gate behind `ARCH_B_SUB_ACCOUNTS_ENABLED` and keep the `smart_wallet_address = sub_account_address, parent_csw_address = funding CSW` invariant so legacy rows (sub_account_address IS NULL) continue to work unchanged.
- **Production host split is two SPA shells, not one.** On `4626.fun`, **`/` is rewritten to `public/immersive/index.html`** (not root `index.html`) via `frontend/vercel.json`; SPA routes like `/waitlist` and `/faq` still use marketing `index.html`. `https://app.4626.fun/*` serves `app.html` (Deploy, Portfolio, and other authenticated routes). **Marketing ↔ app must show the same canonical CSW:** waitlist applies `mergeCanonicalWaitlistAccount` from `/api/onboarding/bootstrap`; `useAccountMe` on `app.4626.fun` must use the same merge — do not let stale `profiles.csw_address` / session snapshots show a different address when navigating `4626.fun/waitlist` → `app.4626.fun/swap`. Favicon/brand fixes must include **`public/immersive/index.html`** and root `/favicon.ico` — partial deploys can leave marketing home stale while SPA routes already bumped `brandAssetVersion`. Privy email OTP and waitlist onboarding auth run only on React SPA routes (`/waitlist` → `WaitlistFlow`) — do not mount Privy on the static immersive home or duplicate auth there; marketing CTAs should link to `/waitlist?start=1` so Privy auto-opens on the SPA waitlist route. Base App mini-app `primary` URL should not bypass that marketing waitlist entry — setting primary URL only to `app.4626.fun` can skip the `4626.fun` immersive home and waitlist flow unless product explicitly wants app-only entry. Deploy/app deep links must use `https://app.4626.fun/...` — pointing users to the bare `4626.fun` host for `/deploy/vault` or similar authenticated routes is a recurring regression that silently lands them on the marketing shell. Auth uses the HttpOnly `cv_auth_session` cookie with `Domain=.4626.fun` on production hosts so sessions survive marketing-shell ↔ app-shell navigation; localhost/dev remains host-only, and session reads tolerate duplicate legacy host-only cookies during rollout. The canonical origin pair is documented in `frontend/.env.example` via `VITE_PRIVY_ALLOWED_ORIGINS` and `DEPLOY_SOLANA_REGISTRATION_ORIGINS`.
- **Waitlist UI must never synthesize a "full" state from missing stats.** `/api/waitlist/stats` returning null/500 or a zero capacity is an unknown state, not a "0 / 0 — Current round full" state. `frontend/src/features/waitlist/WaitlistFlow.tsx` must hide progress/urgency copy when stats are unavailable rather than fabricating a full-round banner — the "Current round full. Next approvals unlock the next batch." regression came from defaulting to 0/0 when the stats endpoint was 500ing.
- **Privy cross-app (Zora) linking must open its popup inside the click gesture.** Privy opens the Zora cross-app window with plain `window.open()`, which browsers only allow within the click's transient user activation. Waitlist `onLinkZora` calls `performZoraCrossAppAuth` synchronously on click and defers the access-token fetch, `/api/accounts/link`, and signal resolution until after cross-app auth completes — any awaited pre-work before the popup (e.g. a slow `/api/zora/resolve`) consumes the activation, the popup is silently blocked, and Privy shows its generic "Authentication failed" modal. This linking lane is read-only (Zora's provider shares the wallet read-only; no transact capability is requested).
- **Zora-provisioned Coinbase Smart Wallets are owned by the Privy embedded EOA inside Zora's Privy app — NOT by the user's external Rabby/MetaMask EOA, and NOT necessarily by our app's Privy embedded EOA.** `CoinbaseSmartWallet.addOwnerAddress(address)` is `MultiOwnable.onlyOwner` (`msg.sender == address(this)`), so adding a new owner requires a self-UserOp signed by an existing owner — a plain `eth_sendTransaction` from any external EOA reverts, even if that EOA is "connected". Privy's Zora cross-app connect path was evaluated and rejected: Zora's provider accepts Connect handshake but refuses transact for our requester app, and the surfaced signer is not a CBSW passkey owner. Do not reintroduce "plain Base tx calling `addOwnerAddress`" language in UI copy or docs — it silently misleads users about what the flow actually does. The canonical in-app owner-install path is `POST /api/onboarding/preview-add-owner` → `useAddOwnerFlow` → `executeOwnerMutationViaRelay` (Relay deposit + solver fill; golden txs in `docs/operations/relay-owner-mutation-kit-guide.md`); Zora iframe / deploy automation may still use `POST /api/onboarding/preview-agent-owner` and `POST /api/onboarding/provision-agent-owner` for the **server agent wallet**, not the login embedded EOA. Also: **never assume a Zora CSW needs an owner install just because it's Zora-linked.** Some users already have our app's Privy embedded EOA as an owner (from earlier direct flows); the install CTA must be gated on an on-chain `CoinbaseSmartWallet.isOwnerAddress(embeddedEoa)` check against `profiles.csw_address`, not on "Zora linked + CSW known."
- **Signer-gate consumers must source live Privy wallets from `useWallets()`, not from `extractPrivyWalletsFromUser(privyUser)`.** The `privyUser` object only carries `{ address, walletClientType }` metadata; the runtime provider state that the `privyEmbeddedEoaCanSign` / `canonicalSignerGate` logic needs (e.g. to know whether the embedded EOA is currently active and can actually sign) only surfaces through the `useWallets()` React hook. `frontend/src/pages/Swap.tsx` was the outlier — it derived the embedded-EOA signer from the metadata-only extraction and got stuck in `embedded-wallet-cannot-sign` even when the embedded EOA was already a confirmed owner of the canonical CSW — and has been switched to `useWallets()` as primary source (metadata extraction kept as fallback). `DeployVault.tsx` and `LaunchCoinCard.tsx` already followed this pattern. Any new page that gates on "can the embedded EOA sign right now?" must read from `useWallets()`, expose a recovery CTA that calls `setActiveWallet(embeddedWallet)`, and treat the metadata extraction as a snapshot-only fallback.
- **`zora-enable/` hosts single-file static HTML artifacts uploaded as Zora content coins (e.g. `agent install`, `feedback`, `signal`, `probe`).** These render inside an iframe on `https://magic.decentralized-content.com/ipfs/<cid>` **without** the `allow-same-origin` sandbox flag, which means: `localStorage` / `sessionStorage` / `document.cookie` all throw, `parent`/`top` are `SecurityError`, the Privy React SDK cannot run (it needs same-origin), WalletConnect's broadcast-channel plumbing frequently fails, and Cloudflare Insights/beacon scripts are blocked by Zora's `script-src-elem` CSP. Only EIP-6963 injected providers (Rabby, MetaMask, Coinbase Wallet extension, Brave) are reachable from inside. Zora's "Link" field on a content coin only accepts a fixed platform list: X, YouTube, TikTok, GitHub (gists work), Substack, Bluesky, Instagram, Farcaster, Wikipedia, Zora, Truth Social, Are.na, arXiv. Thumbnails are generated per-folder via `make-thumbnail.py`; `.glb` uploads require a separate raster thumbnail (Zora rejects `.glb` as its own thumbnail). Keep these artifacts sandbox-compatible — do not add dependencies that assume same-origin, React, Vite, or Privy. Because the sandbox blocks Privy/WalletConnect, the agent-install signing flow itself cannot live inside the Zora artifact — the content coin must only advertise + deep-link into the main `app.4626.fun` install page, which owns the actual `addOwnerAddress` transaction.
- **`indexer/` is the `@4626/zora-csw-indexer` standalone service** that powers targeted agent-install outreach. It scans `ZoraSmartWalletCreated` events on `ZoraAccountManager` (`0x0Ba958A449701907302e28F5955fa9d16dDC45c3` on Base), upserts into Supabase `zora_csw_owners` (`csw_address` PK, `initial_owners`, `current_owners` re-read via `ownerAtIndex(i)` since owners can drift after `addOwnerAddress`), and classifies each owner EOA into `zora_csw_owner_class` via `src/classifyOwners.ts` using a nonce heuristic: `base_nonce == 0 AND mainnet_nonce == 0 → likely_privy_embedded`, otherwise `likely_extension_eoa`. `src/crossReferenceFarcaster.ts` joins each EOA with Neynar verified-addresses to populate `farcaster_fid` / `farcaster_username`. Both tables are RLS-enabled, service-role-only; the indexer owns the sole write path. Prefer classifying top-N-by-`mainnet_nonce` extension owners first when running campaigns. `mainnet.base.org` caps `eth_getLogs` at 10k blocks and rate-limits enrichment — use a paid `BASE_RPC_URL` (Alchemy/QuickNode/matrixed.link). Entry points: `pnpm poc` (recent 100 CSWs), `pnpm enrich` (refresh `current_owners`), plus classify/cross-reference runners in `src/`. Do not reintroduce a Privy-based signing flow inside the Zora content coin — the indexer exists specifically because the sandbox path was abandoned; see `README.md` and `docs/design/sub-account-lifecycle-spec.md`.
- **`zora_csw_owners` dominates Supabase load (~1.5M rows / ~1.5 GB).** Seq-scan-heavy jobs are the main risk; runbook: `docs/operations/supabase-zora-db-optimization.md`. Maintenance migration: `supabase/migrations/20260518220000_zora_table_maintenance.sql` (`lower(csw_address)` index, outreach partial index, weekly `pg_cron` `VACUUM ANALYZE`). `indexer/src/classifyOwners.ts` defaults to `CLASSIFY_MAX_ENRICHED_ROWS=25000` plus optional `CLASSIFY_MIN_CREATION_BLOCK` — use `CLASSIFY_UNLIMITED=1` only for intentional full passes. `frontend/scripts/ethos-zora-backfill.ts` uses `pg_class` row estimates for progress (no `COUNT(*)`).
- **Zora CSW owner Ethos scores are cached on `zora_csw_owner_class`.** The live schema and repo migrations add `ethos_userkey`, `ethos_score`, `ethos_level`, and `ethos_score_updated_at`; `/api/v1/zora-csw/enrich-cron` refreshes owner scores through `refreshZoraOwnerEthosScores` with `INDEXER_ETHOS_ENRICH_BUDGET` (default 250, `0` disables). Keep this as a latest-score cache for app/dashboard reads; Ethos itself exposes `/score/history` for score timelines and `/score/updates` for change-driven refreshes, so only add a local append-only history table if product needs independent audit snapshots.
- **`/explore/creators` Ethos is creator-coin scoped, not `zora_csw_owner_class` UI.** `zora_csw_owner_class` is an EOA/CSW-owner indexer grain (~1.5M rows); Explore lists Zora creator coins and should read Ethos via `creator_ethos_projection` (refreshed from owner scores + profile linkage) with `/api/zora/explore` attaching `ethosScore` on every list mode — including `marketCapDelta24h` on the Ethos-sorted path (`buildEthosSortedCreatorList` + Zora coin enrichment). Do not surface raw `zora_csw_owner_class` in Explore UI or wire the browser to that table; UI should trust server-returned `ethosScore` without requiring Twitter `ethos_userkey` on the client. Ethos sort must dedupe by creator identity (handle/username → address) in SQL and client infinite-scroll flattening — one Zora handle can map to multiple wallet rows. On the projection fast path, skip per-row Zora `getCoin` enrichment and client profile/Ethos hydration when the server already returns scores. `/explore` should mount the same wallet/XMTP account chrome as other app routes; list tabs share **`ExploreListLayout`** (static hero title/subtitle/metrics + `ExploreTabNav`; only `<Outlet>` table content swaps); reset scroll to top on tab change; hide single-option period selectors; prefer working Ethos high-to-low sort over filter sliders. **Explore-first backfill:** prioritize DB-only `refreshCreatorEthosProjection` via crons `/api/v1/chat/ethos-sync` + `ethos-sync-hot` before burning Ethos API quota on full CSW-index passes; for catch-up use `frontend/scripts/ethos-creator-wallet-backfill.ts` (top `creator_coins` by volume on Base — set `ETHOS_CREATOR_WALLET_BACKFILL_CHAIN_ID=8453`, resume cursor `ethos_creator_wallet_backfill_v1`, default `ETHOS_CREATOR_WALLET_BACKFILL_REFRESH_EACH_BATCH=0` so projection refresh runs once at end) rather than full `ethos-zora-backfill.ts`; social path stays `ethos-creator-social-backfill.ts` after `zora_profiles` Twitter handles exist; tune `ETHOS_CREATOR_PROJECTION_LIMIT`, `ETHOS_CREATOR_PROJECTION_LIMIT_HOT`, `INDEXER_ETHOS_ENRICH_BUDGET`. Runbook: `docs/operations/ethos-canonical-score-cache.md`.
- **Explore hero metrics SSoT is Supabase-only at read time.** `GET /api/zora/metrics?scope=creators` aggregates indexed `creators` count and Base (8453) `creator_coins` sums for market cap, 24h volume, and 24h fees; `history30d` comes only from `creator_metrics_daily_snapshots`. Zora sync crons write into those tables — the dashboard must not re-fetch live Zora Explore samples or `max(Supabase, Zora)` blend at request time; do not reintroduce `zoraExploreFinancials`. When `exact: false`, UI copy must say indexed totals only (not "live vol/fees from Zora"). **Dual backfill tracks:** fast Zora explore paging via `runCreatorMetricsExploreBackfill` (`pnpm -C frontend exec tsx scripts/run-creator-metrics-backfill.ts --explore-backfill` or `GET /api/sync-creator-metrics?mode=explore`) sets `exploreBackfillComplete` and refreshes `creator_ethos_projection` for newly indexed creators; on-chain scan (`--fast` / default) sets `backfillComplete` — run explore first for hero financials + Ethos sort, then chain scan for full `exact: true`. For heavy archive/on-chain backfill scans, set `CREATOR_METRICS_ARCHIVE_RPC_URL` to a dedicated paid Base RPC (`creatorMetricsSync.ts` uses it when it differs from `BASE_RPC_URL`). **In-app transparency:** `/explore` hero uses `ExploreAnalyticsSyncBadge` + `ExploreAnalyticsMethodology` (`frontend/src/components/explore/`, `frontend/src/lib/explore/analyticsLinks.ts`); user docs `docs/users/explore-analytics.md`, ops `docs/operations/explore-metrics-operations.md`; optional public **Verify on Dune** link via `VITE_DUNE_DASHBOARD_URL` only (never put API keys in `VITE_*`).
- **Dune analytics is server-only.** `DUNE_API_KEY` lives on Vercel/server env only (`frontend/server/_lib/dune/duneApi.ts`); browser calls `GET /api/analytics/dune?metric=…` or `?probe=1` (`frontend/api/_handlers/analytics/_dune.ts`). SQL templates: `frontend/server/_lib/dune/duneMetricSql.ts`. Probe: `pnpm -C frontend exec tsx scripts/ops/dune-probe.ts`. Runbook: `docs/operations/dune-analytics-runbook.md`. Internal ops mirror: Looker connector `indexer/scripts/lookerStudioConnector/README.md` + `docs/operations/looker-studio-widget-recipe.md`. Integrator token manifest stub: `/data/explore-assets-manifest.json`.
- **Explore 30D sparklines:** `resolveCoinPriceSparkline` reads Uniswap V4 subgraph (`PoolDayData`) first with Zora swap fallback; `/api/zora/explore` inline-hydrates missing sparklines so first paint can skip a second `/api/zora/exploreSparklines` round-trip; hot-sync cron precompute is gated by `CREATOR_METRICS_SPARKLINE_PRECOMPUTE_ENABLED` / `_BUDGET` / `_CONCURRENCY` on Vercel.
- **Portfolio holdings should dedupe when the connected external EOA equals the canonical CSW** and should list full onchain token balances alongside Zora coins, not Zora-only rows duplicated across wallet lanes.
- **Scheduled `zora_profiles` refresh keeps Looker/analytics snapshots current.** `GET|POST /api/v1/zora-profiles/refresh-cron` (Vercel cron `15 */6 * * *`) upserts cached market/volume/holder fields from the Zora explore API, runs bounded wallet enrichment, and reconciles `is_in_csw_index`; gate with `ZORA_PROFILES_REFRESH_ENABLED=1` on production. Monitor via `v_zora_profiles_refresh_freshness`; runbook: `docs/operations/zora-profiles-refresh-runbook.md`.
- **Production onboarding owner-preview depends on `PRIVY_WALLET_POLICY_ID`.** `createAgentWallet` in `frontend/server/_lib/wallet/privyWalletApi.ts` fail-closes in production when this env var is missing, which causes `/api/onboarding/preview-agent-owner` to error even before owner/install checks. Keep `PRIVY_WALLET_POLICY_ID` set on the live `4626-keepr-agent` service whenever rotating Privy config.
- **Privy wallet lanes and embedded-EOA resolution.** Dashboard labels: key quorum → **4626 Server Agent Owner** (`PRIVY_WALLET_OWNER_ID`), policy → **4626 Agent Wallet Policy** (`PRIVY_WALLET_POLICY_ID`), server agent wallet → **4626 Agent Signer (server)** (never `4626.base.eth`), XMTP wallet → **4626 XMTP Agent Signer** — runbook: `docs/operations/privy-wallet-lanes.md`, rename script: `frontend/scripts/ops/rename-privy-wallet-labels.ts`. Owner-install / add-owner resolution (`extractPrivyEmbeddedEoaAddress`, `getPrivyEmbeddedEOA`, client `embeddedWallet.ts` / `privyEmbeddedEoa.ts`) must exclude server-managed wallets (non-empty `policy_ids` or `owner_id === PRIVY_WALLET_OWNER_ID`), prefer nested login embedded EOAs over Privy smart-wallet counterfactuals, reject CSW/sub-account/smart-wallet as `addOwnerAddress` targets, and when multiple login embedded EOAs exist pick the most recently used by Privy recency fields. Login embedded EOAs stay policy-free; server agent wallets are automation-only.
- **AlfaClub live auth uses a separate Privy app from 4626.** Orphan key quorums/wallets left in the 4626 Privy app from experiments are not wired to production AlfaClub chat; Privy API deletion of those quorums requires that quorum's original authorization key (not `PRIVY_WALLET_AUTHORIZATION_KEY` for the server agent owner).
- **`/api/onboarding/preview-agent-owner` now has a CSW-self proof path via ERC-1271.** When `connectedEoa`/`connectedAddress` equals `cswAddress`, callers can provide `ownershipProof { issuedAtMs, message, signature }`; the handler enforces a short freshness window and validates `isValidSignature` (`0x1626ba7e`) against a deterministic message template. The prior EOA-owner path remains unchanged.
- `docs/operations/coinbase-smart-wallet-capabilities.md` is the workspace reference for Coinbase/Base Account SDK capability snapshots; the SDK 2.5.1 E2E pass recorded sub-account add/get, `eth_signTypedData_v4`, and `spendPermission.fetchPermissions()` as working while `personal_sign` may time out in that demo environment.
- **The Zora outreach pipeline in `indexer/` now includes name/profile enrichment and export scripts.** `pnpm -C indexer names` resolves Basename/ENS into `zora_csw_owner_class`, `pnpm -C indexer zora-profiles` adds Zora handles/display names/creator-coin addresses, and `pnpm -C indexer export:outreach` emits ranked triple-signal CSV/JSON cohorts under `indexer/exports/` for outreach planning. The operator dashboard surface is Google Sheets driven by `indexer/scripts/sheetsOutreachSetup.gs.js` (Google Apps Script) — Sheets is the preferred operating surface over Looker Studio, with Notion sync as an optional destination rather than a replacement.
- **Zora Explore list data does not expose canonical ERC-20 ticker/name fields for creator coins.** In `indexer` workflows, `name`/`symbol` from list responses can mirror creator handles (for example `jessepollak`) instead of on-chain token metadata (for example `$jesse`), so ticker/name truth must come from on-chain `name()`/`symbol()` reads (`fix-profile-metadata`) before outreach ranking/export.
- **Base App in-app browsers can suppress native `window.confirm(...)` dialogs, which makes critical owner actions appear as no-ops.** For account-setup owner flows (notably `Add co-owner`), desktop-only confirm gates are safer than universal confirms; in-app/mobile paths should execute directly and surface explicit inline errors instead.
- **`isBaseAppInAppContext` must not false-positive on desktop Chrome/Safari with the Coinbase Wallet extension.** In `frontend/src/lib/wallet/inAppBrowser.ts`, do not treat bare `hasCoinbaseInjectedProvider()` or a generic `coinbase` user-agent substring as Base App — that blocked external-browser owner-install lanes while users were already in a real browser. Restrict to webview/mobile Base App signals (`isToshi`/`isBaseApp`, `cbios`/`cbandroid`/`baseapp`, `wv` + Android/Coinbase in-app); tests live in `inAppBrowser.test.ts`.
- **AlfaClub chat bridge supports runtime JWT rotation without process restart.** `alfaclub_runtime_secret` stores short-lived JWT state, admin route `POST/GET/DELETE /api/v1/alfaclub/chat-token` manages rotation metadata, and chat bridge ticks prefer DB token with fallback to `ALFACLUB_CHAT_JWT`. Privy refresh tokens are single-use: once a refresh succeeds, trust the newly rotated DB values; if a later refresh fails, mint a fresh browser triplet instead of reseeding stale clipboard tokens over the live DB row. **The Vercel cron `/api/v1/alfaclub/chat-token-refresh` is the single canonical rotation owner** — the Railway in-process refresher stays disabled (`ALFACLUB_CHAT_PRIVY_REFRESHER_ENABLED` off) so two runtimes never race over the single-use refresh token. Supabase is storage only; the actual refresh is an HTTP call to Privy's sessions endpoint (`privyTokenRefresher.ts`). Ops repair is `pnpm -C frontend ops:alfaclub:repair-chat-token ./triplet.json`, which seeds via `CRON_SECRET`, runs refresh + bridge checks, and deletes the local triplet by default; production smoke should fail on disabled bridge, `success=false`, or a newer refresh failure than success.
- **AlfaClub counter-trade runner enforces one executor and one active strategy actor per room.** The single executor is now the dedicated Railway project **`4626-inverseakita`** / service **`4626-inverseakita-agent`** (Hermit Docker image via `railway.hermit.toml`, `/data` volume with `ARENA_ACP_HOME=/data/acp-home`); it runs **only** the counter-trade ticker — chat bridge, Privy refresher, and Telegram relay are disabled via env gates — while Hermit keeps the chat bridge/refresher/creative lanes with `ALFACLUB_COUNTER_TRADE_ENABLED=0`. Duplicated/triplicated room posts mean a stale container or a second executor, not a message bug; verify the live image carries the latest code (posts include `Signal <fillAction>`). An optional **Eliza LLM risk-review gate** (`counterTradeLlmAdvisor.ts`) sits between the deterministic decision and execution — `ALFACLUB_COUNTER_TRADE_LLM_MODE=gate` makes vetoes/downsizing live; runbook `docs/operations/alfaclub-counter-trade-production-runbook.md`. Room 1659 allows a single active opt-in; extra active opt-ins are auto-paused, normalize via `pnpm -C frontend ops:counter-trade:normalize-optins -- --room 1659 --apply`. Fill classification in `counterTradeEngine.ts` is action-aware (**open / add / reduce / close** — no synthetic `flip`). DGCLAW lane: `DGCLAW_API_KEY` gates only `dgclaw.sh` leaderboard/forum commands (trading signs directly via the ACP agent wallet) and must be a Railway service variable — the container's `/app/dgclaw-skill/.env` is wiped on redeploy; dgclaw-skill v2 uses flag-style `trade.ts` args run with `tsx`, and ACP session state must live on the persistent volume `HOME`. **ACP refresh tokens are single-use:** running `acp configure` (e.g. during `/arena agent create` session rotation) from a previously captured `ACP_ACCESS_TOKEN`/`ACP_REFRESH_TOKEN` env triplet overwrites the working volume session with consumed tokens — subsequent boots then fail `whoami: Session expired` / `NOT_AUTHENTICATED` until a fresh browser `acp configure` is run as the owning wallet, the Railway `ACP_*` vars are updated, and the stale triplet is cleared so reboot cannot re-poison the session. This is now guarded in code: `acpAuthBootstrap.ts` persists a fingerprint of the consumed triplet (`consumed-seed.json` on the ACP volume HOME) and refuses re-seeding (`seed_already_consumed`); the `/arena` agent-create rotation skips configure with an `acp_session_rotation_skipped_consumed_seed` audit event — only a genuinely fresh token pair re-seeds. `acp agent create` takes `--signer`, not `--owner`.
- **InverseAKITA ACP identity and signer provisioning.** The counter-trade ACP agent is **InverseAKITA** (`019e90fa-3c8c-7ba0-8547-bf6f81698c3d`, agent wallet `0x74ab91cd845ff0d2006404440af49c3bc8c1df96`), owned by the user's **personal** Virtuals account `0x64c3fb828bd2a8cde9cde14d0295d34916bb94e9` by explicit user choice — not the hermit4626 identity. Fresh-volume signer setup: run `acp agent add-signer --agent-id <id> --policy restricted --no-wait --json` inside the container with `HOME` set to the ACP state dir, have the owner wallet approve the returned `signerUrl` within ~5 minutes, then finalize with `acp agent signer-status`; bootstrap reads signer readiness from the `publicKey` in the volume `config.json`, and on fresh volumes auto-pins the keyring file backend. Railway gotcha: GraphQL `variableUpsert` does not reliably restart the running process — a redeploy can snapshot env **before** the upsert lands, leaving a live process on stale values (`railway ssh` shows the new value while the process keeps the old one); force `serviceInstanceRedeploy` after variable changes and verify runtime behavior, not just stored vars.
- **The Virtuals ACP bridge is a separate Eliza plugin lane from the `/arena` dgclaw trading lane.** `frontend/server/agents/eliza/plugins/virtuals/` (`VirtualsAcpService`) drives the app.virtuals.io ACP agent via the `@virtuals-protocol/acp-node-v2` event-driven SDK: per job-room entry it asks the Eliza LLM for a one-line JSON tool decision (respond/negotiate/deliver/complete/reject). Safety rails are in code, not the prompt: budgets clamp to `VIRTUALS_ACP_MAX_BUDGET_USDC` (default 5), the `fund` tool is hidden unless `VIRTUALS_ACP_AUTO_FUND=1`, and `VIRTUALS_ACP_AUTO_LLM=0` is observe-only. Run standalone with `pnpm -C frontend agent:virtuals` (never on the Railway Keepr primary); enable via `VIRTUALS_ACP_ENABLED=1` + agent wallet/Privy-wallet-id/session-signer env. Runbook: `docs/operations/virtuals-acp-bridge.md`. The Arena degen.virtuals.io trading agent stays on `/arena` dgclaw + acp-cli.
- **The AlfaClub ERC-8004 self-feedback lane is intentionally disabled in production.** `ALFACLUB_VIGILANTE_FEEDBACK_ENABLED` is removed from Vercel env, so the daily `/api/v1/alfaclub/run` cron keeps indexing/ranking and publishing Lens scorecards but no longer calls `giveFeedback` on agent 2205. When it was on, feedback txs were signed by the dedicated sidekick EOA (`ALFACLUB_VIGILANTE_SIGNER_PRIVATE_KEY`), not the canonical CSW agent identity. Do not re-enable without a product decision. The 67 historical self-feedback entries on agent 2205 were revoked on-chain (June 2026) via `frontend/scripts/ops/revoke-alfaclub-self-feedback.ts` (dry-run by default; execute requires `--confirm=REVOKE-SELF-FEEDBACK`; `revokeFeedback` is reviewer-scoped, so the original sidekick EOA must sign).
- **AlfaClub chat API calls may route through the optional `alfaclub/infra/cloudflare-proxy/` relay.** When the proxy is configured, `chatBridge` must keep the AlfaClub browser-fingerprint headers derived from the upstream API origin and send the proxy shared secret only to the relay.
- **`alfaclub/infra/cloudflare-proxy` must treat `GET`, `HEAD`, and `OPTIONS` as bodyless methods.** Do not forward `req.body` when constructing upstream requests for those methods; `HEAD` and CORS preflight paths can fail if they accidentally carry a body.
- **AlfaClub websocket sends may route through the optional WS proxy lane.** `chatBridge` uses `ALFACLUB_WS_PROXY_URL`, `ALFACLUB_WS_PROXY_HTTP_SEND_URL`, and the matching proxy secret only when configured; the direct AlfaClub socket path remains the default when those env vars are unset.
- **AlfaClub chat bridge treats Cloudflare challenge failures separately from JWT/auth failures.** Cloudflare blocks should update the Cloudflare health/backoff path without marking the JWT as bad, triggering immediate Privy refresh, or opening the live-socket auth fallback in the same tick.
- **AlfaClub bridge 401s can be proxy-secret drift, not token failure.** If a browser `room_history_paginate` request succeeds but `chat-bridge-run` returns upstream `401` through the Worker relay, check that Worker `PROXY_SHARED_SECRET` and Vercel `ALFACLUB_CHAT_API_PROXY_SECRET` are aligned, then redeploy the Vercel app before rotating AlfaClub auth again.
- **AlfaClub bot replies failing with `bot_message_failed:401 invalid or revoked token` means `ALFACLUB_API_KEY` was revoked** — commands still execute; only the reply lane dies. Rotate the key on both Railway and Vercel production (force a prod rebuild with a `[force-vercel]` commit if needed). The websocket fallback send can log success without delivering when the server kicks sockets shortly after open, so don't trust WS "sent" logs as delivery proof. Quick validity probe: a bogus-room API call returning 403 = valid key, 401 = revoked.
- **AlfaClub live-room ingest now lands in `alfaclub.chat_ingest` (schema `alfaclub`, not `public`).** `chatBridge` ingests incoming WS room messages into this table via `chatIngestStore`, and schema bootstrap includes a one-time copy path from legacy `public.alfaclub_chat_ingest`.
- **Arena room identity resolution requires `alfaclub.arena_identity_mapping` in Supabase.** If that table/migration is missing or DB reads fail, `/arena identity show` falls back to `source: env_default` even after `/arena register default ...`. After applying `20260708000000_alfaclub_arena_identity_mappings.sql`, room `1659` resolves as `source: room_default`; `join` diagnostics can still fail independently without blocking mapping reads.
- **AlfaClub `/arena bridge` moves Base spot USDC to Hyperliquid perp via ACP.** Command flow: `acp client create-job … perp_deposit` then immediate `acp client fund`; on settlement posts in-room and auto-sweeps bridged spot to perp. Preflight detects stale ACP sessions and returns explicit re-auth guidance (`acp configure` in the runtime `ARENA_ACP_HOME` volume) instead of a generic bridge failure.
- **AlfaClub command intake normalizes bare `arena` commands to slash commands.** `chatBridge` rewrites `arena ...` to `/arena ...` before routing, so users can run `arena auth` / `arena bridge <amount>` without a leading slash.
- **Hermit is the AlfaChat creative/meme lane, distinct from Keepr-critical agent paths.** Keep Hermit flows scoped to creative/meme generation and Pinata/IPFS publishing; do not mix Hermit command behavior into Keepr, deploy, wallet, or other critical automation surfaces.
- **Hermit meme assets should publish through Pinata/IPFS for production.** Do not rely on local `frontend/public` assets as the durable production source. Public Hermit URLs should use `https://4626.fun/ipfs/<cid>`; Vercel rewrites that path through the Pinata gateway origin `https://pinata.4626.fun/ipfs/<cid>`. Public gateway reads do not require gateway keys or IP allowlists.
- **Hermit creative draft routing is now first-party and separate from the Railway Hermit worker.** Creative `/hermit` / `/meme` / `/gmeow` drafts route through `frontend/server/_lib/hermit/skillRouter.ts`, which resolves typed per-route policy in `creativePolicy.ts` (route tier, timeout, token/retry budgets, model hints) before calling `HERMIT_AGENT_CHAT_ENDPOINT` + `HERMIT_AGENT_BEARER_TOKEN` (defaulting to Vercel `POST /api/hermit/draft`); lane ownership matrix: `docs/operations/agent-lane-policy-matrix.md`. Keep Hermit creative generation separate from Eliza auth/token-refresh control plane. Railway `frontend/server/agents/hermit/` remains a different runtime. `/api/hermit/draft` is provider-aware via `HERMIT_AGENT_PROVIDER`: `gateway` (default, `HERMIT_AGENT_MODEL` → Vercel AI Gateway) vs `openai-compatible`/`openrouter` (builds `@ai-sdk/openai-compatible` with `HERMIT_AGENT_BASE_URL` default `https://openrouter.ai/api/v1` + `HERMIT_AGENT_API_KEY` falling back to `OPENROUTER_API_KEY`; **fails closed 503** without a key). **Hermes (`nousresearch/hermes-4-70b`/`405b`) runs on OpenRouter — it is not in the Vercel AI Gateway catalog.** Because Hermes 4 is hybrid-reasoning, the compatible path strips `<think>`/`<reasoning>` blocks (`stripReasoningArtifacts`), prepends a `HERMES_OUTPUT_GUARD` system directive (gateway/GPT path unchanged), validates HTTPS base URL (503), and classifies upstream errors (timeout→504, rate-limit→429, else 502). Keep this lane isolated to creative commands and use the AlfaClub X-first toggle (`HERMIT_ALFACLUB_POST_X_FIRST`) when room rendering should be tweet-card driven.
- **AlfaClub `/help` in Hermit command rooms is room-aware Hermit catalog, not Keepr help.** `resolveAlfaClubHelpText` (`alfaclubChatHelp.ts`) routes rooms in `ALFACLUB_HERMIT_COMMAND_ROOMS` to `formatHermitCommandRoomHelp` (`hermitAlfaClubHelp.ts`); `/hermit help` in those rooms returns the same body. `chatBridge.ts` `truncateAlfaClubBotMessage` hard-caps bot posts at **2,000 chars** — keep help under `HERMIT_COMMAND_ROOM_HELP_MAX_CHARS` (tests in `help.test.ts`).
- **Hermit room welcome is one-time per (room, wallet).** `alfaclub.room_welcome_sent` (`20260602000000_alfaclub_room_welcome.sql`) dedupes welcomes in `ALFACLUB_HERMIT_COMMAND_ROOMS`; fires on a wallet’s **first ingested message** (threaded reply) or successful `POST /api/v1/alfaclub/room-access/join` (standalone post). Default on via `HERMIT_ROOM_WELCOME_ENABLED` (`0`/`false`/`off` disables). Ops: `frontend/scripts/ops/hermit-room-welcome-probe.ts` (`--claim`, `--send`, `--reset --confirm=RESET`).
- **Hermit first-time onboarding nudge** (`HERMIT_ONBOARDING_NUDGE` in `skillRouter.ts`) appends the dialect/`/hermit setup` footer on the first successful creative reply until `hermit.onboarded` is persisted for that wallet/room.
- **Hermit AlfaClub optional X cross-post** (`HERMIT_ALFACLUB_POST_X_FIRST`) posts to X first, then sends the tweet URL in-room for card rendering; when the @4626fun app lacks write permission, append `formatHermitXCrossPostSkipMessage` in-room instead of failing the creative reply.
- **Railway Hermit liveness probes `/healthz`, not readiness.** Bootstrap (`frontend/server/agents/hermit/bootstrap.ts`) must return **200 on `/healthz`** during module load (Railway treats non-2xx as down); keep **`/readyz` at 503** until `index.ts` is up. `healthHandoff.ts` closes the bootstrap listener before the real health server binds — double-bind causes `EADDRINUSE` and stuck 503 forever. Deploy via `railway.hermit.toml` → `frontend/Dockerfile.hermit`. **Railway CLI auth:** `RAILWAY_TOKEN` must be an account API token from [railway.com/account/tokens](https://railway.com/account/tokens) (long string), not a 36-char project/service UUID; on WSL when `railway login` cannot open a browser, use `railway login --browserless`.
- **Agent/Hermit Docker build context:** `frontend/Dockerfile.agent` runner must COPY `frontend/shared` (`site-config.json` — imported by `agentRegistration.ts`); `4626-keepr-agent` crashes without it. `frontend/Dockerfile.hermit` deps stage must COPY `frontend/server` plus `src/config`, `src/lib`, and `src/wallet` **before** `packages/server-core` `build.mjs` (re-export scan needs those sources).
- **Pinata Hermit draft on `*.agents.pinata.cloud` often 404s on HTTP and must fall back to gateway WS (never abort on HTTP miss).** Keep `runPinataDraft` tolerant of HTTP failures for this host family and continue through the WebSocket draft path.
- **Hermit prod env on Vercel:** `vercel env pull` can show empty strings for encrypted production vars even when keys exist — verify `HERMIT_AGENT_CHAT_ENDPOINT`, `HERMIT_AGENT_BEARER_TOKEN`, `HERMIT_AGENT_PROVIDER`, and `ALFACLUB_HERMIT_COMMAND_ROOMS` with `vercel env run -e production`; if empty, remove and re-add via `vercel env add`, then production redeploy. E2E audit: `bash frontend/scripts/ops/audit-hermit-e2e.sh` (optional `--production-env --strict`).
- **`hermit.4626.fun` Telegram ingress must hit Vercel, not Railway.** `TELEGRAM_TO_ALFACLUB_INGRESS_HOST=hermit.4626.fun` is correct (hostname only); `POST /api/telegram/webhook` is served by Vercel project `akita-llc/4626`. If DNS points at Railway (`x-railway-edge`), Telegram gets 404 and Hermit relay never runs — add the subdomain on Vercel and point DNS there; keep Railway Hermit health on `*.up.railway.app`. The Hermit Telegram bot is **`@hermit4626bot`** with its webhook on `https://app.4626.fun/api/telegram/hermit-webhook` (aligned via `frontend/scripts/ops/setup-hermit-telegram-webhook.ts`, group privacy OFF), and its bot token env is **`HERMIT_TELEGRAM_BOT_TOKEN`** — renamed from `ALFACLUB_TELEGRAM_BOT_TOKEN`; do not reintroduce the old name. The Telegram Mini App link entry lives on `app.4626.fun/telegram/link`.
- **`chatBridge.ts` must lazy-load `executeDeterministicCommand`.** Eager import creates a circular dependency (`chatBridge` → `execute` → `alfaclub` → `chatBridge`) that crashes `/api/v1/alfaclub/chat-bridge-run` at module load (500) and silences `/alfa` in Hermit rooms like **1659** even when room env is correct.
- **Hermit AlfaClub room 1659 market context:** `room1659Market.ts` injects Hyperliquid snapshot data into Hermit command context; multi-room Hermit routing uses `ALFACLUB_HERMIT_COMMAND_ROOMS` (comma list, e.g. `1043,1659`) in `chatBridge.ts` — runbook `docs/design/room-1659-hermit-market-awareness.md`. **The HL snapshot must read the dedicated room portfolio wallet (the room's `walletAddress`), not the requesting sender** — `resolveRoom1659HyperliquidUserForSnapshot()` ignores `senderAddress` and delegates to `resolveRoom1659HyperliquidPortfolioUser()`. Room 1659 is a `one-to-many` Trading room, so sender-wallet snapshots show empty positions for almost every caller ("wrong address" / no-open-position regression); keep `/halp`, `/hermit`, and market-pulse pinned to the portfolio wallet.
- **`/positions` is a social-signal trading surface** that overlays AlfaClub room chat (e.g. room 1659, via the AlfaClub read API) onto a Hyperliquid-style candlestick chart with per-market position events. Page `frontend/src/pages/Positions.tsx` + `frontend/src/components/positions/*` (`PositionsChartSurface`, `PositionsEventInspector`, `PositionsEventLegend`, `PositionsMarketRail`, `PositionsMarketSignal`, `types.ts`); server data via `frontend/server/_lib/alfaclub/roomTimeline.ts` + `hyperliquid.ts` + `roomCharts.ts` + `positionReport.ts`; API `GET /api/v1/alfaclub/room-timeline` (`_handlers/v1/alfaclub/_room-timeline.ts`); surfaced on both `4626.fun/positions` and `app.4626.fun/positions`. **Markets must stay decoupled per pair** — never combine events across markets (e.g. BTC/USDC vs HYPE/USDC), and the selected market must drive the candles shown. Use **only real room/trade data — never fabricate events**, prices, or P/L.
- **Pinata Hermit stuck on `starting` with `Failed to load agent model` is a model/API-key boot failure**, not Intercom `user_hash`, Stripe CORS, or wallet-extension noise. `pinata agents config set` returns 500 while the container crash-loops — fix model in the Pinata UI or recreate the agent. Current production agent id is `x6bk3ima` (replaced `x7lmjaxx`, which had replaced stuck `xno7wq81`); stable boot model is `openai/gpt-4.1-mini` until the target slug is confirmed available in that runtime. Pinata rejects attaching both `OPENAI_API_KEY` and `OPENAI_OAUTH_TOKEN` on one agent (409) — detach one before attaching the other; prefer OAuth when billing should use ChatGPT Plus/Pro subscription rather than platform API pay-as-you-go. Rebuild runbook: `docs/operations/hermit-pinata-agent-rebuild.md`.
- **Pinata CLI auth in this workspace is TTY-gated.** Non-interactive `pinata auth ...` attempts from agent shells can hang/fail; run interactive `pinata auth` once in a user terminal before CLI automation (`agents list/get/config/channels`) and then proceed with scripted ops.
- **Hermit OpenClaw Telegram hardening baseline:** keep `dmPolicy: pairing`, `groupPolicy: allowlist`, and no wildcard DM `allowFrom` on agent `x6bk3ima`; preserve this posture when applying model/runtime config updates.
- **Telegram link-readiness command surface is `/status` (not `/linked`).** Command registry/webhook/menu callback wiring uses `head: 'status'` and `menu:status`, with no backward-compat alias.
- **`svix` must stay pinned to CJS-compatible `uuid` resolution.** To prevent Vercel runtime failures in `@privy-io/server-auth` (`svix` requiring ESM-only `uuid@14`), keep `svix>uuid` overridden to `10.0.0` in both root and frontend package overrides.
- **Base App owner-action prolinks are centralized and wired across setup surfaces.** `frontend/src/lib/wallet/walletSendCallsPayload.ts` builds EIP-5792 `wallet_sendCalls` v2.0.0 payloads (required `from`, `chainId`, `atomicRequired`, zero-value hex calls); `frontend/src/lib/wallet/cswSendCalls.ts` and `/add` reuse the same helper (do not hand-roll v1.0 payloads). `frontend/src/lib/base/prolink.ts` wraps single-call prolinks for Base mainnet. Owner-add flows in `AdminAgentSetup`, `DeployVault` one-time approval, and waitlist advanced `Add co-owner` use this path, and waitlist prefers backend-prepared `txRequest` payloads when available.
- **Single-agent XMTP runtime now enforces one active DB per env.** In `frontend/server/agents/eliza/index.ts`, `makeDbPath({ enforceSingleFileForEnv: true })` archives stale `xmtp-<env>-*.db3` files and orphaned sidecars (`-wal`, `-shm`, `.sqlcipher_salt`) so startup keeps a single active local database path.
- **Privy XMTP signer must not silently trust stale owner-index hints.** `frontend/server/_lib/wallet/privyXmtpSigner.ts` resolves owner index with `allowConfiguredOwnerIndexFallback: false`; invalid `CANONICAL_CSW_OWNER_INDEX` values are treated as advisory and ignored in `eliza/index.ts` so runtime auto-detects the actual owner slot.
- **DeployVault vanity prefix search is track-specific.** In `frontend/src/pages/deploy/DeployVault.tsx`, vault-prefix-only searches use `VITE_VAULT_VANITY_MAX_TRIES` (default 250,000), while combined vault+share vanity search remains capped for bounded runtime. Rust WASM: root `pnpm build:vanity-wasm` copies `vanity_salt_grinder.wasm` to `frontend/public/vanity/` and `frontend/src/lib/vanity/`; `perVaultVanityWasm.ts` defaults to `/vanity/vanity_salt_grinder.wasm` (no `VITE_VANITY_WASM_URL` unless hosted elsewhere). Restart Vite after adding the WASM to `public/` — otherwise DeployVault logs `Vanity WASM URL is not configured` and falls back to the TypeScript mirror.
- **Known split Phase-1 batchers can reject nonzero salt overrides.** `0xe3F9490CfD6bd3D68010405d18Bf772C167E7178` and `0xF941Bb68e4f083f3F531cc598d5C08d0b8FfbA7E` may revert phase-1 calls with `SaltOverrideDisabled()` (`0xe7fdf838`), so deploy planners/session builders must avoid nonzero share salt overrides. Server-side stale-payload normalization must keep the same `...WithSalt` selector family and zero only the override argument; rewriting to an unsalted selector can hit missing entrypoints on deployed batchers.
- **Current release-target validation lives in `test/current-release-target-guard.sh`.** `test/v183-release-target-guard.sh` is only a deprecated compatibility wrapper; active operator docs/checklists should call the current guard path so release checks validate the live split Phase-1 stack rather than historical v1.8.3 assumptions.
- **Creator-vault batcher config now fail-closes on deprecated aliases.** `normalizeCreatorVaultBatcherAddress` and `isDeprecatedCreatorVaultBatcherAddress` in `frontend/src/config/contracts.defaults.ts` reject `LEGACY_DEPLOYMENT_BATCHER` (`0x56E8527Bf0824155e1556aED5740366f248B68ca`), `MODULE_MISMATCH_DEPLOYMENT_BATCHER` (`0x32403a647e73e04ae42b02bdd1ade9c88698fd0c`), and `PRE_CURRENT_MODULE_DEPLOYMENT_BATCHER` (`0xe3F9490CfD6bd3D68010405d18Bf772C167E7178`); UI and server paths now share `deploymentBatcherNotConfiguredMessage` from `frontend/src/lib/deploy/deploymentBatcherConfigError.ts` (server re-export in `frontend/server/_lib/onchain/deploymentBatcherConfigError.ts`) so stale env values hard-fail and point to canonical `SPLIT_PHASE1_DEPLOYMENT_BATCHER` (`0xa99058f424FB3ACC639F59355C65C40149030651`). Ops scripts (`frontend/scripts/deploy-local-batcher.ts` and `frontend/scripts/ops/propose-batcher-solana-config-safe.ts`) are also wired to reject deprecated-alias env values so the validation boundary covers all call sites.
- **Sourcify can close Basescan CREATE2 verification gaps for immutable-heavy contracts.** For v1.10.1 `DeploymentBatcher.sol` plumbing, Etherscan/Basescan V2 mismatched on `osaka` + `immutableReferences`, but Sourcify returned `runtimeMatch=match` and closed `BASESCAN_VERIFY_PARTIAL_CREATE2`; force the Sourcify path by passing empty Etherscan key env vars because Foundry auto-loads `.env`.
- **v1.10.2 DeploymentBatcher cleanup treats `VaultActivationBatcher` as non-Ownable.** It is permissionless/stateless and has no `owner()` function, so ownership-transfer cleanup covers the four Ownable Phase 1 contracts only; deployment preflights should verify bytecode plus immutable `permit2` / registry references for `VaultActivationBatcher` instead of calling `owner()`.
- **Use `evm_version = "cancun"` for the current Base deployment toolchain unless newer opcodes are explicitly required.** Prior BLS12/EIP-2537 scan hits were false positives from comments or out-of-scope probes, and `hardhat_setCode` / forge-std `blobhashes` are not deployed Solidity bytecode.
- Local dev URL stability expectation: keep app flows on `localhost:5173`/`localhost:5174`; redirects to random localhost ports are treated as regressions to investigate. On WSL2, if `localhost:5174` fails from a Windows browser, try `127.0.0.1:5174` first.
- **Waitlist wagmi mount must defer hook consumers one commit.** `WalletProviders` wraps children in `DeferWagmiConsumers` via `useDeferUntilAfterCommit()` so wagmi `Hydrate` reconnect does not trigger setState-during-render warnings on waitlist setup surfaces.
- **`AddOwnerSigningPanel` must stay lazy-loaded from `AccountSetupWorkspaceView`.** Static imports pull `submitRelayPart1SelfFunded.ts` (~2.3k lines) into the waitlist bootstrap chain (`WaitlistFlow` → setup workspace) and can crash Vite/esbuild on WSL under memory pressure; keep owner-install UI behind `React.lazy` + `Suspense`.
- **Deploy dry-run local forks are shared and stateful.** `pnpm -C frontend run dev:deploy-dry-run` uses a local Anvil fork and `frontend/.env.deploy-dry-run.local`; set `DEPLOY_DRY_RUN_USE_LOCAL_BATCHER=1` when the run should deploy a fresh local batcher onto the fork instead of using the configured/live batcher. Failed or overlapping dry-runs can leave stale listeners, stale env-derived batcher addresses, CREATE2 phase contracts, or dropped impersonation state. Dry-run code should reset/snapshot the fork, reassert impersonation before calls, skip already-complete deterministic phase core only when all expected contracts exist, and fail partial phase state with reset/new-version guidance instead of surfacing raw `No Signer available` or `DeployFailed()` noise. **Phase 2 finalize dry-run prep:** when unauthorized on the fork, impersonate registry owner and call `setAuthorizedFactory(batcher,true)` via `ensureBatcherRegistryAuthorizationOnFork` (`frontend/server/_lib/deploy/ensureBatcherRegistryAuthorization.ts`); attach Pipe A LZ native fee to finalize calls through `attachFinalizeShareBridgeValueToCalls` (same path as live `_continueCore.ts`) — `value: 0 ETH` on finalize is a dry-run bug, not a successful Pipe A quote. **Phase 3 dry-run prep:** auto-authorize `phase3Helper` on the create2 deployer via `ensurePhase3HelperCreate2AuthorizationOnFork` (same pattern as registry auth); stale forks missing helper auth fail Phase 3 with `NotAuthorizedDeployer()` (`0x8164ae93`).
- **Default vanity misses are best-effort again.** In `resolveDeployVanityPlan`, when planner outcome is `missed_defaults`, default targets (`0x4626` vault prefix / `4626` share suffix) now continue with warning text instead of hard-failing; explicit custom vanity requests still fail closed.
- **Deploy dry-run Vite dev requires Node ≥20.19 and WSL-safe watch settings.** Pin `frontend/.nvmrc` to 20.19.x; `scripts/dev-deploy-dry-run.sh` must `cd frontend` before `nvm use`, enforce the engines floor, prefer the `.nvmrc` Node binary directly when installed, and unset `npm_config_prefix` before nvm shell hooks (`pnpm -C frontend` sets prefix to the package dir and can abort nvm). Keep local API dynamic imports out of Vite config deps via `apiImport()` + `/* @vite-ignore */` so editing `api/_handlers/**` does not restart esbuild. Deploy dry-run defaults `VITE_LOW_MEMORY=0` (opt in with `VITE_LOW_MEMORY=1` or `pnpm dev:low-memory` when WSL RAM is tight), sets `VITE_WATCH_POLLING=1`, and `vite.config.ts` ignores `api/`, `server/`, and monorepo siblings from watch to avoid WSL esbuild OOM (`SIGKILL`) and ENOSPC inotify exhaustion. Do not run `pnpm dev` (5173) and deploy-dry-run (5174) concurrently — dual Vite/esbuild workers multiply WSL memory pressure and trigger `write EPIPE` / "The service is no longer running". Pre-commit `lint-staged` ESLint can also fail with a transient "could not open file" WSL filesystem race while the deploy-dry-run dev server is running — re-run the commit (the lint passes on retry); pause the dev server before committing if it recurs. When low-memory mode is on, `vite.config.ts` keeps `alwaysOptimizeInclude` for `cookie`, `eventemitter3`, `ox`/`ox/erc8010`, and `viem`, and `resolveOxModulePlugin` must prefer `ox/_esm` over `_cjs` so named exports like `SignatureErc8010` resolve in the browser. **`localDevEnv.ts` keeps deploy-dry-run `/swap` fast:** `/api/rpc?chain=base` prefers Anvil via `DEPLOY_DRY_RUN_LOCAL_RPC_URL`, skips `DATABASE_URL` unless `DEPLOY_DRY_RUN_KEEP_DB_ENV=1`, and drops slow matrixed.link from dev RPC failover.
- **Base App `/waitlist?setup=base-app` signing errors are pre-signing, not Relay reverts.** `Failed to fetch RPC request` / `An internal error was received` during **Enable 4626 signing** means `wallet_prepareCalls` failed before Coinbase's signing endpoint (`keys.coinbase.com`) — Relay Part 1 never ran and nothing hit chain. Remediation must **stay inside Base App** (force-close, reopen the link, confirm Base Mainnet, rebuild preview, retry once); do **not** tell users to open Chrome/Safari for this path — it contradicts sub-account/app-wallet signing and `SUB_ACCOUNT_WRONG_BROWSER_MESSAGE`. Coinbase Wallet in-app browser is a separate context where external-browser escape may still apply. Base App Relay Part 1 should prepare-first and must not cascade through alternate signing lanes on these RPC failures (avoids repeated passkey prompts).
- **Privy-returned counterfactual smart-wallet addresses must not overwrite `profiles.csw_address`.** The persisted canonical CSW remains the deployed Zora/Coinbase Smart Wallet; bootstrap/wallet-sync paths should preserve it when Privy exposes an undeployed smart-wallet candidate with no Base code, and use Privy wallets (including Zora-app Privy smart-wallet candidates) as signer/sub-account identities rather than promoting them to the asset-holding CSW. All persistence must go through `resolveStoredCanonicalCswAddress` in `frontend/server/_lib/wallet/canonicalCswPersistence.ts` — allowed-owner EOAs and undeployed candidates must never be written as `profiles.csw_address`.
- **Frontend brand assets are centralized in `frontend/public/assets/` with a scripted pipeline.** Committed hand-tuned masters live in `frontend/assets/brand/master/icons/` (restored from `4626-web-starter-v2.zip`); `pnpm generate:brand-icons` (`scripts/generate-brand-icons.mjs`) **syncs** those masters to `public/` — do not Sharp re-render the favicon ladder or overwrite `logo-mark-1024.png` (full-bleed) with the opaque tile. Source of truth for cache busting: `frontend/shared/site-config.json` (`brandAssetVersion`, favicon paths). Also run `pnpm generate:html-shells` and `node scripts/sync-static-favicon-head.mjs` (immersive + static SEO HTML). **v17+ HTML shells emit a single** `<link rel="icon" href="/favicon.ico">` **(no multi-size PNG/apple-touch ladder in head)** so the page does not swap icons after load. App tile / Base launcher icon = **`logo-mark-opaque-1024.png`** (white `4` on rounded black tile); bump bytes or `brandAssetVersion` — path-only `?v=` is not enough for sticky Base App caches. Canonical tab favicon is white **`favicon.ico` / `favicon.svg`** — not `logo-mark-blue.svg`. Avoid **`mask-icon` + `safari-pinned-tab.svg` + `#0000FF`** (reads as a blue-bordered favicon). Base App domain bar often fetches **`/favicon.ico` without `?v=`** and may still hit legacy roots (`/app-icon.png`, `/pwa-512.png`, `/miniapp-icon.png`) — keep real PNGs at those paths (SPA HTML 200 breaks cache refresh). **Domain-bar flicker (old blue squircle → white 4)** is usually **base.dev cached project icon** racing the live site favicon — not repo scripts or Pinata IPFS serving the app mark; live Pinata use is Hermit `/ipfs/*` relay and optional Explore Basescan/Dexscreener link icons only (legacy miniapp tomato CID was removed from git). `/assets/*` is `immutable` for a year; bump `brandAssetVersion` and use new filenames when needed. Base App **launcher/domain chrome** requires **[base.dev](https://base.dev) project icon re-upload** (project `695a49dc4d3a403912ed8ca5`) after deploy — website deploy alone is insufficient. Runbook: `docs/operations/base-app-icon-refresh.md`. Keep `farcaster.json` `homeUrl` on `https://4626.fun/`; verification/app shell assets on `app.4626.fun`.
- **Account tray portfolio uses one batched read path.** `GET /api/wallet/trayPortfolio` (`trayPortfolioResolve.ts`) replaces separate DeBank total + portfolio calls: DeBank lite (`total_balance` + `all_token_list`, 2 calls/wallet) when `DEBANK_ACCESS_KEY` is set, else Base Etherscan v2 fallback (`baseTrayPortfolioEtherscan.ts` + DefiLlama USD); when `preferTrayPortfolioEtherscan()` is on and Etherscan returns an empty shell (0 total, no tokens), fall back to DeBank if configured. Client entry is `fetchAccountTrayPortfolioBatch()` in `ConnectButton`. Surface explicit Base-only copy when on Etherscan fallback.
- **DeployVault session gating must treat the parent-CSW + Privy embedded-owner path as deploy-capable after session restore.** Do not require a separate wagmi smart-wallet connection before showing deploy controls when the canonical CSW and embedded owner signer are already available.
- **DeployVault external signer buttons must use the clicked wallet connector directly.** Named Rabby / MetaMask / Coinbase buttons should request the provider/signer from that connector, switch that provider to Base, and avoid relying only on global wagmi connector state or a stale `useWalletClient` render tick. Rabby/MetaMask provider discovery should prefer targeted EIP-6963 matches and then fall back to legacy globals such as `window.rabby`, because wallet-extension collisions can hide the intended provider from the generic injected connector.
- **Wrong external EOA stuck as MAIN WALLET is a profile sync artifact, not CSW identity.** A one-off wagmi connect can persist `profiles.primary_wallet` / `profile_wallets.is_primary` for an external Rabby/MetaMask EOA while canonical CSW + Privy embedded owner stay correct — use account tray **Disconnect main wallet**, or `pnpm -C frontend exec tsx scripts/fix-stale-external-primary-wallet.ts --email=… --apply` (`disconnectExternalWalletFromProfile`), then reconnect the intended wallet; do not treat the stale EOA as the canonical asset-holding account. When a canonical CSW + embedded signer exist, `walletSync` should keep `primary_wallet` on the embedded EOA via `resolveProfilesPrimaryWalletColumn`, not a transient wagmi extension that `COALESCE` would freeze forever.
- **DeploymentBatcher constructor changes require a complete root call-site sweep.** When changing `DeploymentBatcher` or `DeploymentBatcherPhase2Module` constructor signatures, audit all root `contracts/`, `script/`, and `test/` call sites before patching fixtures; `.worktrees/**` hits belong to separate worktrees and should not be edited from the root checkout.
- **DeploymentBatcher's large constructor is sensitive to viaIR stack pressure.** Keep zero-address checks interleaved immediately before each state assignment and use state-var reads in helper `new` calls; regrouping constructor param checks can recreate the `Variable value4 is 1 too deep in the stack` Yul failure.
- **CREATE2-deployed batcher modules must not rely on `msg.sender` for batcher identity.** `DeploymentBatcherPhase2Module` is deployed by the CREATE2 factory, so constructor-time `msg.sender` is the factory, not `DeploymentBatcher`; pass the intended batcher address explicitly. Its `batcher` guard must stay immutable because the module runs through `delegatecall` and a storage-backed guard reads the outer batcher's storage.
- **`CreatorOVault` greenfield module v2 (`CreatorOVaultModuleStorage.v2`).** New vault deployments can wire fresh modules for `liquiditySnapshot()` (honest `instantIdleBps`), Morpho-style `riskConfigDelay` + `scheduleSetPerformanceFee` / `scheduleSetManagementFee` / `scheduleSetStrategyMaxAssets`, TVL `managementFee` accrual on `report()`, ERC-2612 vault-share `permit`, `valuationMissThreshold` auto-disable, and `migrateStrategy`. Existing mainnet vaults keep prior module wiring until redeploy. Runbook: `docs/operations/creator-oVault-vault-gaps-v2.md`. ERC-7540 async redeem and per-user custom withdraw queues remain deferred. **Phase 1 `InvalidModuleAddress()` (`0x5cfe78fe`)** means stored `CreatorOVault` creation bytecode's `moduleStorageVersion` fingerprint disagrees with the batcher's wired modules — align `DEPLOY_BYTECODE` / store seed with batcher module slots before greenfield Phase 1 core; live batcher `0xa99058…` remains on the `.v2` module fingerprint under the `v1.13.0` release target. **`VITE_DEPLOYMENT_VERSION` is the CREATE2 salt namespace**, separate from the bytecode epoch label (e.g. `v1.13.0-dryrun`). **v1.13.x deploy bytecode:** `CreatorOVaultLiquidityLib.snapshot` is `internal` (inlined — no external library link placeholders in CREATE2 creation code); root `foundry.toml` sets `bytecode_hash = "none"`; regenerate via `./script/generate_bytecode_manifest.sh` + `./script/generate_frontend_deploy_bytecode.sh` (read artifacts from `out/*.json`, not raw `forge inspect` stdout); verify store with `pnpm -C frontend exec tsx scripts/ops/verify-bytecode-store-seeded.ts`.
- **Impairment side-pocket v1 is live in the `CreatorOVault` module stack (`CreatorOVaultModuleStorage.v3`, bumped from v2).** When a strategy is impaired the vault enters `Suspect` mode (`deposit/mint/withdraw/redeem` blocked, `max*` → 0), takes a Merkle snapshot of holders at the trip boundary with a challenge window, finalizes an impairment epoch, mints **non-transferable ERC-1155 epoch claims** (`CreatorOImpairmentClaims`), excludes the impaired strategy from clean-book `totalAssets()`, and routes recoveries through a dedicated `CreatorORecoveryEscrow` paid pro-rata to claim holders only. Core functions: `tripImpairment` / `clearImpairmentTrip` / `proposeImpairmentRoot` / `challengeImpairmentRoot` / `finalizeImpairment` / `mintImpairmentClaim` / `notifyImpairmentRecovery` / `claimImpairmentRecovery` / `reinstateImpairedStrategy`. Claims are non-transferable in v1; transferable claims + claim-aware adapters are v2. Docs: canonical spec `docs/research/impairement.md`, ADR `docs/research/state-machine-rights-separation.md`, rationale `docs/research/impairment-pricing-in-vaults.md`, runbook `docs/operations/impairment-side-pocket-lifecycle-drill.md`, disclosures `docs/reference/impairment-v1-disclosures.md`. **Module swap is not in-place** (modules are `setModulesOnce`, storage moved to v3) — impairment requires a fresh v3 vault deploy. Live batcher `0xa99058…` `phase1Module` was rotated to the v3 modules, so new 1-click vaults use the impairment-capable code path. **1-click deploy auto-wires impairment in Phase 3** (`setImpairmentGuardian` / `setImpairmentClaims` / `setImpairmentRecoveryEscrow`, optional `setImpairmentChallengeWindow`, plus `setVault(...)` on claims+escrow), fail-closed if claims/escrow are not provided together or the deploy owner ≠ aux-contract `owner()`. Config flows through `VITE_IMPAIRMENT_*` / `IMPAIRMENT_*` (`IMPAIRMENT_CHALLENGE_WINDOW_SECONDS` optional) and `/api/deploy/config` exposes `impairmentClaims` / `impairmentRecoveryEscrow` / `impairmentGuardian` / `impairmentChallengeWindowSeconds` (server override resolution needs `ALLOW_API_CONTRACT_OVERRIDES=1` in production).
- **CreatorOVaultWrapper advanced wrap/unwrap paths share the cooldown invariant.** Advanced `wrap()` must record cooldown and advanced `unwrap()` must enforce it; tests that use the advanced path should advance time explicitly rather than bypassing cooldown expectations.
- **CreatorOVaultStrategiesModule hostile-withdraw accounting must include negative idle deltas.** When a failed strategy leg drains idle vault balance, increase the remaining withdrawal deficit before trying the next healthy strategy so final user transfers do not hit `InsufficientBalance`.
- **Solana route provisioner and Solana keeper orchestrator are separate production services.** Route provisioner (`frontend/server/solana-provisioner/`, `SOLANA_DYNAMIC_ROUTE_PROVISIONER_URL(S)`, bearer `SOLANA_DYNAMIC_ROUTE_PROVISIONER_SECRET`) handles deploy-time `POST /provision` and `POST /meteora-ixs` (e.g. `provisioner.4626.fun`). **`provisioner.4626.fun` DNS must A-record to the Vultr ops host (like `orchestrator.4626.fun`) — a CNAME to Vercel serves SPA HTML and breaks the API.** Keeper orchestrator (`kpr/solana-keeper-orchestrator.ts`, `pnpm -C kpr start:solana-orchestrator`, default port **8789**) exposes `GET /healthz` and `POST /reconcile` for ongoing keeper actions (`relay_entries`, `settle_fees`, `winner_relay`, etc.). Vercel `/api/keeper/solana/reconcile` calls only `SOLANA_ORCHESTRATOR_URL` (+ optional `SOLANA_ORCHESTRATOR_API_KEY` as `Authorization: Bearer`); it does **not** fall back to provisioner URLs or the legacy KPR `solana-orchestrator` workflow (no HTTP `/reconcile`).
- **Production orchestrator runs as its own systemd unit beside the route provisioner** — `solana-keeper-orchestrator.service`, env `/etc/4626/solana-keeper-orchestrator.env` (generate with `sudo bash kpr/deploy/seed-solana-orchestrator-env.sh --source /opt/4626/kpr/.env --dest /etc/4626/solana-keeper-orchestrator.env` from repo root on the ops host; do **not** copy full `kpr/.env` — it pulls legacy `CRE_*`, Privy, and XMTP noise the orchestrator does not read). Preflight + runtime prefer the orchestrator env file over `kpr/.env` and coerce deprecated `SOLANA_BRIDGE_ADAPTER` / lottery-manager addresses to canonical mainnet values. Public base `https://orchestrator.4626.fun` (set `SOLANA_ORCHESTRATOR_URL` to that host only, no `/reconcile` suffix). Ops assets: `kpr/deploy/install-systemd.sh`, `kpr/deploy/solana-keeper-orchestrator.service`, `kpr/deploy/solana-keeper-orchestrator.env.example`, `scripts/ops/remote-install-solana-orchestrator.sh`. After changing `SOLANA_ORCHESTRATOR_*` on Vercel (`akita-llc/4626`), **redeploy production** so API routes pick up the values.
- **When `SOLANA_ORCHESTRATOR_URL` is missing on Vercel, solana reconcile checkpoints stay `skipped_unconfigured`** and the control-plane operation should complete as `manual_review` (not `failed`); do not point `SOLANA_ORCHESTRATOR_URL` at the provisioner `/provision` URL.
- **Solana share-mesh lottery policy is locked — do not conflate the two lanes (A/B).** Canonical doc: `docs/operations/solana-share-mesh-lottery-policy.md`. Tradable Solana shares come from bridging Base ShareOFT via `solana_ovault_mesh` + `OVaultHubComposer`, not bridge-wrapped creator SPL or a creator re-deposit loop. **Share-mesh display symbol is `■<TICKER>` for every creator** (same as deploy UI — e.g. `■AKITA`, `■JESSE`); set at LZ deploy + `prepare-token-badge`. **Lottery fires only on pool buy of the tradable share token** — not compose deposit, not bridge receipt, not creator-coin trades. **`relay_entries` stays paused** (`KEEPER_SOLANA_RECONCILE_ACTIONS=settle_fees,winner_relay`, `SOLANA_ORCHESTRATOR_RELAY_ENTRIES_ENABLED=0`) until **B2** hook + share-mesh pool path is live; **B1** (LZ mesh + Meteora trading) keeps relay off until an off-chain swap relay ships. Point `SOLANA_CREATOR_MINTS` at the **share mesh or B2 hook mint**, not creator SPL (`9JWh…`). **AKITA mesh gap:** `creatorMesh(AKITA)` is not configured on-chain — Base ShareOFT lottery still works; Solana share mesh blocked until Phase A/B.
- **Pipe B (creator-coin asset mesh to Solana) is removed from deploy gating — only the share token exists on Solana.** Creator coins (e.g. `$AKITA`) live on Base only; Solana redeem pays out on Base. Deploy preflight/infra status no longer surfaces `depositEligible` / `solanaAssetMeshReady` / `assetPeerSet`, so a dormant asset-mesh lane can never block or confuse a vault deploy. `OVaultHubComposer` keeps its deposit path structurally (inert until `configureCreatorMesh(...)`), so reactivation needs no redeploy. Any future creator-coin bridging must use a **lockbox pattern** (Base stays canonical supply) via a **LayerZero OFT Adapter — this is the committed bridge choice; Chainlink CCIP was ruled out 2026-06-12** (Zora creator coins expose no `owner()`/`getCCIPAdmin()` for CCT self-service registration, and a second bridge vendor adds no capability) — do not reintroduce CCIP without a new product decision.
- **Both mainnet Solana programs have verified builds (solana-verify + OtterSec remote, June 2026), pinned to specific platform-tools versions.** LZ OFT `6ste36Y7fcbzJXkVQj3ApEqYb3wFZsZX63gT6wymhy3s` verifies from [4626fun/oft-solana-6ste-verifiable](https://github.com/4626fun/oft-solana-6ste-verifiable) only with `--cargo-build-sbf-args="--tools-version v1.51"` on the **default solana-verify image** (the `solanafoundation/anchor:v0.31.1` image or default tools produce a different hash — that mismatch caused the earlier failed attempt); `creator_share_hook` `EjpziSWGRcEiDHLXft5etbUtcJiZxEttkwz1tqiuzzWU` verifies from [4626fun/creator-share-hook-verifiable](https://github.com/4626fun/creator-share-hook-verifiable) with `--tools-version v1.52`. Verification PDAs are signed by the upgrade authority `7Qi3WW7q4kmqXcMBca76b3WjNMdRmjjjrpG5FTc8htxY` (the local default Solana keypair). Gotchas: trailing `-- args` in `solana-verify` go to plain `cargo build` and **cannot** carry `--tools-version` (use `--cargo-build-sbf-args`), and OtterSec remote verification caches results per commit — bust a stale failed cache by pushing a new commit and re-uploading the PDA. **Program security.txt and name/logo metadata** are written via `npx @solana-program/program-metadata write security|metadata <programId> <json>` signed by the same upgrade authority (repo-root `security.json` is the source file); explorer display can lag after a successful write. **Solana token metadata URIs must use the public `gateway.pinata.cloud`**, not the dedicated `lime-rear-booby-542.mypinata.cloud` / `pinata.4626.fun` gateway — the dedicated gateway has access controls enabled, so its 401s mean a missing `x-pinata-gateway-token` (`PINATA_GATEWAY_KEY` in `frontend/.env`), and putting a key-gated URL (or `?pinataGatewayToken=…`) in an on-chain URI would publish the secret. Use the dedicated gateway with the key for app/server fetches only; the same CIDs serve through any gateway, so wallet rendering must not depend on dedicated-gateway state. No gateway "index file" is needed for `/ipfs/<cid>` paths; the `pinata.4626.fun` custom domain stays pending until DNS validation completes in the Pinata dashboard.
- **`GET /api/accounts/me` requires `X-Privy-Token`.** Ad-hoc browser `fetch('/api/accounts/me', { credentials: 'include' })` without Privy headers returns `Missing Privy auth token`; use `useAccountMe` / `apiFetch` with `getAccessToken`, or `GET /api/auth/me` for the HttpOnly session snapshot only.
- **`preview-add-owner` / `preview-remove-owner` server code must not re-export `src/` modules that use Vite `@/` aliases** — Vercel Node API cannot resolve `@/`; keep gas-reserve and similar Relay helpers inlined under `server/_lib/relay/`.
- **`profiles.base_sub_account` may persist while `executionTrack` stays `none-yet`.** Gate signing/swap on `executionTrack === 'legacy-owner-install'` and on-chain parent-CSW embedded-owner check, not sub-account registration alone.
- **Solana route provisioner imports bridge helpers from `frontend/server/_lib/onchain/`** (`solanaBridgeCliRunner`, etc.) — `../_lib/` paths break production `solana-provisioner` startup with `ERR_MODULE_NOT_FOUND`.
- **Telegram → AlfaClub inbound relay is env-gated via `TELEGRAM_TO_ALFACLUB_*`.** `frontend/server/_lib/alfaclub/telegramToAlfaclubRelay.ts` posts configured supergroup/topic messages into `TELEGRAM_TO_ALFACLUB_ROOM_ID` (default `ALFACLUB_CHAT_ROOM_ID`) with `ALFACLUB_API_KEY` and skips the normal Telegram handler for that chat; AlfaClub → Telegram remains `ALFACLUB_TELEGRAM_RELAY_*`.
- **AlfaClub Cloudflare proxy auth shape:** only `GET /_health` is public without `x-proxy-secret`. Relayed API paths need `x-proxy-secret` aligned with `ALFACLUB_CHAT_API_PROXY_SECRET` / Worker `PROXY_SHARED_SECRET`. Worker body `{"error":"unauthorized"}` means bad/missing secret; upstream `No JWT token found` with a good secret means the proxy reached AlfaClub (JWT is DB/`chatBridge`, not local `ALFACLUB_CHAT_JWT`).
- **Zora wallet holdings use three coin types (`CREATOR` | `CONTENT` | `TREND`).** Normalize via `frontend/src/lib/zora/coinType.ts` (`normalizeZoraCoinType`) — do not bucket unknown or trend rows as creator coins; tray (`server/_lib/wallet/zoraWalletHoldings.ts`), account tray, and swap selector expose separate trend sections (`sectionTag: 'trend'`).
- **`/swap` token selector must combine vault index with Zora discovery.** Vault-only `/api/v1/explore/vaults` / `keepr_vaults` rows miss creator coins like AKITA; parallel Zora profile/coin resolution lives in `frontend/src/lib/swap/zoraTokenSearch.ts` with `$`/`@` stripped via `normalizeCoinSearchQuery`.
- **`/swap` Buy output display is quote-only.** The read-only Buy field must bind to `estimatedOut` (or post-success `swapCompletion.estimatedOut`), not wallet balance — empty-quote fallback to balance shows misleading `0` while quoting; show "Fetching quote…" while `busy === 'quote'`. Auto-quote effects that pause while signing must destructure `txState` from `useSwapExecution()` — referencing `txState` without pulling it from the hook crashes `/swap`.
- **`useAccountMe` must not cache null/failed responses.** Only cache successful `/api/accounts/me` payloads; retry when Privy token becomes available and bootstrap-fallback execution signals when track is `none-yet` but on-chain owner is confirmed. `canonicalSignerGate` should not return `execution-setup-required` when `ownerCheckStatus === 'owner'` even if server track lags.
- **Canonical swap submit latency:** after a passing Zora router `eth_call` validated at Review, skip the redundant CSW `execute()` simulation on submit — that path was the main multi-second stall on `canonical4337` sells.
- **Ethereum vault Blender generator** (`tools/vault-images/ethereum_vault_generator.py`): `pnpm -C frontend build:vault-glb` or `blender --background --python tools/vault-images/ethereum_vault_generator.py` → `ethereum_vault.glb`, preview PNG, `.blend` in `tools/vault-images/`; copy GLB to `frontend/public/immersive/assets/vault/`. Shield GLSL adapted from cortiz2894/flow-shield-effect (`tools/vault-images/NOTICE.md`). Marketing bundle ~600KB gzip (full R3F + drei + postprocessing) — optimize later if LCP regresses.
- **Arena backtest bar loading:** Hyperliquid exposes ~3.5 days of 1m candles per request; 90-day minute fidelity needs Supabase `backtest_market_bars_1m` via `pnpm -C frontend exec tsx scripts/cache-backtest-minute-bars.ts` (uses `backfill_window` when coverage below ~92%). Auto bar-size selection picks the finest interval with full-horizon coverage — until the cache fills, 90d runs resolve to **1h**. Runs via `POST /api/v1/alfaclub/backtest-run` (returns inline `series` + `resolvedInterval`); deploy-dry-run dev uses `localhost:5174/__api/v1/alfaclub/backtest-run`.
