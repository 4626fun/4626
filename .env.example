# ═══════════════════════════════════════════════════════════════════════════════
#                           4626 Environment
# ═══════════════════════════════════════════════════════════════════════════════
# Copy this file to .env and fill in your values
# ═══════════════════════════════════════════════════════════════════════════════

# ═══════════════════════════════════════════════════════════════════════════════
#                              REQUIRED
# ═══════════════════════════════════════════════════════════════════════════════

# Your deployer private key (without 0x prefix)
PRIVATE_KEY=

# Etherscan API key for contract verification
ETHERSCAN_API_KEY=

# ═══════════════════════════════════════════════════════════════════════════════
#                         NETWORK CONFIGURATION
# ═══════════════════════════════════════════════════════════════════════════════

# Base Mainnet RPC (default works, but you can use your own)
RPC_URL=https://mainnet.base.org

# Base Sepolia Testnet (for testing)
# RPC_URL=https://sepolia.base.org

# ═══════════════════════════════════════════════════════════════════════════════
#                     COINBASE DEVELOPER PLATFORM
# ═══════════════════════════════════════════════════════════════════════════════
# Get your API key from: https://portal.cdp.coinbase.com/

# Your Coinbase Paymaster & Bundler endpoint (same URL for both)
# Example: https://api.developer.coinbase.com/rpc/v1/base/<CDP_API_KEY_ID>
BUNDLER_URL=
PAYMASTER_URL=

# ═══════════════════════════════════════════════════════════════════════════════
#                         ERC-4337 SMART ACCOUNT
# ═══════════════════════════════════════════════════════════════════════════════
# Your ERC-4337 compatible smart account address

SMART_ACCOUNT=

# ═══════════════════════════════════════════════════════════════════════════════
#                     DEPLOYED CONTRACT ADDRESSES
# ═══════════════════════════════════════════════════════════════════════════════
# Fill these in after running: ./script/deploy.sh infrastructure

# Core Infrastructure
CREATOR_REGISTRY=
CREATOR_FACTORY=
PAYOUT_ROUTER_FACTORY=
DEPLOYMENT_BATCHER=

# Shared Services
LOTTERY_MANAGER=
VRF_CONSUMER=

# Payout router keeper resolution (server-side deploy/paymaster + KPR runtime).
# Priority: PAYOUT_ROUTER_KEEPER -> KPR_KEEPER_ADDRESS -> KPR_ADDRESS -> KPR_KEEPER_ADDRESS
# Audit I-13 / 4626-394: do not commit real keeper addresses to example env files.
# Use a placeholder here and inject the real address via Vercel/KPR secrets.
PAYOUT_ROUTER_KEEPER=0x0000000000000000000000000000000000000000
KPR_KEEPER_ADDRESS=0x0000000000000000000000000000000000000000
KPR_ADDRESS=0x0000000000000000000000000000000000000000
# Legacy fallback (kept for compatibility during migration):
KPR_KEEPER_ADDRESS=0x0000000000000000000000000000000000000000

# ═══════════════════════════════════════════════════════════════════════════════
#                         CHAINLINK VRF (Optional)
# ═══════════════════════════════════════════════════════════════════════════════
# Create subscription at: https://vrf.chain.link/base

VRF_SUBSCRIPTION_ID=

# ═══════════════════════════════════════════════════════════════════════════════
#                         PRE-DEPLOYED CONTRACTS
# ═══════════════════════════════════════════════════════════════════════════════
# These are already deployed on Base - no need to change

# Tax Hook (6.9% sell fees)
TAX_HOOK=0xca975B9dAF772C71161f3648437c3616E5Be0088

# LayerZero V2 Endpoint
LZ_ENDPOINT=0x1a44076050125825900e736c501f859c50fE728c

# Chainlink ETH/USD Price Feed
CHAINLINK_ETH_USD=0x71041dddad3595F9CEd3DcCFBe3D1F4b0a16Bb70

# Chainlink VRF Coordinator V2.5
VRF_COORDINATOR=0xd5D517aBE5cF79B7e95eC98dB0f0277788aFF634

# ERC-4337 EntryPoint v0.6
ENTRY_POINT=0x5FF137D4b0FDCD49DcA30c7CF57E578a026d2789

# ═══════════════════════════════════════════════════════════════════════════════
#                               CREATE2 DEPLOYMENT
# ═══════════════════════════════════════════════════════════════════════════════
#
# Universal CREATE2 deployer (present on Base). Useful if you want a known “CREATE2 factory” address.
CREATE2_FACTORY=0x4e59b44847b379578588920cA78FbF26c0B4956C

# 4626 Phase-2 AA deployer (deployed by this repo; has an explicit ABI: deploy(bytes32,bytes)).
# FIX: M-13 (4626-322) — live value redacted. See
# docs/operations/deployment/addresses.md.
CREATE2_DEPLOYER=0xYOUR_CREATE2_DEPLOYER_HERE

# Optional fresh infra epoch controls (DeployBaseMainnetDeployer):
# Precedence for each salt: raw bytes32 env -> *_SALT_TAG keccak256 -> built-in default.
# Convenience mode: set DEPLOYMENT_EPOCH_TAG and deploy scripts derive all *_SALT_TAG values.
DEPLOYMENT_EPOCH_TAG=
INFRA_STORE_SALT=
INFRA_STORE_SALT_TAG=
INFRA_DEPLOYER_FROM_STORE_SALT=
INFRA_DEPLOYER_FROM_STORE_SALT_TAG=
INFRA_VAULT_CORE_MODULE_SALT=
INFRA_VAULT_CORE_MODULE_SALT_TAG=
INFRA_VAULT_STRATEGIES_MODULE_SALT=
INFRA_VAULT_STRATEGIES_MODULE_SALT_TAG=
INFRA_VAULT_ADMIN_MODULE_SALT=
INFRA_VAULT_ADMIN_MODULE_SALT_TAG=
INFRA_DEPLOYMENT_BATCHER_SALT=
INFRA_DEPLOYMENT_BATCHER_SALT_TAG=

# Current live Base deployment batcher (`DeploymentBatcher`, canonical `v1.11.1` release — see docs/reference/addresses.md)
# FIX: M-13 (4626-322) — live values redacted.
CREATOR_VAULT_BATCHER=0xYOUR_CREATOR_VAULT_BATCHER_HERE
DEPLOYMENT_BATCHER=0xYOUR_DEPLOYMENT_BATCHER_HERE
# Optional alias for env-based cutovers. Current live value matches `DEPLOYMENT_BATCHER`.
# CREATOR_VAULT_BATCHER_AUTO_HANDOFF=0xYOUR_AUTO_HANDOFF_HERE

# Optional Solana routing config for DeploymentBatcher (20% split)
SOLANA_BRIDGE_ADAPTER=0xYOUR_SOLANA_BRIDGE_ADAPTER_HERE
SOLANA_DESTINATION=
SOLANA_KEEPER_PUBKEY=
# Optional auto-registration defaults (used by /api/deploy/registerSolanaBridgeToken):
# - signer must match adapter owner
SOLANA_ADAPTER_OWNER_PRIVATE_KEY=
SOLANA_DEFAULT_MINT_BYTES32=
SOLANA_DEFAULT_MINT_DECIMALS=9
# Optional dynamic route provisioning (advanced):
# If enabled, server will run Base bridge CLI wrap-token when route is missing.
# IMPORTANT: on serverless runtimes (e.g. Vercel), local filesystem CLI paths are not available.
# In production, prefer SOLANA_DYNAMIC_ROUTE_PROVISIONER_URL and keep this runtime lightweight.
SOLANA_DYNAMIC_ROUTE_ENABLED=0
SOLANA_BRIDGE_CLI_DIR=
SOLANA_BRIDGE_CLI_BIN=bun
SOLANA_BRIDGE_DEPLOY_ENV=mainnet
SOLANA_BRIDGE_PAYER_KP=config
SOLANA_BRIDGE_SCALER_EXPONENT=9
# The Solana provisioner now passes the exact ERC-20 name/symbol of the
# bridge token to `wrap-token` (strict parity mode). Transform-based fallbacks
# (name/symbol prefix, symbol mode) have been removed; no env var configures
# the mint's display name.
SOLANA_BRIDGE_PAY_FOR_RELAY=1
# Optional remote dynamic provisioner (used when CLI isn't present in runtime, e.g. Vercel).
# Endpoint should return JSON with `mintBytes32` on success.
# If using `frontend/server/solana-provisioner`, set this to: https://<host>/provision
SOLANA_DYNAMIC_ROUTE_PROVISIONER_URL=
SOLANA_DYNAMIC_ROUTE_PROVISIONER_SECRET=
# Enforce SOL-only Meteora quote mint during deploy-session ix generation.
# Keep enabled to avoid helper-token fallback mappings.
SOLANA_STRICT_SOL_PAIR=1
# Optional health endpoint for external checks and deploy infra dashboards.
# Example: https://<host>/healthz
SOLANA_DYNAMIC_ROUTE_PROVISIONER_HEALTH_URL=
# Optional extra origins for session status auto-registration (useful with mixed Vercel + VM runtimes).
# Comma/space-separated absolute origins, e.g.:
# DEPLOY_SOLANA_REGISTRATION_ORIGINS=https://4626.fun,https://session.4626.fun
DEPLOY_SOLANA_REGISTRATION_ORIGINS=
# Optional machine-to-machine secret for /api/deploy/registerSolanaBridgeToken.
# When set on both runtimes, session status will forward this secret as
# X-CV-Solana-Registration-Secret during cross-origin auto-registration calls.
DEPLOY_SOLANA_REGISTRATION_SECRET=
# Deploy-session Solana preflight route mode:
# - ovault_first (default): setupSolanaOvaultMesh, fallback to legacy when route is unavailable
# - legacy_first: registerSolanaBridgeToken first, fallback to OVault route when unavailable
# - ovault_only: only setupSolanaOvaultMesh (no fallback)
# - legacy_only: only registerSolanaBridgeToken (rollback mode)
DEPLOY_SOLANA_PREFLIGHT_ROUTE_MODE=ovault_first
# Emergency rollback kill switch: force legacy_only regardless of route mode.
DEPLOY_SOLANA_OVAULT_KILL_SWITCH=0
# Disable direct writes on legacy route /api/deploy/registerSolanaBridgeToken.
# Keep 0 during canary; set 1 after OVault cutover completes.
DEPLOY_SOLANA_LEGACY_WRITE_DISABLED=0
# Optional: if set, deploy/solanaInfraStatus will validate scalar(route) for this bridge token.
# Leave empty unless you intentionally pin a default bridge-token+mints route.
SOLANA_DEFAULT_BRIDGE_TOKEN=

# ═══════════════════════════════════════════════════════════════════════════════
#                         ZORA REVENUE / REFERRALS
# ═══════════════════════════════════════════════════════════════════════════════
# Platform referrer address for Zora Coins.
# - Coin creation: earns 0.2% of ALL future trades on that coin FOREVER
# - Per-trade: earns 0.04% per trade executed through Keepr
# Falls back to XMTP_AGENT_CSW_ADDRESS if not set.
ZORA_PLATFORM_REFERRER_ADDRESS=
# Client-side (Vite VITE_ prefix) — used by the waitlist coin creation card
VITE_ZORA_PLATFORM_REFERRER_ADDRESS=

# Auto-cast trade events to Farcaster (true/false, default: false)
AUTOCAST_TRADES=false

# Neynar webhook secret for Farcaster mention replies
FC_MENTION_WEBHOOK_SECRET=

# ═══════════════════════════════════════════════════════════════════════════════
#                         4626 INFRASTRUCTURE (Base Mainnet)
# ═══════════════════════════════════════════════════════════════════════════════
# Shared resources (registry/lottery/VRF/payout factory/vault activation batcher)
# carry over from the v1.8.3 broadcast (2026-04-11). Module + bytecode store +
# DeploymentBatcher were re-deployed in the v1.9.1 broadcast (2026-04-19) to
# enable the weight-0-skip + at-least-1-strategy behavior for the paid opt-in
# feature paywall. Historical v1.8.3 addresses for these are preserved in
# AGENTS.md + docs/operations/creator-strategy-features.md for audit.

# FIX: M-13 (4626-322) — live protocol addresses redacted from the
# committed example. Canonicalised in docs/operations/deployment/addresses.md
# and should be pulled from there (or from a CI-injected secret) at
# runtime — never committed.
CREATOR_REGISTRY=0xYOUR_CREATOR_REGISTRY_HERE
CREATOR_FACTORY=0xYOUR_CREATOR_FACTORY_HERE
CREATOR_LOTTERY_MANAGER=0xYOUR_CREATOR_LOTTERY_MANAGER_HERE
CREATOR_VRF_CONSUMER=0xYOUR_CREATOR_VRF_CONSUMER_HERE
PAYOUT_ROUTER_FACTORY=0xYOUR_PAYOUT_ROUTER_FACTORY_HERE
VAULT_ACTIVATION_BATCHER=0xYOUR_VAULT_ACTIVATION_BATCHER_HERE

# v1.9.1 store + deployer + batcher (2026-04-19). Previous v1.8.3
# addresses are preserved under docs/operations/deployment/addresses.md
# (they remain live for AKITA's pre-paywall vault).
# FIX: M-13 (4626-322) — live batcher/store addresses redacted. See
# docs/operations/deployment/addresses.md.
UNIVERSAL_BYTECODE_STORE=0xYOUR_UNIVERSAL_BYTECODE_STORE_HERE
UNIVERSAL_CREATE2_FROM_STORE=0xYOUR_UNIVERSAL_CREATE2_FROM_STORE_HERE
CREATOR_VAULT_BATCHER=0xYOUR_CREATOR_VAULT_BATCHER_HERE
DEPLOYMENT_BATCHER=0xYOUR_DEPLOYMENT_BATCHER_HERE
# Optional alias for env-based cutovers. Current live value matches `DEPLOYMENT_BATCHER`.
# CREATOR_VAULT_BATCHER_AUTO_HANDOFF=0xYOUR_AUTO_HANDOFF_HERE

# ═══════════════════════════════════════════════════════════════════════════════
#                     CREATOR STRATEGY FEATURES — PAYMENT PATHS
# ═══════════════════════════════════════════════════════════════════════════════
# docs/operations/creator-strategy-features.md § "Payment paths"

# USDC destination for all creator strategy feature payments (USDC-on-Base
# and x402). Defaults to the protocol treasury Safe when unset — only override
# this for staging environments that must route payments to a different sink.
# CREATOR_STRATEGY_FEATURE_USDC_TREASURY=0xYOUR_TREASURY_SAFE_HERE

# ----- x402 (HTTP 402 Payment Required, EIP-3009 gasless USDC) -----
# Private key (with or without 0x prefix) of the EOA that broadcasts the
# settled `usdc.transferWithAuthorization(...)` tx on Base. The relayer pays
# Base gas so the creator doesn't need ETH. Fall-through order:
#   1. X402_RELAYER_PRIVATE_KEY (preferred, isolated from infra key)
#   2. PRIVATE_KEY (top of this file; fallback)
# If neither is set, the /api/creator/strategy/x402-activate endpoint still
# returns a valid 402 response but rejects settlement with
# `x402_relayer_not_configured` when a signed authorization arrives.
# X402_RELAYER_PRIVATE_KEY=

# ----- Stripe (credit-card fiat payments) -----
# Get from https://dashboard.stripe.com/apikeys (use TEST keys for dev/staging).
# When unset, /api/creator/strategy/stripe/checkout returns 503 and the
# payment path is effectively disabled.
# STRIPE_SECRET_KEY=sk_test_...
# STRIPE_WEBHOOK_SECRET=whsec_...

# Base origin for the Stripe Checkout return URLs. The handler appends
# /creator/strategy/activation-success?... and /activation-cancelled?...
# to this. Defaults to http://localhost:5173 for local dev.
# STRIPE_RETURN_URL_BASE=https://app.4626.fun

# ═══════════════════════════════════════════════════════════════════════════════
#                     ALFACLUB LP FACTORY (deploy script env)
# ═══════════════════════════════════════════════════════════════════════════════
# Used by `alfaclub/contracts/script/DeployAlfaCreatorKeyLPFactory.s.sol` to deploy
# the AlfaClub sudoswap-style ERC1155 key / ERC20 creator-coin LP factory.
#
# Owner of the deployed `AlfaCreatorKeyLPFactory`. Holds `setPoolCreatorAllowed`
# and `setPairAllowed` allowlist gates. Use a multisig in production.
# FACTORY_OWNER=0x0000000000000000000000000000000000000000
#
# Optional escape hatch for non-Base RPCs. The factory hardcodes the Base
# mainnet AlfaClub FriendKey address, so deploying off Base/Base Sepolia
# yields a brick factory unless you also point the FriendKey constant at a
# valid contract on that chain. Default: refuse to broadcast.
# ALLOW_NON_BASE=1

# ----- Base Sepolia deploy command (dry run) -----
# FACTORY_OWNER=0xYourOwner \
# forge script alfaclub/contracts/script/DeployAlfaCreatorKeyLPFactory.s.sol:DeployAlfaCreatorKeyLPFactory \
#     --rpc-url https://sepolia.base.org \
#     -vvvv

# ----- Base Sepolia deploy command (broadcast + verify) -----
# PRIVATE_KEY=0x... \
# FACTORY_OWNER=0xYourOwner \
# forge script alfaclub/contracts/script/DeployAlfaCreatorKeyLPFactory.s.sol:DeployAlfaCreatorKeyLPFactory \
#     --rpc-url https://sepolia.base.org \
#     --broadcast \
#     --verify \
#     -vvvv

# Frontend wiring: after deploy, set this in the Vite env (or update
# `frontend/src/config/contracts.defaults.ts`) so the AlfaClub liquidity
# page and any consumers point at the live factory.
# VITE_ALFA_CREATOR_KEY_LP_FACTORY=0x0000000000000000000000000000000000000000
