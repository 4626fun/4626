---
title: ve■4626
sidebar_position: 3
---

# ve■4626 (`ve4626`)

Vote-escrow **commitment**: lock **■4626 only** for dual-decay power.

Canonical naming: [ve-naming.md](./ve-naming.md).

| | |
|--|--|
| **Product** | **ve■4626** |
| **Contract** | `ve4626` |
| **Lock asset** | **■4626 only** (`wrappedShareOFT` immutable) — not creator ShareOFT |
| **Utilities** | `ve4626Utility` → **ve33** / **veLottery** |

## Purpose

- Lock **■4626** (protocol ShareOFT / $4626 stack) → dual-decay power
- **Not** creator ■ (e.g. ■AKITA) or Creator Coin — `lock` reverts `InvalidToken`
- Claim **ve33** / **veLottery** via `ve4626Utility` (not automatic)
- Gauge voting + fees use **ve33** when wired
- Lottery mult uses **veLottery** when wired (opt-in)
- After decay, `sync` keeps utilities ≤ capacity

## Lock API

```solidity
// token MUST be wrappedShareOFT (■4626)
lock(token, amount, duration);
extendLock(newEnd);
increaseLock(amount);  // same asset only
unlock();
getVotingPower(user);
getTotalVotingPower(); // dual-decay total — use this for share math
```

## Utility API

```solidity
// ve4626Utility
claimVe33(amount);
claimVeLottery(amount);      // opt-in lottery edge
claimAllOutstanding();       // default: ve33 only
forfeitVe33 / forfeitVeLottery / forfeitAll();
sync(user);                  // burn excess after dual-decay (veLottery first)
previewUtilities(user);      // view: post-haircut ve33/veLottery
effectiveVe33Of(user);       // decay-safe gauge weight
effectiveVeLotteryOf(user);  // decay-safe boost share
// Prefer effective* over raw ve33Of/veLotteryOf (may be stale until sync)
```

## Consumer wiring (deploy)

```solidity
// DeployRewardsEcosystem
boostManager.setUtility(utility);  // also sets veLotteryToken
voting.setUtility(utility);        // also sets ve33Token; vote() syncs
```

## Personal lottery boost (1.0×–2.5×)

```text
working = min(0.4·l + 0.6·L·(ve/Ve), 1.0·l)
boost   = working / (0.4·l) ∈ [1.0×, 2.5×]
effectiveBoost = 1 + coverage·(boost - 1)

l  = min(creatorShareUSD, swapUSD)     // covered skin
L  = total creator ShareOFT supply USD // pool
ve = effectiveVeLotteryOf
Ve = ve4626.getTotalVotingPower()
coverage = l / swapUSD
```

- Curve caps working balance at `l`; **2.5×** is measured relative to the 0.4 tokenless baseline.
- No covered position → personal mult inactive (neutral; base odds unchanged).
- No veLottery → neutral **1.0×**.
- ve share ≥ position share → raw **2.5×**; only covered swap value receives the uplift.
- API: `calculateBoostForPosition(user, shareUSD, swapUSD, totalShareUSD)`.

