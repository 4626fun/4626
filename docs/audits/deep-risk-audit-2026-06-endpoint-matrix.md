# 4626 Deep Risk Audit — Endpoint Matrix

Date: 2026-06-24
Mode: audit-only, Security/API/Auth pass
Repository: `/home/akitav2/projects/4626`
Status: PARTIAL — stopped early after APIAUTH-001 per user stop condition.

## Scope note

This file replaces the Phase 0 scaffold with the required endpoint-matrix columns for the routes inspected before the early-stop condition was met. It is not a full endpoint inventory yet: the pass stopped when `/api/accounts/me` matched the user's immediate-stop condition for an expensive or mutating endpoint with no rate limit.

## Matrix columns

| Column | Meaning |
|---|---|
| Route key | Static route-map key or public API route. |
| Handler file | Concrete handler file inspected. |
| HTTP methods | Methods accepted by the handler. |
| Public/authenticated/admin/machine-auth classification | Observed access control. |
| Mutating or read-only | Observed side-effect class. |
| External side effects | External systems touched or potentially touched. |
| Rate-limit key and limit | Observed limiter key/limit or missing. |
| Body parser and maxBytes value | Observed request body handling. |
| Timeout behavior | Observed explicit timeout behavior; Vercel/global timeout if none. |
| Tests covering auth/rate/body behavior | Tests found during this pass. |
| Audit status | PASS/FAIL/NEEDS MANUAL REVIEW. |

## Endpoint matrix — inspected before early stop

| Route key | Handler file | HTTP methods | Public/authenticated/admin/machine-auth classification | Mutating or read-only | External side effects | Rate-limit key and limit | Body parser and maxBytes value | Timeout behavior | Tests covering auth/rate/body behavior | Audit status: PASS/FAIL/NEEDS MANUAL REVIEW |
|---|---|---|---|---|---|---|---|---|---|---|
| `accounts/me` | `frontend/api/_handlers/accounts/_me.ts` | `GET` only; `OPTIONS` handled by shared `handleOptions` | Authenticated via Privy token (`verifyPrivyForAccounts(req)` -> `verifyPrivyRequest(req)`) | Mutating read path: ensures schema, syncs email identity, upserts account/linked method/point event, then returns snapshot | Privy token verification and `getUserById`; DB reads/writes | MISSING. Search found no `checkRateLimit`, `checkDurableRateLimit`, `RATE_LIMITS`, `rateLimitKey`, or `Retry-After` in handler. | No request body expected for `GET`; no body parser. | No handler-level timeout; falls under catch-all Vercel function config. `frontend/vercel.json` is in pre-existing staged local diff, so any timeout claim from that file is LOCAL-DIFF PRECONDITION unless checked against clean main. | `frontend/api/__tests__/accountsMe.test.ts` covers successful payload and `emailVerified=false`; no rate-limit/429/Retry-After/body tests found. `frontend/src/hooks/useAccountMe.test.ts` covers client use of `X-Privy-Token`. | FAIL — APIAUTH-001 |
| `auth/nonce` | `frontend/api/_handlers/auth/_nonce.ts` | `GET` observed from handler inspection | Public nonce issuance | Mutating/session-adjacent: creates SIWE nonce cookie | Cookie issuance | `authNonce` limiter observed in file read; `Retry-After` present on 429. | No body for `GET`. | No explicit per-handler timeout observed. | Not fully traced before early stop. | NEEDS MANUAL REVIEW |
| `auth/verify` | `frontend/api/_handlers/auth/_verify.ts` | `POST` observed from handler inspection | Public + signature-based SIWE verify | Mutating: verifies SIWE, issues session cookie | Cookie issuance; DB/wallet sync paths possible by imports/read | Rate-limit logic observed in file read; exact key/limit not matrix-certified before early stop. | JSON body parser observed; exact maxBytes not matrix-certified before early stop. | No explicit per-handler timeout observed. | Not fully traced before early stop. | NEEDS MANUAL REVIEW |
| `auth/privy` | `frontend/api/_handlers/auth/_privy.ts` | `POST` observed from handler inspection | Authenticated by Privy token | Mutating: Privy/session bridge; issues HttpOnly session cookie | Privy verify/getUser; DB wallet sync best-effort; cookie issuance | Rate-limit logic observed in file read; exact key/limit not matrix-certified before early stop. | JSON body parser observed; exact maxBytes not matrix-certified before early stop. | No explicit per-handler timeout observed. | Not fully traced before early stop. | NEEDS MANUAL REVIEW |
| `auth/me` | `frontend/api/_handlers/auth/_me.ts` | `GET` observed from handler inspection | Cookie/session snapshot | Read-only session snapshot | None beyond cookie/session read | No rate limit observed during priority read; needs review because read-only session endpoint may be acceptable. | No body. | No explicit per-handler timeout observed. | Not fully traced before early stop. | NEEDS MANUAL REVIEW |
| `auth/logout` | `frontend/api/_handlers/auth/_logout.ts` | `POST` observed from handler inspection | Session/cookie scoped | Mutating: clears session cookie | Cookie mutation | No rate limit certified before early stop. | No body or minimal body; not fully certified. | No explicit per-handler timeout observed. | Not fully traced before early stop. | NEEDS MANUAL REVIEW |
| `auth/handoff/create` | `frontend/api/_handlers/auth/_handoff-create.ts` | `POST` observed from handler inspection | Session/auth scoped | Mutating: creates handoff token/session artifact | DB/session side effect likely; exact sink not fully traced | Rate-limit/body not fully certified before early stop. | JSON/body handling observed but exact maxBytes not certified. | No explicit per-handler timeout observed. | Not fully traced before early stop. | NEEDS MANUAL REVIEW |
| `auth/handoff/redeem` | `frontend/api/_handlers/auth/_handoff-redeem.ts` | `POST` observed from handler inspection | Token redemption | Mutating: redeems/consumes handoff | DB/session side effect likely; exact sink not fully traced | Rate-limit/body not fully certified before early stop. | JSON/body handling observed but exact maxBytes not certified. | No explicit per-handler timeout observed. | Not fully traced before early stop. | NEEDS MANUAL REVIEW |
| `paymaster` | `frontend/api/_handlers/paymaster/_paymaster.ts` | JSON-RPC style POST observed by priority read/search | Authenticated/policy-gated principal + custom owner/deploy-session paths | External sponsorship decision path; can enable ERC-4337 UserOp sponsorship | Coinbase/CDP/bundler/paymaster-facing behavior depending request | Body bounded: `readJsonBody(... PAYMASTER_MAX_BODY_BYTES)` observed; rate-limit/policy details not fully certified before early stop. | `PAYMASTER_MAX_BODY_BYTES` constant used; exact value not certified before early stop. | No explicit whole-handler timeout certified before early stop. | Not fully traced before early stop. | NEEDS MANUAL REVIEW |
| `deploy/v2/session/create` | `frontend/api/_handlers/deploy/v2/session/_createCore.ts` | `POST` | Authenticated by deploy auth (`readDeployAuthFromRequest`) | Mutating create path; `preflightOnly` branch is intended read-only validation but also validates and may perform expensive checks | DB deploy-session writes; Privy creator agent wallet provisioning on create branch | `deploy-preflight:<auth>` 20/min in-memory for preflight; durable `RATE_LIMITS.deployCreate` for create; `Retry-After` set | `readBoundedJsonObjectBody(... 512_000)` | No explicit whole-handler timeout; Vercel catch-all maxDuration subject to local diff caveat | `frontend/api/__tests__/deploySessionOwnership.test.ts`, `deploySession.test.ts` found but not fully mapped | NEEDS MANUAL REVIEW |
| `deploy/v2/session/status` | `frontend/api/_handlers/deploy/v2/session/_statusCore.ts` | `POST` | Authenticated by deploy auth and authorized session access | Mutating/advancing status path possible: status polling can advance deploy stages | DB updates, Privy wallet signing, bundler/paymaster/on-chain UserOps possible by imports/call path | `deploy-session-status:<auth>` with `RATE_LIMITS.deploySessionStatus`; `Retry-After` set | `readBoundedJsonObjectBody(... 8_192)` | No explicit whole-handler timeout; Vercel catch-all maxDuration subject to local diff caveat | `frontend/api/__tests__/deploySession.test.ts` found but not fully mapped | NEEDS MANUAL REVIEW |
| `deploy/v2/session/dry-run` | `frontend/api/_handlers/deploy/v2/session/_dryRunCore.ts` | `POST` | Authenticated by deploy auth | Mutating local fork only; should not mutate production chain | Local Anvil/Hardhat impersonation and fork state mutation | `deploy-session-dry-run:<auth>` with `RATE_LIMITS.deploySessionDryRun`; `Retry-After` set | `readBoundedJsonObjectBody(... 512_000)` | Local fork guard observed; no explicit whole-handler timeout | Not fully traced before early stop. | NEEDS MANUAL REVIEW |
| `telegram/link/complete` | `frontend/api/_handlers/telegram/_link-complete.ts` | `POST` | Authenticated Privy token plus Telegram Mini App session token | Mutating: binds Telegram identity after account resolution; consumes link token if present | DB writes, Telegram link event tracking | `telegram-link-complete:<ip>` with `RATE_LIMITS.telegramLinkWrite`; `Retry-After` set | `readBoundedJsonObjectBody(... 16_384)` | No explicit whole-handler timeout observed | Not fully traced before early stop. | NEEDS MANUAL REVIEW |
| `telegram/link/start` | N/A in current route map | N/A | N/A | N/A | N/A | N/A | N/A | N/A | File requested by user was not present. `_routes.telegram.ts` registers `link/complete`, `link/ready`, `link/telemetry`, `miniapp/status`, `miniapp/session`, `metrics`, `portfolio`, `holder-recheck`, `unlink`; no `link/start` route observed before early stop. | NEEDS MANUAL REVIEW |
| `v1/alfaclub/backtest-run` | `frontend/api/_handlers/v1/alfaclub/_backtest-run.ts` | `POST` | Authenticated by Privy token | Compute-heavy; writes temp backtest outputs under `tmp/backtests` | Privy verify/getUser; market data/cache/backtest execution via `executeBacktestCounterRebalance(...)`; filesystem temp output | `alfaclub-backtest-run:<privyUserId>:<ip>` with `RATE_LIMITS.alfaclubBacktestRun`; `Retry-After` set | `readJsonBody(... 65_536)` | No explicit handler timeout observed; route uses catch-all function config. | Not fully traced before early stop. | NEEDS MANUAL REVIEW |

## Early-stop reason

The full endpoint inventory remains incomplete. The next pass should resume from APIAUTH-001 and continue route-family enumeration only after the launch-blocking `/api/accounts/me` rate/snapshot issue is accepted, mitigated, or explicitly waived.
