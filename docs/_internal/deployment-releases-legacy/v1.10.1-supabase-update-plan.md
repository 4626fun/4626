# v1.10.1 Supabase update plan

Planning-only artifact for the v1.10.1 pre-broadcast cleanup sweep.
No Supabase modifications were performed in this session.

> **AMENDED 2026-05-02:** Live database inspection complete via Supabase connector (project `qajpnuvqlcfseghnldkl`). Orphan-address sweep across 104 address-shaped columns in `public` and `alfaclub` schemas returned **0 matches** for all 6 canonical orphan addresses. **Step 14 Supabase UPDATE: `NOOP_NO_ORPHAN_ROWS` — no action required post-broadcast.**
>
> ~~Tooling not installed locally; plan derived from inspection of supabase/migrations/ and supabase/seed.sql.~~

## Local-artifact scan results

Local tooling status:

- `supabase`: not on `PATH`
- `psql`: not on `PATH`
- `supabase/migrations/`: present
- `supabase/seed.sql`: not present

Orphan-prefix scan:

| Path | Prefixes checked | Result |
| --- | --- | --- |
| `supabase/migrations/` | `0xc618`, `0xd593`, `0x3f7afd`, `0xa39a`, `0xd588`, `0xd9bd` | no matches |
| `supabase/seed.sql` | `0xc618`, `0xd593`, `0x3f7afd`, `0xa39a`, `0xd588`, `0xd9bd` | file not present; no local matches |

Candidate column scan from local migrations found address-shaped columns in
profile/account/runtime tables, but no local migration or seed artifact contained
one of the six orphan address prefixes.

## Operator dashboard discovery query

Run this against Supabase project `qajpnuvqlcfseghnldkl` in the dashboard SQL
editor.

```sql
-- run this against project qajpnuvqlcfseghnldkl
SELECT table_schema, table_name, column_name
FROM information_schema.columns
WHERE column_name ILIKE '%address%'
   OR column_name ILIKE '%router%'
   OR column_name ILIKE '%manager%'
   OR column_name ILIKE '%verifier%'
ORDER BY table_schema, table_name;
```

Then for each candidate column, run targeted checks for the six canonical orphan
addresses:

```sql
-- Template: replace <schema>, <table>, and <column> from the discovery query.
SELECT '<schema>' AS table_schema,
       '<table>' AS table_name,
       '<column>' AS column_name,
       <column> AS current_value,
       COUNT(*) AS row_count
FROM <schema>.<table>
WHERE lower(<column>::text) IN (
  '0xd593a8a58bdf7e7448d2dabde0ae3b2bafda1357',
  '0x3f7afd93824ab25f73bdca59afdab560f865b0c3',
  '0xc618dde25f0085f3b2bc3a48ba806f8fc9a93759',
  '0xa39a71a388816d657300effff1857f938aef65d1',
  '0xd588f54ea9e8c40701b419cf6b8de7ae8d1fb22f',
  '0xd9bdff55a886badb011a12c447d72d174fd15964'
)
GROUP BY <column>;
```

## Candidate rows

Live database inspection completed 2026-05-02 via Supabase connector
(project `qajpnuvqlcfseghnldkl`). Orphan-address sweep executed across
104 address-shaped columns in `public` and `alfaclub` schemas (4 chunked
queries). **Result: `NOOP_NO_ORPHAN_ROWS` — 0 rows** matched any of the
6 canonical orphan addresses. Step 14 Supabase UPDATE is not required.

| Table | Column | Classification | Current value | v1.10.1 target | Step 14 action |
| --- | --- | --- | --- | --- | --- |
| — | — | `NOOP_NO_ORPHAN_ROWS` | Live sweep 2026-05-02: 0 of 104 address-shaped columns matched any orphan address across `public` and `alfaclub` schemas. | No post-broadcast action required. | No update needed. |

## Conditional Step 14 update templates

Run only after v1.10.1 broadcast produces the replacement contract addresses,
and only for rows the operator has verified as matching one of the six orphan
addresses. If a candidate row does not match an orphan address, skip the update
and record the verification outcome in broadcast evidence.

```sql
-- Template: replace <schema>, <table>, <column>, <new_value>, and <orphan_value>.
-- Run only after confirming <column> currently equals <orphan_value>.
UPDATE <schema>.<table>
SET <column> = '<new_value>'
WHERE lower(<column>::text) = lower('<orphan_value>');
```

Suggested targets by orphan type:

| Orphan type | Replacement placeholder |
| --- | --- |
| `CreatorLotteryManager` / manager column | `<v1.10.1 manager TBD post-broadcast>` |
| `LotteryAmoeRouter` / router column | `<v1.10.1 router TBD post-broadcast>` |
| `AmoePlonkVerifier` / verifier column | `<v1.10.1 verifier TBD post-broadcast>` |

No Supabase API keys, `DATABASE_URL` contents, connection strings, decrypted
secrets, row deletes, schema migrations, or write operations were used to
produce this plan.
