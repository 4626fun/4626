---
title: v1.19.2 Phase2 source-ahead
---

# v1.19.2 — Phase2Module source-ahead

Status: **source + live module ahead of sealed store seed** (2026-07-17)

## Why

After the v1.19.1 greenfield seal, `DeploymentBatcherPhase2Module` grew for Base
AA95 / vaultKind wiring (`83b247e8e` and follow-ons). Live batcher
`phase2Module()` is hot-swapped to `0x3089678d53522Aa9cE56AF1a34cb32aDBCc690Ba`.

| Plane | Pin |
|-------|-----|
| Sealed store / CURRENT_RELEASE | `v1.19.1` (`codeId` `0xbe8296b2…`, prior module `0x0DDac7f1…`) |
| Source + SOURCE_RELEASE | `v1.19.2` (`codeId` `0x525ceedc…`) |
| Live batcher phase2Module | `0x3089678d…` |

New vault creation bytecode for non-Phase2 keys still matches the sealed
`v1.19.1` manifest. Only `DeploymentBatcherPhase2Module` differs in `v1.19.2`.

## Ops follow-ups

1. Reseed store with `v1.19.2` Phase2Module `codeId` when ready to CREATE2 that
   module from store again (not required while the hot-swapped module remains live).
2. Phase1 `agentVaultCoreModule` still points at creator core — see
   `v1.19.1-vaultkind-wiring-checklist.md`.
