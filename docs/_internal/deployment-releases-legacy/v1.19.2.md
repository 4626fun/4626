---
title: v1.19.2 bytecode cutover
---

# v1.19.2 — unified bytecode epoch

Status: **repo/docs unified; store reseed pending** (2026-07-17)

## What changed

Same Base greenfield addresses as v1.19.1. Source + release guard + frontend
deploy bytecode now track a single tag: `v1.19.2`.

| Contract | Change |
|----------|--------|
| `DeploymentBatcherPhase2Module` | New creation bytecode (AA95 / vaultKind); live module hot-swapped to `0x3089678d…` |
| `CreatorOVaultWrapper` | Creation bytecode drift vs v1.19.1 seal |
| `AgentOVaultWrapper` | Creation bytecode drift vs v1.19.1 seal |

Manifest: `deployments/base/v1.19.2-bytecode-manifest.json`  
Historical seal kept at `deployments/base/v1.19.1-bytecode-manifest.json`.

## Ops before CREATE2-from-store deploys

Reseed UniversalBytecodeStoreV2 `0xF962…` with the three new `codeId`s from the
v1.19.2 manifest (Phase2Module + both wrappers). Until then,
`verify-bytecode-store-seeded.ts` / full release guard will fail on those keys.

Live Phase2/Phase1 modules remain usable via hot-swap without store reseed.
