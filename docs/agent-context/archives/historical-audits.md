# Historical Audits & Epochs (archived)

Cross-cutting operator prefs: [preferences-active.md](../preferences-active.md).

On-demand Tier 2 context. Migrated from `docs/agent-learned-facts.md` (July 2026 context budget split).
Do not load by default — read when a task touches this domain.

## Learned User Preferences

- **Contracts folder slice (July 2026):** canonical paths are `shared/shareoft-mesh/{cca,univ4}`, `shared/vault/recovery/`, `shared/distribution/`; retired `shared/arms`, `shared/mesh`, top-level `shared/recovery|vesting|revenue`, and `shared/strategies/{cca,univ4,launchpad}`. CI guard: `pnpm -C frontend guard:contracts-folder-paths`.

- Public docs should stay **simple, concise, and product + contracts only** — wallet/account internals, security audits, operator runbooks, and historical ops live in `docs/_internal/` (repo-only, not on `docs.4626.fun`); expand the public set slowly in phased batches and refine before adding pages.

- **Fable audit disclosure on `docs.4626.fun`:** surface Fable engagement in the public docs sidebar as a curated, professional Security/Audits section — include available run artifacts and transcripts from `docs/audits/fable/` (sourced from internal workpapers), not a raw dump; copy and IA should read like product-grade security documentation. Published conclusions must use **formal third-party audit language** (document control, scope, limitations, verdict) — not user/agent conversational framing; original engagement prompts may remain only inside appendix source blocks.

- **Aristotle Lean proof targets belong on public docs**, not only in repo workpapers: nest an **Aristotle** subsection under Security & audits with **Introduction** (`/audits/aristotle`), **2.5× boost (proven)** (`/audits/aristotle/curve-boost`), and **Next targets** (`/audits/aristotle/lean-proof-targets`), cross-linked from Fees & lottery / LotteryManager / GaugeController. Public proof pages must stay plain-language readable (plain claim, worked examples, verdict) — not raw Lean dumps. Prefer the top lottery/gauge math set (base win probability `usd/250_000`, post-boost PPM + cap, VRF fairness `r % 1e6 < p`, gauge fee-split conservation 69% / 9.61% burn / 21.39% voters, jackpot payout = 69% of reserve) over ad-hoc proof topics. Gauge fee-split Lean examples must match on-chain residual assignment (ShareOFT residual → burn; vault-share residual → voters) with **no separate dust bucket** and **omit the zero `creatorShareBps` lane** from tuples/tables unless product re-enables that lane.

## Learned Workspace Facts

- **Legacy Twin mint naming is historical only.** The lowercase wrapped-mint PDA tooling, parity verifier, adapter default/env, and operator CLI were removed in July 2026. `docs/_internal/operations/operations/solana/solana-bridge-naming-invariant.md` is retained only to interpret historical deployments and audits; do not use it to provision a creator.

- **July 1 2026 Solidity security audit + Solana strategy cleanup:** full-scope review of all `/contracts` (~103 files, ~26.8k LOC); High findings remediated across 14 files; same pass removed Phase-3 Solana vault strategy contracts/KPR rebalance and aligned docs to Pipe A + Charm/Ajna product truth; canonical audit artifacts at `docs/audits/july-1-2026/` (`index.md`, `audit-report.md`, `remediation.md`); post-remediation risk assessed **Medium-Low** — run `forge test` before deploy.

- **July 2 2026 pre-launch audit (4626-FABLE-2026-07-FINAL):** artifacts at `docs/audits/july-2-2026/` (`audit-report.md`, `remediation.md`, `delta-vs-july-1.md`); post-remediation pass closed H2-01–H2-06 (frontend test harness, deploy-status read-only boundary, settled-truth authority, sweep invariant bypass, Solana relay default-deny) — treat that remediation doc as the fix-status source of truth for v1.15.0 AKITA canary gates.

- **Shared ABI neutralization (July 2026):** lane-shared contracts use lane-neutral selectors — e.g. `Registry4626` `TokenInfo`/`registerToken`/`setOracleForToken`; `CharmStrategy4626` `ASSET`/`assetOracle`/`setAssetOracle`; shared consumers type oracles via `IOracle4626`; `OVaultHubComposer` `configureTokenMesh`/`tokenMesh`; lane-specific wrappers keep creator/agent naming where the asset is explicit.

- **Lottery/VRF/AMOE wiring:** live stack follows **`v1.19.1`**: LotteryManager `0xB45E68a5867935a5734E4185977F81c528006650`, Registry `0x1365e9CEfc516f8A287c51FBaeF96FB4581c6CA2`, Factory `0xCAb65a066A4D52DD29ffB418B319819176b89610`, DeploymentBatcher `0xa18169caf37fa0347285B16aAFC2B09eCB43F145`, store `0xF9622613682a12E46b914c7498716F42E44c4d36`, and CREATE2 deployer `0xe2a8aA094EAf0f9ED05C030E6FcB90B9d139b0e2`. Sources: `docs/reference/addresses.md`, `frontend/src/config/contracts.defaults.ts`, `deployments/base/v1.19.1-bytecode-manifest.json`, and `tmp/base-v1.19.1-handoff.env`. The v1.18.0-greenfield, v1.16.1-share-mesh, and v1.11 greenfield stacks remain superseded broadcast history — do not conflate them when debugging AMOE/lottery wiring.

- **ve■4626 lottery personal mult (locked product math, July 2026):** `ve4626BoostManager.calculateBoostForPosition` uses Curve-style `working = min(0.4·l + 0.6·L·(ve/Ve), l)` and returns tokenless-normalized `boost = working/(0.4·l) ∈ [1.0, 2.5]`. Launch/canary `l` = covered creator Share USD (Share coverage + 0.4→1.0 working factor); `L` = that ShareOFT total supply USD, `ve` = `effectiveVeLotteryOf`, and `Ve` = live total ve4626 power. Covered-path floor is always `BOOST_PRECISION` (1.0×); `setBoostParameters` rejects `baseBoost != 10_000`. LotteryManager applies only the covered-fraction uplift: `1 + (l/swapUSD)·(boost-1)`. Curve caps working balance at `l` (`working/l ∈ [0.4, 1.0]`); the quoted **2.5×** is `working/(0.4·l)` — do not treat `working/l` as ranging to 2.5 or require ve share ≈ 3.5× LP share. No covered position → base odds unchanged. Lock only **■4626** (`wrappedShareOFT`); claim **ve33** / **veLottery** via `ve4626Utility` (not `veVote` / `veChance`). Gauge is separate additive PPM. Leave LM `boostManager` / `vaultGaugeVoting` at 0 until canary ([lottery-canary-checklist-2026-07.md](operations/lottery-canary-checklist-2026-07.md)). Oracle pricing lives in `LotteryManager4626PricingLib` with **windowed** `oracleMaxDeviationBps` + `oracleDeviationWindow` (no permanent lockout; fail-closed on stale/bad oracle). **v2 boost direction:** true Curve/Hermes proportion matching needs user-attributable in-range CREATOR/ETH CL liquidity as `l` (e.g. `CreatorActiveLiquidityRegistry` or share-mesh gauge stake with in-range filter) — not vault guts alone; with pure LP, personal mult is position-based and trade size stays in basePPM. Naming: `docs/contracts/governance/ve-naming.md`. Aristotle delta D1–D3 CONFIRMed on project `46f81830-…` task `6fb0af61-…` (`docs/audits/aristotle/ve4626-curve-boost/`).

- **Aristotle formal-verification audits** live under `docs/audits/aristotle/` (and lane-specific trees such as `docs/audits/CreatorOVault_aristotle/`). When remediating open findings, re-run Aristotle on the touched modules and keep the **AgentTokenV4** lane in the audit set alongside creator/oracle modules — do not treat creator-lane coverage as complete without the agent-token lane. Public curated routes (allowlist + nested Security & audits → Aristotle sidebar): Introduction `/audits/aristotle`, proven Curve boost `/audits/aristotle/curve-boost`, next Lean prompts `/audits/aristotle/lean-proof-targets`. Curve-boost Lean project `46f81830-…` formalizes the 1.0×–2.5× personal mult; it does not replace forge tests, canary wiring, or the full lottery/VRF pipeline.

- **ODA public pins (4626fun/4626):** current auditor source is `main` [`contracts/`](https://github.com/4626fun/4626/tree/main/contracts). Historical tags `audit/oda-2026-07-22`, `audit/oda-2026-07-23-remediated`, `audit/oda-2026-07-28-*` remain immutable. Agent-lane contracts were private-only until 2026-07-28.
