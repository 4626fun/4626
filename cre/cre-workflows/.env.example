## Local CRE workflow simulation (example)
# Copy to `.env` and fill in real values. Never commit `.env`.

# Used by CRE simulate / local runners (DO NOT use real keys in committed files).
CRE_ETH_PRIVATE_KEY=YOUR_PRIVATE_KEY_HEX_WITHOUT_0x

# Example for secret mapping tests only.
MY_SECRET_VALUE=example-value

