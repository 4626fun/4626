# https://docs.github.com/code-security/dependabot/dependabot-version-updates/configuration-options-for-the-dependabot.yml-file
#
# Cost posture: fewer ecosystems, lower PR limits, monthly where possible.
# Each Dependabot PR can re-trigger CI — keep volume low.

version: 2
updates:
  # Primary app surface
  - package-ecosystem: "npm"
    directory: "/frontend"
    schedule:
      interval: "monthly"
    open-pull-requests-limit: 3
    groups:
      frontend-minor-patch:
        patterns:
          - "*"
        update-types:
          - "minor"
          - "patch"

  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "monthly"
    open-pull-requests-limit: 2
    groups:
      root-minor-patch:
        patterns:
          - "*"
        update-types:
          - "minor"
          - "patch"

  - package-ecosystem: "npm"
    directory: "/kpr"
    schedule:
      interval: "monthly"
    open-pull-requests-limit: 2
    groups:
      kpr-minor-patch:
        patterns:
          - "*"
        update-types:
          - "minor"
          - "patch"

  # bun kpr-workflows: disabled — no stable lockfiles and high PR churn.
  # Re-add a single monthly bun entry under /kpr/kpr-workflows if needed.

  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "monthly"
    open-pull-requests-limit: 2
    groups:
      actions:
        patterns:
          - "*"
