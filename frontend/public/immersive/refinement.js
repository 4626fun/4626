/*
 * frontend/public/immersive/refinement.js
 *
 * Stub. The canonical refinement.js holds secondary scroll-trigger
 * refinements layered on top of app.js's main timeline. It was never
 * committed to git (referenced by index.html since fc884c1 but absent
 * from every branch). This empty stub stops the production MIME-type
 * errors; replace wholesale when the real file is recovered from the
 * design team's local working tree.
 */
