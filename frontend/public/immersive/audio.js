/*
 * frontend/public/immersive/audio.js
 *
 * Stub. The canonical audio.js injects a floating bottom-right "Sound"
 * button that toggles a low-volume background score (sarabande.mp3).
 * It was never committed to git (referenced by index.html since fc884c1
 * but absent from every branch). This empty stub stops the production
 * MIME-type errors; replace wholesale when the real file is recovered
 * from the design team's local working tree.
 */
