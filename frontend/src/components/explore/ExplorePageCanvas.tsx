/** Full-page explore backdrop — vault-adjacent flat base + subtle grain (slightly darker than app shell). */
export function ExplorePageCanvas() {
  return <div className="explore-page-canvas pointer-events-none absolute inset-0 z-0" aria-hidden="true" />
}
