/**
 * Display mirrors of server waitlist point awards.
 * Keep even where the server awards even amounts (referral passthrough).
 * Do not invent game-scale numbers — match waitlistPoints / PROVIDER_POINTS / X quest XP.
 */
export const WAITLIST_JOIN_POINTS = 5

export const WAITLIST_REFERRAL_EARN_COPY =
  'Earn when friends join and climb — you get half of their waitlist points.'
