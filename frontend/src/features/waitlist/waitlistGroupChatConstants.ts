/** Delay before auto-syncing the waitlist group after server join completes. */
export const WAITLIST_GROUP_SYNC_DELAY_MS = 12_000

/** Timeout for POST /api/waitlist/xmtp-join. */
export const WAITLIST_JOIN_REQUEST_TIMEOUT_MS = 30_000
