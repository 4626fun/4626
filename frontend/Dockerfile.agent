# --------------------------------------------------------------------------
# 4626 Agent — Production Dockerfile (Keepr + Hermit)
# --------------------------------------------------------------------------
# Runs long-lived agent processes (Keepr XMTP primary or Hermit AlfaClub/Pinata).
# Includes the frontend/ workspace + selected internal packages (notably
# @4626/server-core, which is now imported by command families and many
# server/_lib modules used by the Hermit bridge).
#
# Build:
#   docker build -f frontend/Dockerfile.agent -t 4626-agent .
#
# Run:
#   docker run --env-file frontend/.env -v xmtp-data:/data/.xmtp-data 4626-agent
# --------------------------------------------------------------------------

FROM node:22-bookworm-slim AS base

# Install OS deps for native modules (sharp, XMTP bindings) + gosu for entrypoint
RUN apt-get update && apt-get install -y --no-install-recommends \
    python3 make g++ ca-certificates gosu \
    && rm -rf /var/lib/apt/lists/*

# Enable corepack for pnpm
RUN corepack enable && corepack prepare pnpm@10.15.1 --activate

WORKDIR /app

# --------------------------------------------------------------------------
# Stage 1: Install dependencies
# --------------------------------------------------------------------------
FROM base AS deps

# Copy package manifests + patch files (referenced by pnpm.patchedDependencies)
COPY frontend/package.json frontend/pnpm-lock.yaml ./
# Use the frontend workspace patches directory because pnpm resolves
# `patchedDependencies` paths relative to the package.json being installed
# (frontend/package.json). The repo-root `patches/` is for the Solidity
# workspace and does not contain frontend patches.
COPY frontend/patches/ ./patches/

# Include @4626/server-core (workspace package) so pnpm can resolve the
# workspace:* link during install. This package is now imported by command
# families (e.g. alfaclub.ts) and many server/_lib modules used by the
# Hermit bridge and other agent surfaces.
COPY frontend/packages/server-core/package.json ./packages/server-core/package.json
COPY frontend/packages/server-core/tsconfig.json ./packages/server-core/tsconfig.json
COPY frontend/packages/server-core/src ./packages/server-core/src

# Install ALL deps (tsx, @elizaos/core are in devDependencies).
# Use --ignore-scripts first to avoid OOM from parallel native builds,
# then rebuild native modules sequentially.
RUN pnpm install --frozen-lockfile --ignore-scripts \
    && pnpm rebuild

# --------------------------------------------------------------------------
# Stage 2: Production image
# --------------------------------------------------------------------------
FROM base AS runner

WORKDIR /app

# Copy installed node_modules from deps stage
COPY --from=deps /app/node_modules ./node_modules
COPY --from=deps /app/package.json ./package.json

# Copy the server-core workspace package (source + built artifacts if present).
# This is required at runtime because several command families and _lib modules
# now import from '@4626/server-core'.
COPY --from=deps /app/packages/server-core ./packages/server-core

# Copy server + API + config source used by agent runtime.
# Some agent command modules import Telegram webhook helpers from `api/_handlers/*`,
# so `frontend/api` must be present in the image.
COPY frontend/server ./server
COPY frontend/api ./api
COPY frontend/src/config ./src/config
COPY frontend/src/lib ./src/lib
COPY frontend/src/wallet ./src/wallet
COPY frontend/tsconfig.json frontend/tsconfig.node.json ./

# Create persistent XMTP database directory
# Mount a volume here to preserve agent installations across restarts
RUN mkdir -p /data/.xmtp-data

# Health check port (tiny HTTP server in the agent)
ENV PORT=8080
EXPOSE 8080

# XMTP database persistence
ENV XMTP_DB_DIRECTORY=/data/.xmtp-data

# Entrypoint: fix volume permissions (Railway mounts as root), then drop to node.
# AGENT_PROCESS=hermit runs the AlfaClub/Pinata Hermit lane without booting the
# Keepr XMTP runtime. Default remains Keepr for the existing production service.
CMD ["bash", "-lc", "if [ \"${AGENT_PROCESS:-keepr}\" = \"hermit\" ]; then exec gosu node bash server/agents/hermit/run-hermit.sh; fi; install -d -m 0755 /data/.xmtp-data && find /data/.xmtp-data -xdev -exec chown -h node:node {} + && exec gosu node bash server/agents/eliza/run-agent.sh"]
