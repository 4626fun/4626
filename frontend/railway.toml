# Railway worker deployment config — Subdomain Indexer

[build]
dockerfilePath = "Dockerfile.indexer"

[deploy]
numReplicas = 1
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
startCommand = "npx tsx scripts/subdomain-indexer.ts"
