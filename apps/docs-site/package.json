{
  "name": "creatorvault-docs-site",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "generate:contracts": "cd ../.. && (command -v forge >/dev/null && forge doc || echo 'Skipping forge doc (not installed)')",
    "generate:frontend": "cd ../../frontend && ([ -d node_modules/typedoc ] && pnpm exec typedoc || echo 'Skipping typedoc (dependencies not installed)')",
    "generate:all": "pnpm generate:contracts; pnpm generate:frontend; true",
    "sync-docs": "node scripts/sync-docs.mjs",
    "sync-docs:strict": "node scripts/sync-docs.mjs --strict",
    "api:postprocess": "tsx scripts/postprocess-api-docs.ts",
    "api:postprocess:strict": "tsx scripts/postprocess-api-docs.ts --strict",
    "docusaurus": "docusaurus",
    "start": "pnpm generate:all && pnpm sync-docs && pnpm api:postprocess && docusaurus start",
    "build": "pnpm generate:all && pnpm sync-docs && pnpm api:postprocess && docusaurus build",
    "build:quick": "pnpm sync-docs && pnpm api:postprocess && docusaurus build",
    "build:strict": "pnpm sync-docs:strict && pnpm api:postprocess:strict && docusaurus build",
    "swizzle": "docusaurus swizzle",
    "deploy": "docusaurus deploy",
    "clear": "docusaurus clear",
    "serve": "docusaurus serve",
    "write-translations": "docusaurus write-translations",
    "write-heading-ids": "docusaurus write-heading-ids"
  },
  "dependencies": {
    "@docusaurus/core": "^3.7.0",
    "@docusaurus/preset-classic": "^3.7.0",
    "@docusaurus/theme-mermaid": "^3.7.0",
    "@mdx-js/react": "^3.0.0",
    "clsx": "^2.1.1",
    "prism-react-renderer": "^2.3.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@docusaurus/module-type-aliases": "^3.7.0",
    "@docusaurus/tsconfig": "^3.7.0",
    "@docusaurus/types": "^3.7.0",
    "@types/node": "^22.10.0",
    "fast-glob": "^3.3.2",
    "gray-matter": "^4.0.3",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  },
  "browserslist": {
    "production": [
      ">0.5%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 3 chrome version",
      "last 3 firefox version",
      "last 5 safari version"
    ]
  },
  "engines": {
    "node": ">=18.0"
  }
}
